<?xml version="1.0" encoding="utf-8"?>
<search>
<entry><title>[Web Font] 使用 unicode-range 拆分字体文件</title><url>/htmlcss/20260823/26082304/</url><content><![CDATA[<p>按真实字符集生成字体子集，并通过多个 @font-face 与 unicode-range 让浏览器只下载页面需要的字形，降低中文字体加载成本。</p><a id="more"></a>
<h2 id="为什么中文字体需要拆分"><a href="#为什么中文字体需要拆分" class="headerlink" title="为什么中文字体需要拆分"></a>为什么中文字体需要拆分</h2><p>一个包含常用汉字、拉丁字母和标点的完整字体可能达到数 MB。直接把它作为单个 WOFF2 会阻塞文本稳定显示，也浪费只访问少量页面用户的流量。字体子集把字形拆成多个文件，unicode-range 告诉浏览器每个文件覆盖哪些码点。</p>
<h2 id="unicode-range-决定是否下载"><a href="#unicode-range-决定是否下载" class="headerlink" title="unicode-range 决定是否下载"></a>unicode-range 决定是否下载</h2><p>同一个 font-family 可以声明多次，每条 @font-face 指向不同子集。浏览器扫描页面实际字符；只要某个字符落入范围，就下载对应的整个子集。unicode-range 不会自动裁剪字体文件，src 指向的文件本身也必须只保留相应字形。</p><figure class="highlight css"><table><tr><td class="code"><pre><span class="line">@font-face {</span><br><span class="line">  font-family: &#x27;Blog Sans&#x27;;</span><br><span class="line">  src: url(&#x27;/fonts/blog-latin.woff2&#x27;) format(&#x27;woff2&#x27;);</span><br><span class="line">  font-display: swap;</span><br><span class="line">  unicode-range: U+0000-00FF;</span><br><span class="line">}</span><br><span class="line">@font-face {</span><br><span class="line">  font-family: &#x27;Blog Sans&#x27;;</span><br><span class="line">  src: url(&#x27;/fonts/blog-cjk-01.woff2&#x27;) format(&#x27;woff2&#x27;);</span><br><span class="line">  font-display: swap;</span><br><span class="line">  unicode-range: U+4E00-5FFF;</span><br><span class="line">}</span><br></pre></td></tr></table></figure>
<h2 id="先统计真实字符，再设计分片"><a href="#先统计真实字符，再设计分片" class="headerlink" title="先统计真实字符，再设计分片"></a>先统计真实字符，再设计分片</h2><p>从文章、导航、错误提示和动态接口中收集字符，规范化后去重。拉丁、数字和常用标点单独做基础片；汉字按常用频率或码点区间拆分；品牌名和首屏固定文案可以做极小核心片。不要只扫描构建时 HTML而遗漏用户内容和运行时文案。</p>
<h2 id="用-fonttools-生成真正子集"><a href="#用-fonttools-生成真正子集" class="headerlink" title="用 fonttools 生成真正子集"></a>用 fonttools 生成真正子集</h2><p>pyftsubset 可以按文本或 Unicode 范围保留字形，并输出 WOFF2。保留必要的布局特性和可变字体轴，生成后再用字体检查工具确认 cmap 与字形完整。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pyftsubset Source.ttf \</span><br><span class="line">  --output-file=blog-core.woff2 \</span><br><span class="line">  --flavor=woff2 \</span><br><span class="line">  --text-file=core-chars.txt \</span><br><span class="line">  --layout-features=&#x27;*&#x27; \</span><br><span class="line">  --no-hinting</span><br></pre></td></tr></table></figure>
<h2 id="自动生成-CSS-清单"><a href="#自动生成-CSS-清单" class="headerlink" title="自动生成 CSS 清单"></a>自动生成 CSS 清单</h2><p>构建脚本应同时输出字体文件、内容哈希和 unicode-range 声明，避免文件与范围错配。文件名带哈希并设置长期 immutable 缓存；CSS 或 manifest 记录版本。新增字符时只更新受影响的分片。</p>
<h2 id="加载策略与字体回退"><a href="#加载策略与字体回退" class="headerlink" title="加载策略与字体回退"></a>加载策略与字体回退</h2><p>font-display: swap 让回退字体先显示，optional 更偏向性能但可能本次访问不切换。选择度量接近的系统回退字体，并使用 size-adjust、ascent-override 等描述符减少布局偏移。只预加载确定会在首屏使用的核心片，预加载所有分片会失去按需加载价值。</p>
<h2 id="如何验证是否真的省流量"><a href="#如何验证是否真的省流量" class="headerlink" title="如何验证是否真的省流量"></a>如何验证是否真的省流量</h2><p>为中文、纯英文和混合页面分别录制 Network，确认未出现字符所在分片不会下载。检查字体请求的缓存头、压缩格式、传输体积和 Layout Shift。清空缓存测试首次访问，再测试跨页面复用；只看 Lighthouse 总分无法发现范围错配。</p>
<h2 id="容易踩的坑"><a href="#容易踩的坑" class="headerlink" title="容易踩的坑"></a>容易踩的坑</h2><p>码点范围写对但文件仍是完整字体；拆得太细导致大量请求；动态字符缺失出现豆腐块；不同字重各复制一套巨大分片；预加载与实际 CSS URL 不一致；Service Worker 长期缓存旧字体清单。生产发布应提供字符缺失监控和系统字体兜底。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/%40font-face/unicode-range" target="_blank" rel="external nofollow noopener noreferrer">MDN：unicode-range</a></li><li><a href="https://www.w3.org/TR/css-fonts-4/#unicode-range-desc" target="_blank" rel="external nofollow noopener noreferrer">CSS Fonts Module Level 4</a></li><li><a href="https://fonttools.readthedocs.io/en/latest/subset/index.html" target="_blank" rel="external nofollow noopener noreferrer">fonttools：Subsetting</a></li></ul>]]></content></entry>
<entry><title>[React-Konva] 结合图像分割实现自动抠图编辑器</title><url>/React/20260614/26061403/</url><content><![CDATA[<p>把图像分割模型输出的蒙版转换为透明前景，并用 react-konva 完成拖拽、缩放、背景替换、笔刷修边和高清导出。</p><a id="more"></a>
<h2 id="系统拆成推理与编辑两层"><a href="#系统拆成推理与编辑两层" class="headerlink" title="系统拆成推理与编辑两层"></a>系统拆成推理与编辑两层</h2><p>图像分割负责为每个像素预测前景概率，编辑器负责把蒙版转换为可交互图层。两层通过原图尺寸、蒙版尺寸和坐标变换连接。模型可以在浏览器 Worker、服务端 GPU 或云推理服务运行，UI 不应依赖具体模型实现。</p>
<h2 id="输入图片先做方向与尺寸归一化"><a href="#输入图片先做方向与尺寸归一化" class="headerlink" title="输入图片先做方向与尺寸归一化"></a>输入图片先做方向与尺寸归一化</h2><p>读取文件后校验真实类型和像素总量，处理 EXIF 方向，再生成用于推理的缩略图。编辑仍保留原图；蒙版在导出阶段映射回原尺寸。Object URL 使用结束后及时 revoke，避免大图反复上传造成内存泄漏。</p>
<h2 id="把概率蒙版变成-Alpha"><a href="#把概率蒙版变成-Alpha" class="headerlink" title="把概率蒙版变成 Alpha"></a>把概率蒙版变成 Alpha</h2><p>模型常返回 0 到 1 的概率数组。阈值决定边缘硬度，也可以用平滑曲线保留半透明头发。将原图 RGB 与蒙版 Alpha 写入离屏 Canvas，得到透明前景位图。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">for (let i = 0; i &lt; mask.length; i++) {</span><br><span class="line">  const alpha = Math.max(0, Math.min(1, (mask[i] - 0.35) / 0.3));</span><br><span class="line">  pixels[i * 4 + 3] = Math.round(alpha * 255);</span><br><span class="line">}</span><br><span class="line">offscreenCtx.putImageData(imageData, 0, 0);</span><br></pre></td></tr></table></figure>
<h2 id="用-react-konva-组织图层"><a href="#用-react-konva-组织图层" class="headerlink" title="用 react-konva 组织图层"></a>用 react-konva 组织图层</h2><p>Stage 保存画布坐标系；背景、透明前景、修边蒙版和选区工具放在独立 Layer。React 状态只保存可序列化的变换参数，ImageBitmap、Konva Node 等对象放在 ref。频繁笔刷点位可批量提交，避免每个 pointermove 都触发整棵 React 树更新。</p><figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line">&lt;Stage ref={stageRef} width={viewport.w} height={viewport.h}&gt;</span><br><span class="line">  &lt;Layer listening={false}&gt;&lt;Rect fill={background} /&gt;&lt;/Layer&gt;</span><br><span class="line">  &lt;Layer&gt;&lt;Image image={cutout} {...transform} draggable /&gt;&lt;/Layer&gt;</span><br><span class="line">  &lt;Layer&gt;&lt;Transformer ref={transformerRef} /&gt;&lt;/Layer&gt;</span><br><span class="line">&lt;/Stage&gt;</span><br></pre></td></tr></table></figure>
<h2 id="选择、缩放与坐标换算"><a href="#选择、缩放与坐标换算" class="headerlink" title="选择、缩放与坐标换算"></a>选择、缩放与坐标换算</h2><p>Transformer 需要手动绑定选中的 Konva 节点。缩放结束后把 scaleX/scaleY 合并进业务宽高，再把节点 scale 重置为 1。舞台缩放、图片变换和原图像素是三套坐标系，修边笔刷必须通过节点逆变换映射到蒙版坐标。</p>
<h2 id="修边笔刷修改蒙版"><a href="#修边笔刷修改蒙版" class="headerlink" title="修边笔刷修改蒙版"></a>修边笔刷修改蒙版</h2><p>保留原始概率蒙版和用户修正层。恢复笔刷把修正值设为前景，擦除笔刷设为背景，羽化笔刷改变邻域权重。操作记录为笔画命令以支持撤销；达到一定数量后合并检查点，避免历史无限增长。</p>
<h2 id="高清导出与跨域限制"><a href="#高清导出与跨域限制" class="headerlink" title="高清导出与跨域限制"></a>高清导出与跨域限制</h2><p>导出时按原图分辨率创建离屏舞台或 Canvas，重放背景、变换与最终蒙版。预览只需屏幕分辨率，导出再做高质量重采样。远程图片必须返回允许的 CORS 响应头并以 anonymous 加载，否则 Canvas 会被污染，toDataURL 或读取像素将失败。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">const dataUrl = stageRef.current.toDataURL({</span><br><span class="line">  pixelRatio: originalWidth / stageRef.current.width(),</span><br><span class="line">  mimeType: &#x27;image/png&#x27;</span><br><span class="line">});</span><br></pre></td></tr></table></figure>
<h2 id="性能与失败恢复"><a href="#性能与失败恢复" class="headerlink" title="性能与失败恢复"></a>性能与失败恢复</h2><p>推理放进 Worker，使用可转移的 ImageBitmap 或 ArrayBuffer；大图设置像素上限；快速连续上传时用任务 ID 丢弃旧结果。保存原图引用、模型版本、阈值、变换和笔画命令，刷新后可以恢复编辑，而不是只保存最后一张低清预览。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://ai.google.dev/edge/mediapipe/solutions/vision/image_segmenter/web_js" target="_blank" rel="external nofollow noopener noreferrer">MediaPipe：Image segmentation for web</a></li><li><a href="https://konvajs.org/docs/react/Images.html" target="_blank" rel="external nofollow noopener noreferrer">Konva：Images in React</a></li><li><a href="https://konvajs.org/docs/react/Transformer.html" target="_blank" rel="external nofollow noopener noreferrer">Konva：Transformer in React</a></li><li><a href="https://konvajs.org/docs/react/Canvas_Export.html" target="_blank" rel="external nofollow noopener noreferrer">Konva：Canvas Export</a></li></ul>]]></content></entry>
<entry><title>[Slate.js] 封装一个可扩展的 React 富文本编辑器</title><url>/React/20260322/26032202/</url><content><![CDATA[<p>从 Slate 的节点与操作模型出发，封装工具栏、快捷键、自定义元素、序列化、粘贴处理和表单接入，形成可维护的编辑器组件。</p><a id="more"></a>
<h2 id="为什么选择-Slate"><a href="#为什么选择-Slate" class="headerlink" title="为什么选择 Slate"></a>为什么选择 Slate</h2><p>Slate 提供可组合的编辑器核心和 React 渲染层，不预设固定工具栏与文档结构。自由度适合业务编辑器，但意味着团队要自己定义 Schema、插件顺序、序列化协议和升级策略。官方文档仍将其标记为 beta，生产项目应锁定版本并覆盖关键回归测试。</p>
<h2 id="文档模型先于界面"><a href="#文档模型先于界面" class="headerlink" title="文档模型先于界面"></a>文档模型先于界面</h2><p>编辑器 value 是 Descendant 节点树。元素节点包含 type 和 children，文本节点保存字符串与 bold 等 marks。先定义稳定的业务模型，再写渲染组件；不要把按钮样式或 DOM 结构直接当作持久化格式。</p><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line">type Paragraph = { type: &#x27;paragraph&#x27;; children: CustomText[] };</span><br><span class="line">type Heading = { type: &#x27;heading&#x27;; level: 1 | 2; children: CustomText[] };</span><br><span class="line">type CustomElement = Paragraph | Heading;</span><br><span class="line">type CustomText = { text: string; bold?: boolean; code?: boolean };</span><br></pre></td></tr></table></figure>
<h2 id="创建稳定的-Editor-实例"><a href="#创建稳定的-Editor-实例" class="headerlink" title="创建稳定的 Editor 实例"></a>创建稳定的 Editor 实例</h2><p>Editor 必须在组件生命周期中保持同一个引用。withReact 提供 DOM 映射，withHistory 提供撤销重做，自定义插件负责覆盖 normalizeNode、isInline 或 insertData。插件顺序要固定并写入测试。</p><figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line">const [editor] = useState(() =&gt;</span><br><span class="line">  withLinks(withHistory(withReact(createEditor())))</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line">return &lt;Slate editor={editor} initialValue={value} onChange={onChange}&gt;</span><br><span class="line">  &lt;Toolbar /&gt;</span><br><span class="line">  &lt;Editable renderElement={renderElement} renderLeaf={renderLeaf} /&gt;</span><br><span class="line">&lt;/Slate&gt;;</span><br></pre></td></tr></table></figure>
<h2 id="命令层隔离工具栏"><a href="#命令层隔离工具栏" class="headerlink" title="命令层隔离工具栏"></a>命令层隔离工具栏</h2><p>工具栏不直接操作 DOM，而是调用 toggleBlock、toggleMark、insertLink 等命令。命令读取 selection，使用 Editor.nodes 判断当前状态，再通过 Transforms 修改节点。这样按钮、快捷键和斜杠菜单可以复用同一行为。</p>
<h2 id="渲染、粘贴与链接安全"><a href="#渲染、粘贴与链接安全" class="headerlink" title="渲染、粘贴与链接安全"></a>渲染、粘贴与链接安全</h2><p>renderElement 必须把 attributes 和 children 传给对应元素；renderLeaf 组合文本 marks。粘贴 HTML 时只接受允许的标签和属性，链接协议限制为 http、https、mailto。图片上传先插入带本地 ID 的占位节点，完成后再用 Transforms 更新 URL。</p>
<h2 id="保存与序列化"><a href="#保存与序列化" class="headerlink" title="保存与序列化"></a>保存与序列化</h2><p>保存 Slate JSON 能完整保留结构，适合再次编辑；展示端可以把经过版本迁移的 JSON 渲染为 React，而不是把编辑器 HTML 原样注入页面。文档记录 schemaVersion，模型变化时集中迁移旧数据。防抖保存只处理内容变化，不要因 selection 变化反复请求。</p>
<h2 id="受控边界与表单集成"><a href="#受控边界与表单集成" class="headerlink" title="受控边界与表单集成"></a>受控边界与表单集成</h2><p>Slate 的 value 更接近 initialValue，直接用外部 props 覆盖内部节点会破坏历史记录。表单层保存最新文档引用，在明确的“载入另一篇文档”动作中重建 editor。校验规则应检查空段落、最大节点数、链接和图片状态。</p>
<h2 id="测试最值得覆盖的路径"><a href="#测试最值得覆盖的路径" class="headerlink" title="测试最值得覆盖的路径"></a>测试最值得覆盖的路径</h2><p>测试命令对节点树的变换、normalize 规则的收敛、复制粘贴、中文输入法组合态、撤销重做、序列化往返和历史数据迁移。端到端测试要覆盖光标、选区和键盘快捷键，因为这些行为无法只靠快照证明。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://docs.slatejs.org/" target="_blank" rel="external nofollow noopener noreferrer">Slate：Introduction</a></li><li><a href="https://docs.slatejs.org/libraries/slate-react/editable" target="_blank" rel="external nofollow noopener noreferrer">Slate：Editable Component</a></li><li><a href="https://docs.slatejs.org/libraries/slate-react/react-editor" target="_blank" rel="external nofollow noopener noreferrer">Slate：ReactEditor</a></li></ul>]]></content></entry>
<entry><title>[前端安全] 从 XSS、CSRF 到 CSP 的防护清单</title><url>/javascript/20260118/26011801/</url><content><![CDATA[<p>用数据流和浏览器安全边界理解 XSS、CSRF、点击劫持与供应链风险，并把编码、净化、CSP 和 Cookie 策略落到工程中。</p><a id="more"></a>
<h2 id="威胁从不可信数据开始"><a href="#威胁从不可信数据开始" class="headerlink" title="威胁从不可信数据开始"></a>威胁从不可信数据开始</h2><p>URL 参数、富文本、接口字段、localStorage、postMessage 和第三方脚本都可能成为不可信输入。安全评审要沿着“来源—转换—危险落点”追踪数据，而不是只搜索 script 标签。innerHTML、insertAdjacentHTML、document.write、eval 和字符串形式的定时器都是高风险落点。</p>
<h2 id="XSS-要按输出上下文处理"><a href="#XSS-要按输出上下文处理" class="headerlink" title="XSS 要按输出上下文处理"></a>XSS 要按输出上下文处理</h2><p>React 默认会转义文本插值，但 dangerouslySetInnerHTML 会绕过保护。普通文本使用 textContent；HTML 富文本使用经过配置的净化器；URL 只允许明确协议；不要把用户输入拼进 JavaScript、CSS 或事件属性。不同上下文需要不同编码，统一“替换尖括号”并不可靠。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">title.textContent = untrustedTitle;</span><br><span class="line"></span><br><span class="line">const clean = DOMPurify.sanitize(untrustedHtml, {</span><br><span class="line">  ALLOWED_TAGS: [&#x27;p&#x27;, &#x27;strong&#x27;, &#x27;em&#x27;, &#x27;a&#x27;],</span><br><span class="line">  ALLOWED_ATTR: [&#x27;href&#x27;]</span><br><span class="line">});</span><br></pre></td></tr></table></figure>
<h2 id="CSP-提供第二道防线"><a href="#CSP-提供第二道防线" class="headerlink" title="CSP 提供第二道防线"></a>CSP 提供第二道防线</h2><p>通过响应头部署 CSP，先使用 Report-Only 收集违规，再逐步移除内联脚本和 unsafe-eval。基于 nonce 或 hash 的 strict CSP 能限制脚本执行来源；object-src none 和 base-uri none 可减少绕过面。CSP 不能替代正确编码与净化。</p><figure class="highlight http"><table><tr><td class="code"><pre><span class="line">Content-Security-Policy:</span><br><span class="line">  script-src &#x27;nonce-{RANDOM}&#x27; &#x27;strict-dynamic&#x27;;</span><br><span class="line">  object-src &#x27;none&#x27;;</span><br><span class="line">  base-uri &#x27;none&#x27;;</span><br><span class="line">  frame-ancestors &#x27;none&#x27;</span><br></pre></td></tr></table></figure>
<h2 id="CSRF-与-Cookie-会话"><a href="#CSRF-与-Cookie-会话" class="headerlink" title="CSRF 与 Cookie 会话"></a>CSRF 与 Cookie 会话</h2><p>浏览器会自动携带 Cookie，因此攻击页面可能诱导用户发起带身份的请求。使用 SameSite=Lax 或 Strict、CSRF Token 与 Origin/Referer 校验；修改状态的接口不得使用 GET。XSS 能绕过许多 CSRF 防护，所以两者必须同时治理。</p>
<h2 id="登录凭据与敏感信息"><a href="#登录凭据与敏感信息" class="headerlink" title="登录凭据与敏感信息"></a>登录凭据与敏感信息</h2><p>会话 Cookie 使用 HttpOnly、Secure 和最小 Path/Domain。敏感令牌不写 URL，不进入日志和前端错误上报。退出、改密和风险事件要撤销会话。前端环境变量会被打包进静态文件，不能存放真正的秘密。</p>
<h2 id="第三方依赖与供应链"><a href="#第三方依赖与供应链" class="headerlink" title="第三方依赖与供应链"></a>第三方依赖与供应链</h2><p>锁定依赖版本，在 CI 中审计高风险更新，减少不必要的第三方脚本。外链静态资源可使用 Subresource Integrity；第三方脚本拥有页面同等权限，应通过 CSP、沙箱 iframe 或服务端代理缩小能力。</p>
<h2 id="文件上传与下载"><a href="#文件上传与下载" class="headerlink" title="文件上传与下载"></a>文件上传与下载</h2><p>仅凭扩展名不能判断文件类型。服务端限制大小、解析真实格式、重新编码图片，并把用户文件放在独立域名或强制下载。生成 CSV 时防止公式注入；下载文件设置正确 Content-Type 和 Content-Disposition。</p>
<h2 id="安全回归清单"><a href="#安全回归清单" class="headerlink" title="安全回归清单"></a>安全回归清单</h2><p>为富文本、搜索参数、错误消息和跳转 URL 建立攻击样例；检查响应安全头；测试跨站请求、iframe 嵌入和权限越界；把 CSP 报告、登录异常和依赖告警接入监控。修复必须落在共同组件或安全边界上，避免每个页面重复补丁。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Cross_Site_Scripting_Prevention_Cheat_Sheet.html" target="_blank" rel="external nofollow noopener noreferrer">OWASP：XSS Prevention Cheat Sheet</a></li><li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html" target="_blank" rel="external nofollow noopener noreferrer">OWASP：CSRF Prevention Cheat Sheet</a></li><li><a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CSP" target="_blank" rel="external nofollow noopener noreferrer">MDN：Content Security Policy</a></li></ul>]]></content></entry>
<entry><title>[支付接入] 前端对接支付宝与微信支付的完整流程</title><url>/javascript/20250921/25092103/</url><content><![CDATA[<p>把支付接入拆成创建订单、服务端下单、前端唤起、异步通知、主动查询与退款，建立可信且可恢复的支付状态机。</p><a id="more"></a>
<h2 id="先确定支付场景"><a href="#先确定支付场景" class="headerlink" title="先确定支付场景"></a>先确定支付场景</h2><p>支付宝电脑网站、手机网站和 App 支付，以及微信 JSAPI、小程序、Native、H5 支付的唤起方式不同。前端先根据运行环境选择产品，服务端再调用对应下单接口。商户号、应用私钥、APIv3 密钥和平台证书只能保存在服务端。</p>
<h2 id="订单状态由服务端掌握"><a href="#订单状态由服务端掌握" class="headerlink" title="订单状态由服务端掌握"></a>订单状态由服务端掌握</h2><p>创建业务订单时由服务端根据商品快照计算金额，前端只提交商品与优惠选择。订单至少包含商户订单号、金额、币种、用户、渠道、状态和过期时间。商户订单号作为幂等键，重复点击不得创建多笔待支付订单。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">const result = await fetch(&#x27;/api/orders&#x27;, {</span><br><span class="line">  method: &#x27;POST&#x27;,</span><br><span class="line">  headers: { &#x27;Content-Type&#x27;: &#x27;application/json&#x27; },</span><br><span class="line">  body: JSON.stringify({ skuId, quantity, channel: &#x27;wechat-jsapi&#x27; })</span><br><span class="line">}).then(r =&gt; r.json());</span><br></pre></td></tr></table></figure>
<h2 id="服务端下单，前端只负责唤起"><a href="#服务端下单，前端只负责唤起" class="headerlink" title="服务端下单，前端只负责唤起"></a>服务端下单，前端只负责唤起</h2><p>服务端向支付宝或微信支付发送签名请求，拿到表单、跳转地址、二维码或 prepay_id，再把有限的唤起参数返回前端。微信 JSAPI 参数需要服务端二次签名；支付宝返回的 form 应从可信接口获得，前端不得拼接私钥签名。</p>
<h2 id="前端回调不能判定支付成功"><a href="#前端回调不能判定支付成功" class="headerlink" title="前端回调不能判定支付成功"></a>前端回调不能判定支付成功</h2><p>用户可能关闭页面、网络可能中断，回调参数也可能被伪造。前端唤起结束后显示“正在确认支付”，轮询自己的订单查询接口。最终状态以支付平台异步通知验签后的服务端记录为准；通知可能重复，处理必须幂等。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">async function waitForPaid(orderId) {</span><br><span class="line">  for (let i = 0; i &lt; 10; i++) {</span><br><span class="line">    const order = await fetch(`/api/orders/${orderId}`).then(r =&gt; r.json());</span><br><span class="line">    if (order.status === &#x27;PAID&#x27;) return order;</span><br><span class="line">    if ([&#x27;CLOSED&#x27;, &#x27;REFUNDED&#x27;].includes(order.status)) throw new Error(order.status);</span><br><span class="line">    await new Promise(r =&gt; setTimeout(r, 1500));</span><br><span class="line">  }</span><br><span class="line">  return { status: &#x27;PENDING&#x27; };</span><br><span class="line">}</span><br></pre></td></tr></table></figure>
<h2 id="异步通知必须验签"><a href="#异步通知必须验签" class="headerlink" title="异步通知必须验签"></a>异步通知必须验签</h2><p>服务端保留原始请求体，按渠道规范验证签名、证书序列号、时间戳和随机串，并校验商户号、应用 ID、订单号、金额与币种。先完成幂等状态转换和账务记录，再按规范返回成功响应。验签失败绝不能更新订单。</p>
<h2 id="状态机与补偿任务"><a href="#状态机与补偿任务" class="headerlink" title="状态机与补偿任务"></a>状态机与补偿任务</h2><p>订单可以从 CREATED 进入 PAYING、PAID、CLOSED，退款再进入 REFUNDING、REFUNDED。禁止从 CLOSED 直接变成 PAID 等非法跳转。定时任务主动查询长时间未确认的订单，处理通知丢失；每日对账发现本地与渠道状态不一致。</p>
<h2 id="退款与异常体验"><a href="#退款与异常体验" class="headerlink" title="退款与异常体验"></a>退款与异常体验</h2><p>退款仍由服务端发起并记录唯一退款单号。前端展示“已提交、处理中、已退款、退款失败”，不要在请求超时时直接告诉用户失败。支付按钮提交后立即禁用，但要提供恢复入口；用户返回页面时根据订单号继续查询。</p>
<h2 id="上线前清单"><a href="#上线前清单" class="headerlink" title="上线前清单"></a>上线前清单</h2><p>先在沙箱或测试商户验证成功、取消、超时、重复通知、金额不一致和退款。生产环境限制通知来源并依赖签名验证，监控下单成功率、通知延迟和未知状态。任何密钥都不能进入前端包、仓库或埋点。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://open.alipay.com/module/webApp" target="_blank" rel="external nofollow noopener noreferrer">支付宝开放平台：网页与移动应用</a></li><li><a href="https://pay.wechatpay.cn/doc/v3/merchant/4012791897" target="_blank" rel="external nofollow noopener noreferrer">微信支付：JSAPI/小程序下单</a></li><li><a href="https://github.com/wechatpay-apiv3" target="_blank" rel="external nofollow noopener noreferrer">微信支付 APIv3 SDK</a></li></ul>]]></content></entry>
<entry><title>[登录鉴权] 前端如何安全地使用 JWT</title><url>/javascript/20250518/25051802/</url><content><![CDATA[<p>围绕登录、续期、退出和权限校验，设计一套前端可落地的 JWT 会话方案，并避开 localStorage 与无限刷新等常见陷阱。</p><a id="more"></a>
<h2 id="JWT-解决什么问题"><a href="#JWT-解决什么问题" class="headerlink" title="JWT 解决什么问题"></a>JWT 解决什么问题</h2><p>JWT 是一种紧凑的声明载体，由 Header、Payload 和 Signature 三部分组成。签名用于发现篡改，不会自动加密 Payload，因此令牌中不能放密码、身份证号等敏感数据。JWT 只是令牌格式，完整登录系统还需要签发、存储、续期、撤销和风控策略。</p>
<h2 id="访问令牌与刷新令牌分工"><a href="#访问令牌与刷新令牌分工" class="headerlink" title="访问令牌与刷新令牌分工"></a>访问令牌与刷新令牌分工</h2><p>访问令牌生命周期短，用于调用 API；刷新令牌生命周期更长，只用于换取新访问令牌。前端可以把访问令牌保存在内存，把刷新令牌放入 HttpOnly、Secure、SameSite Cookie，使普通 JavaScript 无法直接读取长期凭据。服务端对刷新令牌做轮换并记录令牌族，发现旧令牌被再次使用时撤销整个会话。</p>
<h2 id="登录与续期流程"><a href="#登录与续期流程" class="headerlink" title="登录与续期流程"></a>登录与续期流程</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">let accessToken = null;</span><br><span class="line"></span><br><span class="line">async function login(credentials) {</span><br><span class="line">  const res = await fetch(&#x27;/auth/login&#x27;, {</span><br><span class="line">    method: &#x27;POST&#x27;,</span><br><span class="line">    credentials: &#x27;include&#x27;,</span><br><span class="line">    headers: { &#x27;Content-Type&#x27;: &#x27;application/json&#x27; },</span><br><span class="line">    body: JSON.stringify(credentials)</span><br><span class="line">  });</span><br><span class="line">  accessToken = (await res.json()).accessToken;</span><br><span class="line">}</span><br></pre></td></tr></table></figure><p>登录响应通过 Set-Cookie 写入刷新令牌，并在响应体返回短期访问令牌。页面刷新后内存令牌消失，应用启动时调用一次 /auth/refresh 恢复会话。</p>
<h2 id="并发-401-只刷新一次"><a href="#并发-401-只刷新一次" class="headerlink" title="并发 401 只刷新一次"></a>并发 401 只刷新一次</h2><p>多个请求同时过期时，如果每个请求都刷新，会产生竞态和令牌轮换失败。用共享 Promise 把并发刷新合并，成功后仅重放一次原请求；刷新仍失败则清空状态并回到登录页。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">let refreshTask;</span><br><span class="line">async function refreshOnce() {</span><br><span class="line">  refreshTask ??= fetch(&#x27;/auth/refresh&#x27;, {</span><br><span class="line">    method: &#x27;POST&#x27;, credentials: &#x27;include&#x27;</span><br><span class="line">  }).then(r =&gt; {</span><br><span class="line">    if (!r.ok) throw new Error(&#x27;session expired&#x27;);</span><br><span class="line">    return r.json();</span><br><span class="line">  }).finally(() =&gt; { refreshTask = null; });</span><br><span class="line">  return refreshTask;</span><br><span class="line">}</span><br></pre></td></tr></table></figure>
<h2 id="前端路由守卫不是权限边界"><a href="#前端路由守卫不是权限边界" class="headerlink" title="前端路由守卫不是权限边界"></a>前端路由守卫不是权限边界</h2><p>前端根据用户声明隐藏按钮、阻止进入页面，只能改善体验。攻击者可以直接调用 API，因此服务端必须在每个受保护资源上验证签名、iss、aud、exp 和权限，并避免接受算法降级。角色权限变化后，短期令牌能缩短旧权限继续生效的时间。</p>
<h2 id="退出与多设备会话"><a href="#退出与多设备会话" class="headerlink" title="退出与多设备会话"></a>退出与多设备会话</h2><p>退出时调用服务端接口撤销当前刷新令牌并清除 Cookie，再清空内存状态。账户安全页应展示设备、最近活动时间和撤销入口。修改密码、检测到令牌重放或管理员禁用账户时，服务端应能撤销相关会话。</p>
<h2 id="前端安全检查"><a href="#前端安全检查" class="headerlink" title="前端安全检查"></a>前端安全检查</h2><p>全站使用 HTTPS；刷新接口检查 Origin 并配置 CSRF 防护；不要把令牌写入 URL、日志和错误监控；不要仅解析 Payload 就当作已验证身份；不要让刷新请求无限循环；敏感操作要求近期重新认证。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://www.rfc-editor.org/rfc/rfc7519.html" target="_blank" rel="external nofollow noopener noreferrer">RFC 7519：JSON Web Token</a></li><li><a href="https://cheatsheetseries.owasp.org/cheatsheets/JSON_Web_Token_for_Java_Cheat_Sheet.html" target="_blank" rel="external nofollow noopener noreferrer">OWASP：JWT Cheat Sheet</a></li><li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html" target="_blank" rel="external nofollow noopener noreferrer">OWASP：Session Management Cheat Sheet</a></li></ul>]]></content></entry>
<entry><title>[HTTP] 前端开发必须掌握的请求、缓存与连接语义</title><url>/javascript/20250216/25021601/</url><content><![CDATA[<p>从一次浏览器请求出发，理解 HTTP 方法、状态码、缓存协商、跨域和不同协议版本对前端性能与正确性的影响。</p><a id="more"></a>
<h2 id="从-URL-到响应：一次请求经过什么"><a href="#从-URL-到响应：一次请求经过什么" class="headerlink" title="从 URL 到响应：一次请求经过什么"></a>从 URL 到响应：一次请求经过什么</h2><p>浏览器解析 URL 后会检查缓存、DNS、连接复用和安全策略，再发送请求。HTTP 定义消息的语义，TLS 保护传输内容，HTTP/2 或 HTTP/3 决定消息如何在连接中承载。前端看到的 Fetch API 是这条链路的上层入口。</p>
<h2 id="方法表达意图，不只是接口命名"><a href="#方法表达意图，不只是接口命名" class="headerlink" title="方法表达意图，不只是接口命名"></a>方法表达意图，不只是接口命名</h2><p>GET 用于读取，HEAD 只获取响应头，POST 提交处理，PUT 替换资源，PATCH 局部修改，DELETE 删除资源。安全方法不应改变服务器状态；幂等方法重复执行应得到相同目标状态。网络重试必须考虑幂等性，支付创建等操作还要使用业务幂等键。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">await fetch(&#x27;/api/profile&#x27;, {</span><br><span class="line">  method: &#x27;PATCH&#x27;,</span><br><span class="line">  headers: { &#x27;Content-Type&#x27;: &#x27;application/json&#x27; },</span><br><span class="line">  body: JSON.stringify({ nickname: &#x27;Wyatt&#x27; })</span><br><span class="line">});</span><br></pre></td></tr></table></figure>
<h2 id="状态码决定前端分支"><a href="#状态码决定前端分支" class="headerlink" title="状态码决定前端分支"></a>状态码决定前端分支</h2><p>2xx 表示请求已被成功处理；304 让浏览器复用缓存正文；400 表示请求本身无效；401 表示缺少有效身份；403 表示身份有效但没有权限；404 表示资源不存在；409 适合表达状态冲突；429 表示请求过多；5xx 表示服务器处理失败。前端不要把所有非 200 响应都归为“网络错误”。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">const response = await fetch(url);</span><br><span class="line">if (response.status === 401) return refreshSession();</span><br><span class="line">if (response.status === 409) return showConflict(await response.json());</span><br><span class="line">if (!response.ok) throw new HttpError(response.status);</span><br></pre></td></tr></table></figure>
<h2 id="强缓存与协商缓存"><a href="#强缓存与协商缓存" class="headerlink" title="强缓存与协商缓存"></a>强缓存与协商缓存</h2><p>Cache-Control: max-age 控制新鲜期，immutable 适合带内容哈希的静态资源；no-cache 允许存储但要求复验；no-store 才是不存储。过期后浏览器可携带 If-None-Match，服务器比较 ETag 并返回 304。HTML 通常短缓存或协商缓存，带哈希的 JS、CSS 和字体可长缓存。</p>
<h2 id="CORS-是浏览器的读取边界"><a href="#CORS-是浏览器的读取边界" class="headerlink" title="CORS 是浏览器的读取边界"></a>CORS 是浏览器的读取边界</h2><p>跨源请求是否允许由响应头决定。非简单请求会先发送 OPTIONS 预检。服务端应精确返回允许的 Origin、方法和请求头；携带 Cookie 时不能使用通配符 Origin。CORS 不是身份认证，也不会阻止服务器收到恶意请求。</p><figure class="highlight http"><table><tr><td class="code"><pre><span class="line">Access-Control-Allow-Origin: https://app.example.com</span><br><span class="line">Access-Control-Allow-Credentials: true</span><br><span class="line">Vary: Origin</span><br></pre></td></tr></table></figure>
<h2 id="HTTP-版本变化了什么"><a href="#HTTP-版本变化了什么" class="headerlink" title="HTTP 版本变化了什么"></a>HTTP 版本变化了什么</h2><p>HTTP/1.1 依靠多个连接缓解队头阻塞；HTTP/2 在单条 TCP 连接上多路复用并压缩头部；HTTP/3 使用 QUIC，在丢包时避免不同流相互等待。核心方法、状态码和缓存语义保持一致。前端优化应优先减少无效资源、正确缓存和控制优先级，而不是把升级协议当作万能加速。</p>
<h2 id="调试清单"><a href="#调试清单" class="headerlink" title="调试清单"></a>调试清单</h2><p>在 DevTools Network 中检查请求方法、状态码、响应时间分段、缓存命中、重定向链、CORS 响应头和协议版本。复现问题时使用“复制为 cURL”，把浏览器策略问题与服务器响应问题分开。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://www.rfc-editor.org/rfc/rfc9110.html" target="_blank" rel="external nofollow noopener noreferrer">RFC 9110：HTTP Semantics</a></li><li><a href="https://www.rfc-editor.org/rfc/rfc9111.html" target="_blank" rel="external nofollow noopener noreferrer">RFC 9111：HTTP Caching</a></li><li><a href="https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API" target="_blank" rel="external nofollow noopener noreferrer">MDN：Fetch API</a></li></ul>]]></content></entry>
<entry><title>[Docker 部署] 从镜像到线上服务的检查清单</title><url>/Deployment/20241020/24102003/</url><content><![CDATA[<p>镜像能运行只是部署的起点。生产服务还需要可观察、可恢复，并且能在版本失败时快速回到上一版。</p><a id="more"></a>
<h2 id="部署前"><a href="#部署前" class="headerlink" title="部署前"></a>部署前</h2><ul><li>镜像使用唯一版本标签并完成漏洞扫描。</li><li>环境变量与密钥由部署平台管理。</li><li>数据库迁移支持向前兼容，先扩展再收缩。</li><li>准备健康检查与回滚命令。</li></ul>
<h2 id="入口与-TLS"><a href="#入口与-TLS" class="headerlink" title="入口与 TLS"></a>入口与 TLS</h2><p>只暴露反向代理的 80/443 端口。TLS 证书自动续期，代理传递真实客户端协议与请求 ID，并设置合理的请求大小和超时。</p>
<h2 id="滚动更新"><a href="#滚动更新" class="headerlink" title="滚动更新"></a>滚动更新</h2><p>新容器通过 readiness 检查后再接收流量，旧容器先停止接收新请求并等待连接结束。单机 Compose 可以采用双实例或短暂维护窗口；多实例平台则使用滚动发布。</p>
<h2 id="日志监控与备份"><a href="#日志监控与备份" class="headerlink" title="日志、监控与备份"></a>日志、监控与备份</h2><p>应用将结构化日志写到标准输出，平台负责收集。至少监控错误率、延迟、CPU、内存和磁盘。数据库备份必须定期执行恢复演练，仅有备份文件并不等于可恢复。</p>
<h2 id="回滚"><a href="#回滚" class="headerlink" title="回滚"></a>回滚</h2><p>保留上一版镜像标签与配置。发布后做一次冒烟测试；错误率异常时先回滚应用，再分析原因。若数据库变更不可逆，应用回滚也可能失败，所以迁移设计必须纳入发布方案。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://docs.docker.com/compose/how-tos/production/" target="_blank" rel="external nofollow noopener noreferrer">Docker：Use Compose in production</a></li><li><a href="https://docs.docker.com/engine/containers/start-containers-automatically/" target="_blank" rel="external nofollow noopener noreferrer">Docker：Start containers automatically</a></li></ul>]]></content></entry>
<entry><title>[React] 性能优化从测量开始</title><url>/React/20240616/24061602/</url><content><![CDATA[<p>性能优化最容易变成“到处加 memo”。更可靠的顺序是先复现卡顿、测量提交，再缩小变化范围。</p><a id="more"></a>
<h2 id="先找出慢在哪里"><a href="#先找出慢在哪里" class="headerlink" title="先找出慢在哪里"></a>先找出慢在哪里</h2><p>使用 React DevTools Profiler 记录一次真实交互，观察哪次提交耗时高、哪些组件重复渲染。浏览器 Performance 面板还能判断问题来自 JavaScript、布局还是网络。</p>
<h2 id="状态放在使用它的最近位置"><a href="#状态放在使用它的最近位置" class="headerlink" title="状态放在使用它的最近位置"></a>状态放在使用它的最近位置</h2><p>把输入框状态放在页面顶层，会让整棵页面树随每个按键更新。状态下沉到表单组件，或把不依赖该状态的内容通过 children 传入，通常比记忆化更直观。</p>
<h2 id="谨慎使用记忆化"><a href="#谨慎使用记忆化" class="headerlink" title="谨慎使用记忆化"></a>谨慎使用记忆化</h2><p><code>memo</code> 适合渲染昂贵且 props 多数时间稳定的组件；<code>useMemo</code> 适合昂贵计算。依赖每次都创建的新对象时，记忆化仍会失效。</p>
<h2 id="列表与代码分割"><a href="#列表与代码分割" class="headerlink" title="列表与代码分割"></a>列表与代码分割</h2><ul><li>长列表优先分页或虚拟化。</li><li>key 使用稳定业务 ID。</li><li>图表、编辑器等大模块按路由或交互延迟加载。</li><li>图片设置明确尺寸，减少布局跳动。</li></ul>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://react.dev/reference/react/memo" target="_blank" rel="external nofollow noopener noreferrer">React：memo</a></li><li><a href="https://react.dev/reference/react/useMemo" target="_blank" rel="external nofollow noopener noreferrer">React：useMemo</a></li></ul>]]></content></entry>
<entry><title>[GitHub Actions] 自动构建并发布 Docker 镜像</title><url>/Deployment/20240218/24021801/</url><content><![CDATA[<p>自动部署的第一步不是登录服务器，而是得到一个可追踪、可重复构建的镜像。工作流应先测试，再构建和发布。</p><a id="more"></a>
<h2 id="触发条件"><a href="#触发条件" class="headerlink" title="触发条件"></a>触发条件</h2><p>Pull Request 只运行检查；合并到主分支可以发布快照标签；正式 release 再发布语义化版本。这样可以避免每次提交都覆盖生产标签。</p>
<h2 id="最小权限"><a href="#最小权限" class="headerlink" title="最小权限"></a>最小权限</h2><p>工作流默认只授予所需权限。发布到 GHCR 通常需要 <code>contents: read</code> 与 <code>packages: write</code>。第三方仓库凭据放在 Secrets 中。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line">permissions:</span><br><span class="line">  contents: read</span><br><span class="line">  packages: write</span><br><span class="line">steps:</span><br><span class="line">  - uses: actions/checkout@v4</span><br><span class="line">  - uses: docker/setup-buildx-action@v3</span><br><span class="line">  - uses: docker/build-push-action@v5</span><br><span class="line">    with:</span><br><span class="line">      push: true</span><br><span class="line">      tags: ghcr.io/example/api:${{ github.sha }}</span><br></pre></td></tr></table></figure>
<h2 id="标签不可只用-latest"><a href="#标签不可只用-latest" class="headerlink" title="标签不可只用 latest"></a>标签不可只用 latest</h2><p>至少保留 commit SHA 标签，出现问题时才能准确回滚。latest 可以作为便利别名，但不能成为唯一版本。</p>
<h2 id="固定第三方-Action"><a href="#固定第三方-Action" class="headerlink" title="固定第三方 Action"></a>固定第三方 Action</h2><p>正式项目可将第三方 Action 固定到完整 commit SHA，并定期由依赖更新工具升级，避免标签被意外修改。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://docs.github.com/en/actions/tutorials/publish-packages/publish-docker-images" target="_blank" rel="external nofollow noopener noreferrer">GitHub：Publishing Docker images</a></li><li><a href="https://docs.github.com/en/actions/security-for-github-actions/security-guides/automatic-token-authentication" target="_blank" rel="external nofollow noopener noreferrer">GitHub：GITHUB_TOKEN</a></li></ul>]]></content></entry>
<entry><title>[Node.js + TypeScript] 组织一个可扩展的后端项目</title><url>/Nodejs/20231112/23111203/</url><content><![CDATA[<p>TypeScript 能检查类型，却不会自动带来清晰架构。真正重要的是模块边界：业务代码不应该直接依赖 Web 框架和数据库实现。</p><a id="more"></a>
<h2 id="按业务模块组织"><a href="#按业务模块组织" class="headerlink" title="按业务模块组织"></a>按业务模块组织</h2><p>相比把所有 controller、service 分成大目录，可以按 users、orders 等业务模块组织，每个模块内部再分 controller、service 和 repository。修改一个功能时，相关文件更集中。</p>
<h2 id="类型不能替代运行时校验"><a href="#类型不能替代运行时校验" class="headerlink" title="类型不能替代运行时校验"></a>类型不能替代运行时校验</h2><p>HTTP 请求、环境变量和数据库 JSON 都来自运行时，必须校验后才能视为可信类型。不要用 <code>as User</code> 跳过边界检查。</p>
<h2 id="业务错误使用明确类型"><a href="#业务错误使用明确类型" class="headerlink" title="业务错误使用明确类型"></a>业务错误使用明确类型</h2><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line">class AppError extends Error {</span><br><span class="line">  constructor(</span><br><span class="line">    readonly code: string,</span><br><span class="line">    readonly status: number,</span><br><span class="line">    message: string</span><br><span class="line">  ) { super(message); }</span><br><span class="line">}</span><br></pre></td></tr></table></figure><p>统一错误类型让控制器只负责把领域错误映射为 HTTP 响应。</p>
<h2 id="构建与运行分开"><a href="#构建与运行分开" class="headerlink" title="构建与运行分开"></a>构建与运行分开</h2><p>开发时使用监听器，CI 中执行类型检查和测试，生产环境只运行编译后的输出。确保构建目录不包含测试文件和源码映射中的敏感路径。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://nodejs.org/en/learn/typescript/introduction" target="_blank" rel="external nofollow noopener noreferrer">Node.js：Introduction to TypeScript</a></li><li><a href="https://www.typescriptlang.org/docs/handbook/intro.html" target="_blank" rel="external nofollow noopener noreferrer">TypeScript Handbook</a></li></ul>]]></content></entry>
<entry><title>[Docker Compose] 组合 Node.js、PostgreSQL 与 Nginx</title><url>/Docker/20230625/23062502/</url><content><![CDATA[<p>Compose 的价值是把多个容器的运行方式写进一个可审查的文件。下面用 API、PostgreSQL 和 Nginx 组成一个最小服务栈。</p><a id="more"></a>
<h2 id="服务间使用服务名通信"><a href="#服务间使用服务名通信" class="headerlink" title="服务间使用服务名通信"></a>服务间使用服务名通信</h2><p>容器内部的数据库地址应写成 <code>db:5432</code>，而不是 localhost。localhost 永远指向当前容器本身。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line">services:</span><br><span class="line">  api:</span><br><span class="line">    build: .</span><br><span class="line">    environment:</span><br><span class="line">      DATABASE_URL: postgres://app:${DB_PASSWORD}@db:5432/app</span><br><span class="line">    depends_on:</span><br><span class="line">      db:</span><br><span class="line">        condition: service_healthy</span><br><span class="line">  db:</span><br><span class="line">    image: postgres:15-alpine</span><br><span class="line">    volumes:</span><br><span class="line">      - postgres-data:/var/lib/postgresql/data</span><br></pre></td></tr></table></figure>
<h2 id="健康检查不是启动顺序"><a href="#健康检查不是启动顺序" class="headerlink" title="健康检查不是启动顺序"></a>健康检查不是启动顺序</h2><p>容器启动不代表服务已经可用。数据库需要 healthcheck，API 也应提供轻量的 <code>/health</code>，让代理和部署平台判断实例状态。</p>
<h2 id="持久化与密钥"><a href="#持久化与密钥" class="headerlink" title="持久化与密钥"></a>持久化与密钥</h2><p>数据库数据放在命名卷中，应用源代码不在生产环境挂载。密码放在宿主机环境或部署平台的密钥系统中，不提交到 Compose 文件。</p>
<h2 id="Nginx-只暴露一个入口"><a href="#Nginx-只暴露一个入口" class="headerlink" title="Nginx 只暴露一个入口"></a>Nginx 只暴露一个入口</h2><p>通常只把 80/443 映射到宿主机，API 和数据库留在内部网络。Nginx 负责 TLS、压缩和反向代理，应用继续专注业务。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://docs.docker.com/compose/" target="_blank" rel="external nofollow noopener noreferrer">Docker Compose</a></li><li><a href="https://docs.docker.com/compose/how-tos/startup-order/" target="_blank" rel="external nofollow noopener noreferrer">Docker：Control startup order</a></li></ul>]]></content></entry>
<entry><title>[Docker] 为 Node.js 应用编写多阶段镜像</title><url>/Docker/20230219/23021901/</url><content><![CDATA[<p>开发环境能运行的 Dockerfile，到了生产环境还要考虑镜像大小、依赖边界、安全身份和关闭信号。多阶段构建可以把编译工具留在构建阶段。</p><a id="more"></a>
<h2 id="先写-dockerignore"><a href="#先写-dockerignore" class="headerlink" title="先写 .dockerignore"></a>先写 .dockerignore</h2><p>排除 <code>node_modules</code>、日志、测试覆盖率和 Git 目录，既减少构建上下文，也避免把本地文件误装进镜像。</p>
<h2 id="多阶段构建"><a href="#多阶段构建" class="headerlink" title="多阶段构建"></a>多阶段构建</h2><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line">FROM node:18-alpine AS build</span><br><span class="line">WORKDIR /app</span><br><span class="line">COPY package*.json ./</span><br><span class="line">RUN npm ci</span><br><span class="line">COPY . .</span><br><span class="line">RUN npm run build</span><br><span class="line"></span><br><span class="line">FROM node:18-alpine AS runtime</span><br><span class="line">WORKDIR /app</span><br><span class="line">ENV NODE_ENV=production</span><br><span class="line">COPY package*.json ./</span><br><span class="line">RUN npm ci --omit=dev &amp;&amp; npm cache clean --force</span><br><span class="line">COPY --from=build /app/dist ./dist</span><br><span class="line">USER node</span><br><span class="line">CMD ["node", "dist/server.js"]</span><br></pre></td></tr></table></figure>
<h2 id="缓存依赖层"><a href="#缓存依赖层" class="headerlink" title="缓存依赖层"></a>缓存依赖层</h2><p>先复制 lockfile 并执行 <code>npm ci</code>，再复制源代码。只要依赖没有变化，Docker 就能复用依赖层。</p>
<h2 id="运行时原则"><a href="#运行时原则" class="headerlink" title="运行时原则"></a>运行时原则</h2><ul><li>使用明确的 Node 主版本或镜像摘要。</li><li>以非 root 用户运行。</li><li>密钥通过运行时环境变量注入。</li><li>应用监听 <code>0.0.0.0</code> 并正确处理 SIGTERM。</li></ul>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://docs.docker.com/guides/nodejs/" target="_blank" rel="external nofollow noopener noreferrer">Docker：Node.js language-specific guide</a></li><li><a href="https://docs.docker.com/build/building/multi-stage/" target="_blank" rel="external nofollow noopener noreferrer">Docker：Multi-stage builds</a></li></ul>]]></content></entry>
<entry><title>[Node.js] 异步错误处理与优雅退出</title><url>/Nodejs/20221030/22103002/</url><content><![CDATA[<p>线上 Node.js 服务最怕“看起来还活着，但已经不能正确处理请求”。错误处理不仅是捕获异常，还包括记录上下文、停止接收新请求和安全释放连接。</p><a id="more"></a>
<h2 id="错误分为可预期与意外"><a href="#错误分为可预期与意外" class="headerlink" title="错误分为可预期与意外"></a>错误分为可预期与意外</h2><p>参数错误、资源不存在属于可预期错误，可以返回明确状态码；空指针、连接池损坏属于意外错误，需要记录完整堆栈，并评估是否重启进程。</p>
<h2 id="异步路由统一交给错误中间件"><a href="#异步路由统一交给错误中间件" class="headerlink" title="异步路由统一交给错误中间件"></a>异步路由统一交给错误中间件</h2><p>每个异步入口都必须把 rejected Promise 交给统一处理层。日志至少包含请求 ID、路径、状态码和错误堆栈，但不能记录令牌与密码。</p>
<h2 id="响应后不要再次写入"><a href="#响应后不要再次写入" class="headerlink" title="响应后不要再次写入"></a>响应后不要再次写入</h2><p>错误中间件先检查 <code>res.headersSent</code>。若响应头已经发送，应把错误交回框架默认处理，避免出现 “headers already sent”。</p>
<h2 id="优雅退出"><a href="#优雅退出" class="headerlink" title="优雅退出"></a>优雅退出</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">process.on('SIGTERM', () =&gt; {</span><br><span class="line">  server.close(async () =&gt; {</span><br><span class="line">    await database.close();</span><br><span class="line">    process.exit(0);</span><br><span class="line">  });</span><br><span class="line">});</span><br></pre></td></tr></table></figure><p>收到部署平台的终止信号后，先停止接收新连接，再等待进行中的请求完成，最后关闭数据库和消息队列连接。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://nodejs.org/api/process.html#signal-events" target="_blank" rel="external nofollow noopener noreferrer">Node.js：Signal events</a></li><li><a href="https://nodejs.org/api/http.html#serverclosecallback" target="_blank" rel="external nofollow noopener noreferrer">Node.js：http.Server.close</a></li></ul>]]></content></entry>
<entry><title>[前端动画] 博客页面渐显效果是怎么实现的</title><url>/javascript/20220717/22071703/</url><content><![CDATA[<p>进入这个博客时，标题、菜单和正文不会生硬地同时出现，而是按顺序滑入并渐显。这个效果并不是一条 CSS 动画完成的，而是由“初始隐藏、启动入口、任务队列、动画执行、无脚本兜底”五个部分共同完成。</p><a id="more"></a>
<h2 id="先看完整执行链路"><a href="#先看完整执行链路" class="headerlink" title="先看完整执行链路"></a>先看完整执行链路</h2>
<p>HTML 根容器带有 <code>use-motion</code> 类。CSS 根据这个类把准备参与动画的元素设为透明。DOM 构建完成后，<code>next-boot.js</code> 调用动画模块，模块再依次处理站点标识、菜单、文章内容和侧边栏。</p>
<ol><li>浏览器解析 HTML，目标元素以 <code>opacity: 0</code> 开始。</li><li><code>DOMContentLoaded</code> 触发 <code>NexT.boot.motion()</code>。</li><li>integrator 将多个动画任务按顺序放入队列。</li><li>Velocity 执行动画；当前任务完成后调用 <code>integrator.next()</code>。</li><li>最后一个任务结束，页面保持正常可见状态。</li></ol>
<h2 id="第一步：用-CSS-准备初始状态"><a href="#第一步：用-CSS-准备初始状态" class="headerlink" title="第一步：用 CSS 准备初始状态"></a>第一步：用 CSS 准备初始状态</h2>
<p>渐显需要从透明状态过渡到不透明状态，因此主题先隐藏文章块、标题、正文和归档标题：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">.use-motion .post-block,</span><br><span class="line">.use-motion .pagination,</span><br><span class="line">.use-motion .comments,</span><br><span class="line">.use-motion .post-header,</span><br><span class="line">.use-motion .post-body,</span><br><span class="line">.use-motion .collection-header {</span><br><span class="line">  opacity: 0;</span><br><span class="line">}</span><br></pre></td></tr></table></figure>
<p><code>use-motion</code> 是一个开关。如果不希望启用动画，只需不添加这个类，正文便不会经历透明状态。把选择器限定在这个开关下面，也能避免动画样式污染其他页面。</p>
<h2 id="第二步：在-DOM-就绪后启动"><a href="#第二步：在-DOM-就绪后启动" class="headerlink" title="第二步：在 DOM 就绪后启动"></a>第二步：在 DOM 就绪后启动</h2>
<p><code>next-boot.js</code> 监听 <code>DOMContentLoaded</code>。此时页面结构已经可以查询，又不必等待图片等资源全部下载，动画能够更早开始。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">window.addEventListener('DOMContentLoaded', () =&gt; {</span><br><span class="line">  NexT.boot.registerEvents();</span><br><span class="line">  NexT.boot.refresh();</span><br><span class="line">  NexT.boot.motion();</span><br><span class="line">});</span><br></pre></td></tr></table></figure>
<p><code>NexT.boot.motion()</code> 会读取配置中的 <code>motion.enable</code>。只有动画开启时才组装并启动任务队列，因此配置层和执行层保持分离。</p>
<h2 id="第三步：用-integrator-控制顺序"><a href="#第三步：用-integrator-控制顺序" class="headerlink" title="第三步：用 integrator 控制顺序"></a>第三步：用 integrator 控制顺序</h2>
<p>页面不是靠多个 <code>setTimeout</code> 猜测执行时间，而是使用一个很小的队列控制器。每个中间任务在完成时主动通知队列继续：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">NexT.motion.integrator</span><br><span class="line">  .add(NexT.motion.middleWares.logo)</span><br><span class="line">  .add(NexT.motion.middleWares.menu)</span><br><span class="line">  .add(NexT.motion.middleWares.postList)</span><br><span class="line">  .add(NexT.motion.middleWares.sidebar)</span><br><span class="line">  .bootstrap();</span><br></pre></td></tr></table></figure>
<p><code>bootstrap()</code> 执行第一个任务。任务拿到 <code>integrator</code> 后，在动画完成回调中调用 <code>integrator.next()</code>。这样即使设备性能和内容长度不同，下一个阶段也只会在上一个阶段真正结束后开始。</p>
<h2 id="第四步：Velocity-负责渐显和滑入"><a href="#第四步：Velocity-负责渐显和滑入" class="headerlink" title="第四步：Velocity 负责渐显和滑入"></a>第四步：Velocity 负责渐显和滑入</h2>
<p>配置中为不同区域指定了效果：文章块使用 <code>fadeIn</code>，文章头部和正文使用 <code>slideDownIn</code>，归档标题使用 <code>slideLeftIn</code>。<code>postList</code> 收集对应节点，然后交给 Velocity：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">Velocity(postBody, 'transition.' + postBodyTransition, {</span><br><span class="line">  stagger: 100,</span><br><span class="line">  drag: true,</span><br><span class="line">  complete() {</span><br><span class="line">    integrator.next();</span><br><span class="line">  }</span><br><span class="line">});</span><br></pre></td></tr></table></figure>
<p><code>stagger: 100</code> 让一组元素错开 100 毫秒开始，形成层次感；<code>drag: true</code> 会调整同组动画的持续时间。真正的“渐显”来自透明度变化，滑动只是额外的位移反馈。</p>
<h2 id="为什么还要-noscript-兜底"><a href="#为什么还要-noscript-兜底" class="headerlink" title="为什么还要 noscript 兜底"></a>为什么还要 noscript 兜底</h2>
<p>如果 JavaScript 被禁用或资源加载失败，元素会永远停在 <code>opacity: 0</code>。因此页面在 <code>noscript</code> 中把透明度恢复为初始值。动画属于增强效果，不能成为阅读正文的前提。</p>
<h2 id="用原生-JavaScript-实现精简版本"><a href="#用原生-JavaScript-实现精简版本" class="headerlink" title="用原生 JavaScript 实现精简版本"></a>用原生 JavaScript 实现精简版本</h2>
<p>现代浏览器可以用 CSS transition 和少量 JavaScript 完成相同思路：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">.reveal {</span><br><span class="line">  opacity: 0;</span><br><span class="line">  transform: translateY(16px);</span><br><span class="line">  transition: opacity .45s ease, transform .45s ease;</span><br><span class="line">}</span><br><span class="line">.reveal.is-visible {</span><br><span class="line">  opacity: 1;</span><br><span class="line">  transform: translateY(0);</span><br><span class="line">}</span><br></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">document.querySelectorAll('.reveal').forEach((element, index) =&gt; {</span><br><span class="line">  element.style.transitionDelay = `${index * 80}ms`;</span><br><span class="line">  requestAnimationFrame(() =&gt; element.classList.add('is-visible'));</span><br><span class="line">});</span><br></pre></td></tr></table></figure>
<p><code>requestAnimationFrame</code> 让浏览器先提交初始样式，再在下一帧添加可见类。若在同一次样式计算中直接添加两个状态，浏览器可能合并更新，看不到过渡过程。</p>
<h2 id="性能与可访问性"><a href="#性能与可访问性" class="headerlink" title="性能与可访问性"></a>性能与可访问性</h2>
<ul><li>优先动画 <code>opacity</code> 和 <code>transform</code>，避免频繁触发布局。</li><li>首屏动画保持简短，正文不应长时间不可见。</li><li>监听 <code>prefers-reduced-motion</code>，为减少动态效果的用户直接显示内容。</li><li>不要在滚动事件中重复启动整页动画。</li><li>动画失败时必须恢复可见状态，避免用户面对空白页面。</li></ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">@media (prefers-reduced-motion: reduce) {</span><br><span class="line">  .reveal {</span><br><span class="line">    opacity: 1;</span><br><span class="line">    transform: none;</span><br><span class="line">    transition: none;</span><br><span class="line">  }</span><br><span class="line">}</span><br></pre></td></tr></table></figure>
<h2 id="实现要点"><a href="#实现要点" class="headerlink" title="实现要点"></a>实现要点</h2>
<p>这套实现把职责拆得很清楚：CSS 负责准备状态，启动模块选择时机，integrator 保证顺序，Velocity 执行具体过渡，<code>noscript</code> 保证内容始终可读。理解这条链路后，即使换掉动画库，也能保留清晰、可靠的页面进入体验。</p>]]></content></entry>
<entry><title>[Node.js] 从零搭建一个可维护的 REST API</title><url>/Nodejs/20220417/22041701/</url><content><![CDATA[<p>能返回 JSON 的服务不等于可维护的 API。随着接口增加，路由、业务规则和数据库操作需要明确分工。</p><a id="more"></a>
<h2 id="三层结构"><a href="#三层结构" class="headerlink" title="三层结构"></a>三层结构</h2><ul><li><strong>route/controller</strong>：读取请求并写回响应。</li><li><strong>service</strong>：处理业务规则，不依赖 HTTP。</li><li><strong>repository</strong>：集中数据库查询。</li></ul><p>控制器保持短小，service 才是主要测试对象。</p>
<h2 id="先校验输入"><a href="#先校验输入" class="headerlink" title="先校验输入"></a>先校验输入</h2><p>进入业务逻辑前校验参数。缺少字段返回 400，没有权限返回 403，资源不存在返回 404；服务器内部错误才使用 500。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">app.post('/api/tasks', async (req, res, next) =&gt; {</span><br><span class="line">  try {</span><br><span class="line">    const input = parseTask(req.body);</span><br><span class="line">    const task = await taskService.create(input);</span><br><span class="line">    res.status(201).json({ data: task });</span><br><span class="line">  } catch (error) { next(error); }</span><br><span class="line">});</span><br></pre></td></tr></table></figure>
<h2 id="统一响应与错误"><a href="#统一响应与错误" class="headerlink" title="统一响应与错误"></a>统一响应与错误</h2><p>成功响应使用稳定的 <code>data</code> 结构，错误响应包含机器可判断的 <code>code</code> 和给用户看的 <code>message</code>。不要把堆栈返回给浏览器。</p>
<h2 id="配置来自环境变量"><a href="#配置来自环境变量" class="headerlink" title="配置来自环境变量"></a>配置来自环境变量</h2><p>端口、数据库地址和密钥应从 <code>process.env</code> 读取，并在启动时一次性校验。缺失关键配置时直接退出，比运行到请求阶段才报错更容易排查。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://nodejs.org/en/learn/getting-started/introduction-to-nodejs" target="_blank" rel="external nofollow noopener noreferrer">Node.js：Introduction</a></li><li><a href="https://nodejs.org/api/process.html#processenv" target="_blank" rel="external nofollow noopener noreferrer">Node.js：process.env</a></li></ul>]]></content></entry>
<entry><title>[React] 从表单逻辑提炼自定义 Hook</title><url>/React/20210905/21090502/</url><content><![CDATA[<p>表单复杂后，输入值、错误信息和提交状态很容易散落在组件各处。自定义 Hook 可以把行为集中起来，让页面组件只负责展示。</p><a id="more"></a>
<h2 id="先定义返回接口"><a href="#先定义返回接口" class="headerlink" title="先定义返回接口"></a>先定义返回接口</h2><p>从调用者需要什么开始：字段值、错误、变更方法和提交方法。稳定的返回接口比一开始追求通用更重要。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">function useForm(initialValues, validate, onSubmit) {</span><br><span class="line">  const [values, setValues] = useState(initialValues);</span><br><span class="line">  const [errors, setErrors] = useState({});</span><br><span class="line">  const [submitting, setSubmitting] = useState(false);</span><br><span class="line">  // change 与 submit 逻辑放在这里</span><br><span class="line">  return { values, errors, submitting, change, submit };</span><br><span class="line">}</span><br></pre></td></tr></table></figure>
<h2 id="校验保持纯函数"><a href="#校验保持纯函数" class="headerlink" title="校验保持纯函数"></a>校验保持纯函数</h2><p><code>validate(values)</code> 只接收数据并返回错误对象，不读取 DOM，也不修改外部变量。这样既容易测试，也方便替换校验规则。</p>
<h2 id="提交状态要覆盖失败"><a href="#提交状态要覆盖失败" class="headerlink" title="提交状态要覆盖失败"></a>提交状态要覆盖失败</h2><p>提交时先校验，再进入 loading。异步请求应放在 <code>try / catch / finally</code> 中，确保失败后按钮能够恢复，并把服务器错误转换成用户可理解的信息。</p>
<h2 id="不要过早抽象"><a href="#不要过早抽象" class="headerlink" title="不要过早抽象"></a>不要过早抽象</h2><p>只有一个简单表单时，本地状态往往更清楚。当相同的字段处理、校验或提交模式出现第二次，再提炼 Hook，接口会更贴近真实需要。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://react.dev/learn/reusing-logic-with-custom-hooks" target="_blank" rel="external nofollow noopener noreferrer">React：Reusing Logic with Custom Hooks</a></li><li><a href="https://react.dev/reference/rules/rules-of-hooks" target="_blank" rel="external nofollow noopener noreferrer">React：Rules of Hooks</a></li></ul>]]></content></entry>
<entry><title>[React] 用 Hooks 整理组件状态与副作用</title><url>/React/20210314/21031401/</url><content><![CDATA[<p>Hooks 让函数组件拥有状态与生命周期能力，但真正的价值不是少写一个 class，而是把同一件事的状态和副作用放在一起。本文用一个搜索列表拆解最常见的写法。</p><a id="more"></a>
<h2 id="状态只保留最小集合"><a href="#状态只保留最小集合" class="headerlink" title="状态只保留最小集合"></a>状态只保留最小集合</h2><p>能由现有数据计算出的值不必再放进 state。输入词和结果需要保存，结果数量可以在渲染时直接计算。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">const [keyword, setKeyword] = useState('');</span><br><span class="line">const [items, setItems] = useState([]);</span><br><span class="line">const resultCount = items.length;</span><br></pre></td></tr></table></figure>
<h2 id="Effect-用于同步外部系统"><a href="#Effect-用于同步外部系统" class="headerlink" title="Effect 用于同步外部系统"></a>Effect 用于同步外部系统</h2><p><code>useEffect</code> 适合请求、订阅和操作浏览器 API。依赖项应完整填写，并在组件卸载或关键词变化时取消旧请求。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">useEffect(() =&gt; {</span><br><span class="line">  const controller = new AbortController();</span><br><span class="line">  fetch(`/api/search?q=${keyword}`, { signal: controller.signal })</span><br><span class="line">    .then(res =&gt; res.json()).then(setItems);</span><br><span class="line">  return () =&gt; controller.abort();</span><br><span class="line">}, [keyword]);</span><br></pre></td></tr></table></figure>
<h2 id="何时抽成自定义-Hook"><a href="#何时抽成自定义-Hook" class="headerlink" title="何时抽成自定义 Hook"></a>何时抽成自定义 Hook</h2><p>当多个组件共享“状态 + 副作用”的组合时，再抽成 <code>useSearch</code>。自定义 Hook 复用的是逻辑，每个调用者仍有独立状态。Hook 必须在组件顶层调用，不能放进条件语句。</p>
<h2 id="检查清单"><a href="#检查清单" class="headerlink" title="检查清单"></a>检查清单</h2><ul><li>不要把可计算值重复存入 state。</li><li>Effect 中使用的响应式值都写入依赖项。</li><li>订阅、计时器和请求都提供清理函数。</li><li>先保证逻辑清楚，再考虑 <code>useMemo</code>。</li></ul>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="https://react.dev/reference/react/useState" target="_blank" rel="external nofollow noopener noreferrer">React：useState</a></li><li><a href="https://react.dev/learn/synchronizing-with-effects" target="_blank" rel="external nofollow noopener noreferrer">React：Synchronizing with Effects</a></li></ul>]]></content></entry>

  <entry>
    <title>在工作中学 Git：rebase -i 指令</title>
    <url>/Git/20200621/438927621/</url>
    <content>
      <![CDATA[<p>上一篇说到 <code>rebase</code> 的用法，那就不得不继续讲 <code>rebase -i</code> 指令，学会这个指令的话就可以随意地对 commit 进行各种修改与排列组合，是我心中的 git 神兵利器之一，以下就让我谈谈我在开发时如何使用这个指令吧！</p>
<a id="more"></a>

<h2 id="什么是-rebase-i"><a href="#什么是-rebase-i" class="headerlink" title="什么是 rebase -i"></a>什么是 rebase -i</h2><p><code>rebase -i</code> 的意思是<strong>互动式 rebase</strong>（git interactive rebase），当使用 <code>rebase</code> 这个指令的时候，后面加上一个 <code>-i</code> 的参数，git 就会打开一个界面（vim 编辑器），让你知道有哪些 commit 会被复制下来，它也会告诉你这些 commit 的编号以及信息。</p>
<p><code>rebase -i</code> 常见的用途包括但不限于：</p>
<ul>
<li>调换 commit 的顺序</li>
<li>修改 commit 的信息</li>
<li>删除一个 commit</li>
<li>把数个 commit 合并在一起</li>
</ul>
<h2 id="指令"><a href="#指令" class="headerlink" title="指令"></a>指令</h2><p>由于 <code>rebase -i</code> 会复制数个 commit，因此使用该指令时，需要指定一个起始的 commit 来决定编辑的范围。注意，在指令中输入的 commit 必须是<strong>起始点的前一个 commit</strong>！</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git rebase -i &lt;commit编号&gt;</span><br></pre></td></tr></table></figure>

<p>那么我们要如何知道 commit 的编号呢？可以使用 <code>git log --oneline</code>，就会出现 commit 历史记录的列表，就能看到各个 commit 的编号以及信息了。</p>
<h3 id="合并数个-commit"><a href="#合并数个-commit" class="headerlink" title="合并数个 commit"></a>合并数个 commit</h3><p>下了上面这一段指令后，终端就会出现一个操作界面（也就是 vim 编辑器），在界面中按 <code>i</code> 表示开始编辑，画面中会出现被你选择的所有 commit，下方会有参数的说明。</p>
<p>以合并数个 commit 为例，第一个 commit 前面请使用 <code>pick</code> 参数，其他要合并的 commits 前面则使用 <code>s</code> （<code>squash</code>）参数。这样一来，使用 <code>s</code> 的 commit 就会合并进入第一个 commit 中。</p>
<p>编辑完 commit 后按 <code>esc</code> → 输入 <code>:wq</code> → 界面会问你要不要更动 commit 信息，不修改的话可直接输入 <code>:wq</code>。</p>
<h2 id="使用情境"><a href="#使用情境" class="headerlink" title="使用情境"></a>使用情境</h2><p>我使用这个指令通常是为了让同一个目的的 commit 集成在一起，避免线图太乱。例如，我在分支上做了一些功能，但累积了很多琐碎的 commit，此时我就会用 <code>rebase -i</code> 将所有 <code>commit</code> 合并为一个，之后才 <code>rebase</code> 上游分支、push 到远端并发 PR。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul>
<li><a href="https://ithelp.ithome.com.tw/articles/10139572" target="_blank" rel="external nofollow noopener noreferrer">30 天精通 Git 版本控管 (23)：修正 commit 过的版本历史记录 Part 5</a></li>
<li><a href="https://github.com/zuopf769/how_to_use_git/blob/master/%E4%BD%BF%E7%94%A8git%20rebase%E5%90%88%E5%B9%B6%E5%A4%9A%E6%AC%A1commit.md" target="_blank" rel="external nofollow noopener noreferrer">使用git rebase合并多次commit</a></li>
</ul>
<hr>
<p>本篇文章只是简单地介绍 <code>rebase -i</code>，并分享我平常如何使用它，但它还有很多丰富的用途是我没有介绍到的，因此我很推荐大家可以延伸阅读我所附的参考资料，会对这个指令的用法有更深入的了解哦！</p>
]]>
    </content>
    <categories>
      <category>Git</category>
    </categories>
    <tags>
      <tag>w3HexSchool</tag>
      <tag>Git</tag>
      <tag>Rebase</tag>
      <tag>GitFlow</tag>
    </tags>
  </entry>
  <entry>
    <title>在工作中学 Git：rebase 指令</title>
    <url>/Git/20200608/1285084183/</url>
    <content>
      <![CDATA[<p>在进入公司以前，我都是用 <code>git merge</code> 来合并分支；直到进入公司后，团队都是用 <code>git rebase</code> 来合并，一开始很不习惯，到最近我才渐渐习惯这种合并方式，因此想把 <code>rebase</code> 指令适用的场景记录下来，跟大家分享。</p>
<a id="more"></a>

<h2 id="什么是-rebase"><a href="#什么是-rebase" class="headerlink" title="什么是 rebase"></a>什么是 rebase</h2><p><code>rebase</code> 是 <code>merge</code> 以外的另一种合并方式，它会取出一连串的 commit，「复制」它们，然后把它们接在别的地方。</p>
<p>跟 <code>merge</code> 的差别在于： <code>merge</code> 会产生额外的 commit 来合并两个分支；而 <code>rebase</code> 则不会有额外的 commit，如果用线图来表示的话，可以看到用 <code>rebase</code> 来合并分支时，线图将会呈现一条直线。</p>
<p>这样一来，我们的 repo 将会有更好看的 commit 记录。</p>
<p>这是用 <code>merge</code> 指令来合并的线图：</p>
<p><img src="https://i.imgur.com/2guwwkO.png" alt="Source: Learn Git Branching"></p>
<p>这是使用 <code>rebase</code> 指令合并过后的线图：</p>
<p><img src="https://i.imgur.com/UH4vBmc.png" alt="Source: Learn Git Branching"></p>
<h2 id="使用目的"><a href="#使用目的" class="headerlink" title="使用目的"></a>使用目的</h2><p>我在工作上使用 rebase 的目的是，为了在远端合并时不发生冲突，需要事先让本地端的 feature branch 的前段长得跟 develop branch 一样，而在后段多出自己添加的 commit。</p>
<h2 id="指令"><a href="#指令" class="headerlink" title="指令"></a>指令</h2><p><code>git rebase 要合并的分支</code></p>
<h2 id="使用情境"><a href="#使用情境" class="headerlink" title="使用情境"></a>使用情境</h2><p><code>feature/add-addon-categories</code> 分支是从 <code>develop</code> 分支分出来的，在该分支做了一些开发后，准备要推到远端、发 PR 给团队成员 code review。在这之前，需要先 <code>rebase</code> 上游分支（<code>develop</code>），确保在远端合并到 <code>develop</code> 时不会发生冲突。</p>
<p>此时，就可以在 <code>feature/add-addon-categories</code> 中下这个指令：<code>git rebase develop</code></p>
<h2 id="冲突"><a href="#冲突" class="headerlink" title="冲突"></a>冲突</h2><p><code>rebase</code> 的过程中跟 <code>merge</code> 一样可能会遇到冲突，解冲突时一个 commit 要解一次，冲突解完后要依循以下步骤：<br><code>git add .</code> ⇒ <code>git rebase --continue</code> ⇒ 下一个冲突 ⇒ 重复。</p>
<p>至于遇到冲突可以怎么解，我就留到之后再分享啦～<del>不然我已经快没梗了</del></p>
<h2 id="状况题"><a href="#状况题" class="headerlink" title="状况题"></a>状况题</h2><h3 id="rebase-完-push-失败，git-提示先-pull"><a href="#rebase-完-push-失败，git-提示先-pull" class="headerlink" title="rebase 完 push 失败，git 提示先 pull"></a>rebase 完 push 失败，git 提示先 pull</h3><p>如果你的分支曾经推到远端过，然后中间又 <code>rebase</code> 其他分支，那之后再 <code>push</code> 时，Git 就会叫你 <code>pull</code> 远端下来再推一次。</p>
<p>这是因为 Git 此时判定本地端的内容比远端的内容还旧，所以才会出现这种提示，然而，<strong>此时不能 <code>pull</code></strong>，不然代码又会变成旧的！</p>
<p>所以这时候的解法就是要用 <code>git push --force-with-lease</code> ，硬推上去更新远端内容。</p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul>
<li><a href="https://backlog.com/git-tutorial/tw/stepup/stepup2_8.html" target="_blank" rel="external nofollow noopener noreferrer">使用 rebase 合并【教学1 使用分支】 | 连猴子都能懂的Git入门指南 | 贝格乐（Backlog）</a></li>
<li><a href="https://gitbook.tw/chapters/branch/merge-with-rebase.html" target="_blank" rel="external nofollow noopener noreferrer">另一种合并方式（使用 rebase） - 为你自己学 Git | 高见龙</a></li>
<li><a href="https://learngitbranching.js.org/?locale=zh_TW" target="_blank" rel="external nofollow noopener noreferrer">Learn Git Branching</a></li>
</ul>
<hr>
<p>都已经讲到 <code>rebase</code> 了，感觉就应该要继续讲 <code>rebase -i</code>（互动式 rebase），不过就让我挪到下礼拜再写吧！<del>因为真的快没题材了</del></p>
]]>
    </content>
    <categories>
      <category>Git</category>
    </categories>
    <tags>
      <tag>w3HexSchool</tag>
      <tag>Git</tag>
      <tag>Rebase</tag>
      <tag>GitFlow</tag>
    </tags>
  </entry>
  <entry>
    <title>[JS] 讲座笔记：JavaScript 设计模式——关注点分离</title>
    <url>/javascript/20200601/270454248/</url>
    <content>
      <![CDATA[<p>本篇笔记来自 2020.4.15 六角学院在线研讨会，讲者为卡斯伯老师。在这一次讲座中，老师讲解了关注点分离作为一种 JavaScript 设计模式是如何运作的。</p>
<a id="more"></a>

<h2 id="何谓关注点分离"><a href="#何谓关注点分离" class="headerlink" title="何谓关注点分离"></a>何谓关注点分离</h2><p>画面归画面、逻辑归逻辑、数据归数据<del>、孙子龟孙子</del></p>
<ul>
<li>画面：HTML 渲染方式，HTML 结构、样式等等</li>
<li>逻辑：处理数据的方法</li>
<li>数据：就是数据</li>
</ul>
<p>MVC 也是属于此概念。</p>
<h2 id="开发技巧"><a href="#开发技巧" class="headerlink" title="开发技巧"></a>开发技巧</h2><ol>
<li>将数据拆分出来</li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> data = [</span><br><span class="line">  &#123;</span><br><span class="line">    first: <span class="string">'Mark'</span>,</span><br><span class="line">    last: <span class="string">'Otto'</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    first: <span class="string">'Jacob'</span>,</span><br><span class="line">    last: <span class="string">'Thornton'</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    first: <span class="string">'Larry'</span>,</span><br><span class="line">    first: <span class="string">'the Bird'</span></span><br><span class="line">  &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>一个区域维持一种渲染模式（大多数框架就是让开发者省去这段流程）</li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> htmlString= <span class="string">''</span>;</span><br><span class="line">  data.forEach( <span class="function">(<span class="params">item, i</span>) =&gt;</span> &#123;</span><br><span class="line">    htmlString = htmlString + <span class="string">`&lt;tr&gt;</span></span><br><span class="line"><span class="string">        &lt;th scope="row"&gt;<span class="subst">$&#123; i + <span class="number">1</span> &#125;</span>&lt;/th&gt;</span></span><br><span class="line"><span class="string">        &lt;td&gt;<span class="subst">$&#123; item.first &#125;</span>&lt;/td&gt;</span></span><br><span class="line"><span class="string">        &lt;td&gt;<span class="subst">$&#123; item.last &#125;</span>&lt;/td&gt;</span></span><br><span class="line"><span class="string">        &lt;td&gt;&lt;button class="btn btn-outline-danger" data-id="<span class="subst">$&#123;i&#125;</span>"&gt;删除&lt;/button&gt;&lt;/td&gt;</span></span><br><span class="line"><span class="string">      &lt;tr&gt;`</span></span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>逻辑不会与渲染方式混在一起<br>此部分重点在于<strong>数据处理的熟练度</strong></li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">removeItem</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> id = e.target.dataset.id;</span><br><span class="line">  data.splice(id, <span class="number">1</span>);</span><br><span class="line">  render();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">addItem</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> newItem = &#123;</span><br><span class="line">    first: <span class="string">'Greta'</span>,</span><br><span class="line">    last: <span class="string">'Ma'</span></span><br><span class="line">  &#125;</span><br><span class="line">  data.push(newItem);</span><br><span class="line">  render();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p><a href="https://paper.dropbox.com/doc/0415-JavaScript-bXc2gUg81pibTQMmAvmjL" target="_blank" rel="external nofollow noopener noreferrer">问答会共笔</a></p>
]]>
    </content>
    <categories>
      <category>JavaScript</category>
    </categories>
    <tags>
      <tag>w3HexSchool</tag>
      <tag>SeparationOfConcerns</tag>
    </tags>
  </entry>
  <entry>
    <title>[Gulp] 在 Gulp 项目中引入 EJS template</title>
    <url>/Gulp/20200525/2363080042/</url>
    <content>
      <![CDATA[<p>这篇文章是我在开始工作前学习 Gulp 与 EJS 时的笔记，现在的公司虽然不是用 Gulp 也不是用 EJS，而是采用一个电商后台服务 Shopify 自创的模板语言（liquid），但因为同为模板语言，因此让我想起曾写过这篇跟 EJS 有关的笔记，所以就拿来发铁人赛啦～</p>
<a id="more"></a>

<p>如何在 Gulp 项目中安装 EJS 可以参考<a href="https://hsiangfeng.github.io/gulp/20190613/2073498620/" target="_blank" rel="external nofollow noopener noreferrer">这是在讲 Gulp 不是饮料是任务自动化工具这件事之番外篇：加入 ejs 篇</a>。</p>
<p>以下内容关于是如何在项目中撰写 EJS。</p>
<h2 id="gulpfile-js"><a href="#gulpfile-js" class="headerlink" title="gulpfile.js"></a>gulpfile.js</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ejs block</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ejs</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> gulp.src([<span class="string">'./src/templates/**.ejs'</span>, <span class="string">'!./src/templates/**/_*.ejs'</span>])</span><br><span class="line">    .pipe($.ejs(&#123;</span><br><span class="line">      msg: <span class="string">'Hello Gulp!'</span>,</span><br><span class="line">    &#125;))</span><br><span class="line">    .pipe($.rename(&#123; <span class="attr">extname</span>: <span class="string">'.html'</span> &#125;))</span><br><span class="line">    .pipe($.<span class="keyword">if</span>(options.env === <span class="string">'prod'</span>, $.htmlmin(&#123; <span class="attr">collapseWhitespace</span>: <span class="literal">true</span> &#125;)))</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./public/'</span>))</span><br><span class="line">    .pipe(browserSync.stream());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// watch files</span></span><br><span class="line">gulp.task(<span class="string">'watch'</span>, gulp.parallel(<span class="string">'browser-sync'</span>, () =&gt; &#123;</span><br><span class="line">  <span class="comment">// 改第一行</span></span><br><span class="line">  gulp.watch(<span class="string">'./src/templates/**/*.ejs'</span>, gulp.series(<span class="string">'ejs'</span>));</span><br><span class="line">  gulp.watch(<span class="string">'./src/sass/**/*.scss'</span>, gulp.series(<span class="string">'scss'</span>));</span><br><span class="line">  gulp.watch(<span class="string">'./src/js/**/*.js'</span>, gulp.series(<span class="string">'babel'</span>));</span><br><span class="line">&#125;));</span><br></pre></td></tr></table></figure>

<p>如果有需要加载其他软件包的 js 文件，需要再写一个段落来集成。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">vendorsJs</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> gulp.src([</span><br><span class="line">    <span class="string">'./node_modules/jquery/dist/**/jquery.min.js'</span>,</span><br><span class="line">    <span class="string">'./node_modules/popper.js/dist/umd/**/popper.min.js'</span>,</span><br><span class="line">    <span class="string">'./node_modules/bootstrap/dist/js/**/bootstrap.bundle.min.js'</span>,</span><br><span class="line">    <span class="string">'./node_modules/wowjs/dist/wow.min.js'</span>,</span><br><span class="line">  ])</span><br><span class="line">    .pipe($.concat(<span class="string">'vendors.js'</span>))</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./public/javascript'</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="项目文件夹结构"><a href="#项目文件夹结构" class="headerlink" title="项目文件夹结构"></a>项目文件夹结构</h2><figure class="highlight 1c"><table><tr><td class="code"><pre><span class="line">src/</span><br><span class="line">  <span class="string">|-- templates/</span></span><br><span class="line">        <span class="string">|-- index/</span></span><br><span class="line">        <span class="string">|-- layout/</span></span><br><span class="line">        <span class="string">|-- index.ejs</span></span><br></pre></td></tr></table></figure>

<ul>
<li>index 文件夹：各区域的 .ejs 文件，每个文件里面的内容都是用 HTML 标签撰写的，且每个文件名开头都是 <code>_</code>，例如 <code>_footer.ejs</code>。</li>
<li>layout 文件夹：放一些共用的内容，例如 <code>&lt;head&gt;</code> 中的信息、所有要加载的 CDN <code>&lt;script&gt;</code> 等。</li>
<li>index.ejs：把以上两个文件夹中所有 .ejs 排成一个完整的网页。</li>
</ul>
<h2 id="index-ejs"><a href="#index-ejs" class="headerlink" title="index.ejs"></a>index.ejs</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">%-</span> <span class="attr">include</span> ('<span class="attr">.</span>/<span class="attr">layout</span>/<span class="attr">_head.ejs</span>') %&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- header &amp; solvable --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">%-</span> <span class="attr">include</span> ('<span class="attr">.</span>/<span class="attr">layout</span>/<span class="attr">_header.ejs</span>') %&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- capable --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">%-</span> <span class="attr">include</span> ('<span class="attr">.</span>/<span class="attr">index</span>/<span class="attr">_capable.ejs</span>') %&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- formula --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">%-</span> <span class="attr">include</span> ('<span class="attr">.</span>/<span class="attr">index</span>/<span class="attr">_formula.ejs</span>') %&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- footer --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">%-</span> <span class="attr">include</span> ('<span class="attr">.</span>/<span class="attr">index</span>/<span class="attr">_footer.ejs</span>') %&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- script --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">%-</span> <span class="attr">include</span> ('<span class="attr">.</span>/<span class="attr">layout</span>/<span class="attr">_script.ejs</span>') %&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>引入其他的 .ejs 文件，要用 <code>&lt;%- include (&#39;路径&#39;) %&gt;</code> 标签。</p>
]]>
    </content>
    <categories>
      <category>Gulp</category>
    </categories>
    <tags>
      <tag>w3HexSchool</tag>
      <tag>Gulp</tag>
      <tag>EJS</tag>
    </tags>
  </entry>
  <entry>
    <title>[JS] JavaScript ES6 常用语法笔记</title>
    <url>/javascript/20200504/221423942/</url>
    <content>
      <![CDATA[<p>一直以为有发过这篇，结果竟然没有！<br>学习 JavaScript 这么久了，笔记中当然不能少了 ES6 常用语法啰～</p>
<a id="more"></a>

<h2 id="变量声明"><a href="#变量声明" class="headerlink" title="变量声明"></a>变量声明</h2><h3 id="var-与-let-的区别"><a href="#var-与-let-的区别" class="headerlink" title="var 与 let 的区别"></a><code>var</code> 与 <code>let</code> 的区别</h3><ol>
<li>是否会提升<br>当我们分别用 <code>var</code> 跟 <code>let</code> 并在声明变量之前调用变量时，<code>var</code> 会显示 <code>undefined</code>，代表内存有预先准备空间；<code>let</code> 则会显示 <code>not defined</code>，代表内存没有预先准备空间。</li>
<li>作用域不同<br><code>var</code> 是以 function 做分隔，<code>let</code> 是以 block ( <code>{}</code> ) 做分隔。</li>
</ol>
<h3 id="const"><a href="#const" class="headerlink" title="const"></a><code>const</code></h3><p>代表常量，唯读，不可再次赋值。但是如果用 <code>const</code> 声明一个对象，可以在之后修改或添加该对象内部的<strong>特性</strong>，因为对象是传址（参考）而不是传值，所以可以这样做。其他特色与 <code>let</code> 一样。</p>
<h3 id="循环内的-setTimeout"><a href="#循环内的-setTimeout" class="headerlink" title="循环内的 setTimeout()"></a>循环内的 <code>setTimeout()</code></h3><p>假如我们希望用 <code>setTimeout</code> 配合循环输出 1~10，而且每次输出都延迟 10 毫秒：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>( <span class="keyword">var</span> i=<span class="number">0</span>; i&lt;<span class="number">10</span>; i++)&#123;</span><br><span class="line">  setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    comsole.log(i);</span><br><span class="line">  &#125;,<span class="number">10</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此时循环的计数器 <code>i</code> 是用 <code>var</code> 来声明，则 <code>setTimeout</code> 得到的数字不会是 1~10，而是得到最后的数字 10，为什么会这样呢？</p>
<p>因为 JS 异步的特点，所以 <code>for</code> 循环并不会等到 <code>setTimeout()</code> 结束后才继续，而是一口气跑完 1~10；又因为 <code>var</code> 的作用域是以 function 做切分，<code>setTimeout()</code> 的 <code>i</code> 会从外层去取值，此时的 <code>i</code> 已经是跑完 <code>for</code> 循环的 10 了。</p>
<p>解决方法是改用 <code>let</code> 声明计数器，这样 <code>setTimeout</code> 得到的就会是当下循环跑的数字。</p>
<h4 id="另一种解法：立即函数-IIFE"><a href="#另一种解法：立即函数-IIFE" class="headerlink" title="另一种解法：立即函数 (IIFE)"></a>另一种解法：立即函数 (IIFE)</h4><p>除了改用 <code>let</code> 声明以外，也可以用<strong>立即函数</strong>包住 <code>setTimeout</code>，并将循环的 <code>i</code> 作为参数传入立即函数中，保留当下的 <code>i</code> 一笔笔输出数字。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 立即函数语法</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="comment">// 做某事</span></span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 用立即函数包住 setTimeout()</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i=<span class="number">0</span>; i&lt;<span class="number">10</span>; i++)&#123;  </span><br><span class="line">  (<span class="function"><span class="keyword">function</span>(<span class="params">x</span>)</span>&#123;</span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'这执行第'</span>+ x +<span class="string">'次'</span>);</span><br><span class="line">    &#125;,<span class="number">10</span>);</span><br><span class="line">  &#125;)(i);  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="展开符号-…"><a href="#展开符号-…" class="headerlink" title="展开符号 …"></a>展开符号 <code>…</code></h2><p>展开符号能够把数组与类数组里的内容给取出来。以下说明展开符号的几个用途。</p>
<h3 id="合并数个数组"><a href="#合并数个数组" class="headerlink" title="合并数个数组"></a>合并数个数组</h3><p>在一个新声明的数组中，把展开符号 <code>...</code> 加在要合并的数组前方，这种合并的方式与使用 <code>concat()</code> 的效果是一样的。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> groupA = [<span class="string">'小明'</span>, <span class="string">'杰伦'</span>, <span class="string">'阿姨'</span>];</span><br><span class="line"><span class="keyword">let</span> groupB = [<span class="string">'老妈'</span>, <span class="string">'老爸'</span>];</span><br><span class="line"><span class="keyword">let</span> groupAll = [...groupA, ...groupB];</span><br><span class="line"><span class="built_in">console</span>.log(groupAll);</span><br><span class="line"><span class="comment">// ['小明', '杰伦', '阿姨','老妈', '老爸']</span></span><br></pre></td></tr></table></figure>

<h3 id="复制数组（浅复制）"><a href="#复制数组（浅复制）" class="headerlink" title="复制数组（浅复制）"></a>复制数组（浅复制）</h3><p>数组与对象一样，都是传址（传参考），所以如果将 B 数组指向 A数组，A、B 数组会被视为同一个实体。如此一来，当我们想要修改 B 数组时，就会连 A 数组的内容也修改到。这叫做深复制。</p>
<p>如果只想修改 B 数组的话，就可以用展开符号来复制 A 数组，再进行操作。只传值的复制方式，就是浅复制。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> groupA = [<span class="string">'小明'</span>, <span class="string">'杰伦'</span>, <span class="string">'阿姨'</span>];</span><br><span class="line"><span class="keyword">let</span> groupB = [...groupA] <span class="comment">// 新数组</span></span><br><span class="line">groupB.push(<span class="string">'阿明'</span>);</span><br><span class="line"><span class="built_in">console</span>.log(groupA); <span class="comment">// 不会有阿明</span></span><br></pre></td></tr></table></figure>

<h3 id="类数组"><a href="#类数组" class="headerlink" title="类数组"></a>类数组</h3><p>有些东西外观很像数组，也具有一些数组的特性，但却不是真正的数组，称为类数组，例如用 <code>querySelectorAll()</code> 取出来的 <code>NodeList</code> 会以类数组的形式呈现所有被选择的节点，但它却不是真正的数组。</p>
<p>在新的空数组中，通过展开符号可以复制类数组中的内容，借此将类数组<strong>转为真正的数组</strong>。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> doms = <span class="built_in">document</span>.querySelectorAll(<span class="string">'li'</span>); <span class="comment">// doms 为类数组</span></span><br><span class="line"><span class="keyword">let</span> newDoms = [...doms]; <span class="comment">// newDoms 为数组</span></span><br></pre></td></tr></table></figure>

<p>另一个类数组的例子是函数内置的 <code>arguments</code> 对象，<code>arguments</code> 会包含所有传入函数的参数。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> originCash = <span class="number">1000</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">updateEasyCard</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">// let arg = arguments;</span></span><br><span class="line">  <span class="keyword">let</span> arg = [...arguments]; <span class="comment">// 转成真正的数组</span></span><br><span class="line">  <span class="keyword">let</span> sum = arg.reduce(<span class="function"><span class="keyword">function</span> (<span class="params">accumulator, currentValue</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> accumulator + currentValue;</span><br><span class="line">    &#125;, <span class="number">0</span>);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'我有 '</span> + sum + <span class="string">' 元'</span>);</span><br><span class="line"> &#125;</span><br><span class="line">updateEasyCard(<span class="number">0</span>); <span class="comment">// 我有 1000 元</span></span><br><span class="line">updateEasyCard(<span class="number">10</span>, <span class="number">50</span>, <span class="number">100</span>, <span class="number">50</span>, <span class="number">5</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">500</span>); <span class="comment">// 我有 718 元</span></span><br></pre></td></tr></table></figure>

<h3 id="其余参数"><a href="#其余参数" class="headerlink" title="其余参数"></a>其余参数</h3><p>如果不确定之后要传入函数的参数数量，可以在函数的<strong>唯一一个</strong>或<strong>最后一个</strong>参数前面加上展开符号（该参数就会变成其余参数），就能把多传入的参数值以<strong>数组</strong>的形式，容纳在其余参数中。</p>
<p>注意，函数里只能有一个其余参数，而且一定要放在最右边。</p>
<p>参照：<a href="https://developer.mozilla.org/zh-TW/docs/Web/JavaScript/Reference/Functions/rest_parameters" target="_blank" rel="external nofollow noopener noreferrer">其余参数 - JavaScript | MDN</a></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">moreMoney</span>(<span class="params">...money</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(money);</span><br><span class="line">  <span class="comment">// [100, 200, 300]</span></span><br><span class="line">&#125;</span><br><span class="line"> moreMoney(<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">moreMoney</span>(<span class="params">name, ...money</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(name, money);</span><br><span class="line">  <span class="comment">// "Grete" [100, 200, 300, 400, 500]</span></span><br><span class="line">&#125;</span><br><span class="line"> moreMoney(‘Grete’, <span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>, <span class="number">400</span>, <span class="number">500</span>);</span><br></pre></td></tr></table></figure>

<h2 id="解构赋值"><a href="#解构赋值" class="headerlink" title="解构赋值"></a>解构赋值</h2><p>解构赋值语法可以将数组或对象中的数据取出成独立变量。<br>基本上就是把等号右边的值，应用到等号左边。</p>
<h3 id="数组解构赋值"><a href="#数组解构赋值" class="headerlink" title="数组解构赋值"></a>数组解构赋值</h3><h4 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h4><p>在之前，我们如果要修改数组内的元素，都只能用 <code>=</code> 一个一个直接赋值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> family = [<span class="string">'小明'</span>, <span class="string">'杰伦'</span>, <span class="string">'阿姨'</span>, <span class="string">'老妈'</span>, <span class="string">'老爸'</span>];</span><br><span class="line"><span class="keyword">let</span> ming = family[<span class="number">0</span>];</span><br><span class="line"><span class="keyword">let</span> jay = family[<span class="number">1</span>];</span><br><span class="line"><span class="keyword">let</span> auntie = family[<span class="number">2</span>];</span><br></pre></td></tr></table></figure>

<p>而现在可以用解构的方式，一次对数组内所有元素赋值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> family = [<span class="string">'小明'</span>, <span class="string">'杰伦'</span>, <span class="string">'阿姨'</span>, <span class="string">'老妈'</span>, <span class="string">'老爸'</span>];</span><br><span class="line"><span class="keyword">let</span> [ming, jay, aunt, mom, dad] = family;</span><br><span class="line"><span class="comment">// 左边是新变量，右边是要赋予的值</span></span><br><span class="line"><span class="built_in">console</span>.log(ming, jay, aunt, mom, dad);</span><br><span class="line"><span class="comment">// "小明" "杰伦" "阿姨" "老妈" "老爸"</span></span><br></pre></td></tr></table></figure>

<h4 id="当输入的变量少于所给的值"><a href="#当输入的变量少于所给的值" class="headerlink" title="当输入的变量少于所给的值"></a>当输入的变量少于所给的值</h4><p>如果缺少的变量是数组最后的值，那就会因为没有被赋予变量而查不出来。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> family = [<span class="string">'小明'</span>, <span class="string">'杰伦'</span>, <span class="string">'阿姨'</span>, <span class="string">'老妈'</span>, <span class="string">'老爸'</span>];</span><br><span class="line"><span class="keyword">let</span> [ming, jay, aunt] = family;</span><br><span class="line"><span class="built_in">console</span>.log(ming, jay, aunt);</span><br><span class="line"><span class="comment">// "小明" "杰伦" "阿姨"</span></span><br></pre></td></tr></table></figure>

<p>如果缺少的变量是在数组中间的话，缺少的变量要<strong>留空</strong>，此时其他变量依然可以按照对应的数组元素赋值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> family = [<span class="string">'小明'</span>, <span class="string">'杰伦'</span>, <span class="string">'阿姨'</span>, <span class="string">'老妈'</span>, <span class="string">'老爸'</span>];</span><br><span class="line"><span class="keyword">let</span> [ming, jay, , mom, dad] = family;</span><br><span class="line"><span class="built_in">console</span>.log(ming, jay, mom, dad);</span><br><span class="line"><span class="comment">// "小明" "杰伦" "老妈" "老爸"</span></span><br></pre></td></tr></table></figure>

<h4 id="当输入的变量多于所给的值"><a href="#当输入的变量多于所给的值" class="headerlink" title="当输入的变量多于所给的值"></a>当输入的变量多于所给的值</h4><p>如果变量的数量多于赋予的值时，多出来的那个变量会被赋予 <code>undefined</code> 的值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> family = [<span class="string">'小明'</span>, <span class="string">'杰伦'</span>, <span class="string">'阿姨'</span>, <span class="string">'老妈'</span>, <span class="string">'老爸'</span>];</span><br><span class="line"><span class="keyword">let</span> [ming, jay, aunt, mom, dad, sis] = family;</span><br><span class="line"><span class="built_in">console</span>.log(ming, jay, aunt, mom, dad, sis);</span><br><span class="line"><span class="comment">// "小明" "杰伦" "阿姨" "老妈" "老爸" undefined</span></span><br></pre></td></tr></table></figure>

<h4 id="变量交换"><a href="#变量交换" class="headerlink" title="变量交换"></a>变量交换</h4><p>使用解构赋值可以实时交换两个变量的值，不需要通过声明第三个变量来达成。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> fruit = <span class="string">'banana'</span>;</span><br><span class="line"><span class="keyword">let</span> veg = <span class="string">'tomato'</span>;</span><br><span class="line">[fruit, veg] = [veg, fruit]; <span class="comment">// 左边是新的变量名，右边是交换后的值</span></span><br><span class="line"><span class="built_in">console</span>.log(fruit, veg);</span><br><span class="line"><span class="comment">// "tomato" "banana"</span></span><br></pre></td></tr></table></figure>

<h4 id="把字符串的字取出并个别声明变量"><a href="#把字符串的字取出并个别声明变量" class="headerlink" title="把字符串的字取出并个别声明变量"></a>把字符串的字取出并个别声明变量</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> str = <span class="string">'吃饱没'</span>;</span><br><span class="line"><span class="keyword">let</span> [a, b, c] = str;</span><br><span class="line"><span class="built_in">console</span>.log(a, b, c);</span><br><span class="line"><span class="comment">// "吃" "饱" "没"</span></span><br></pre></td></tr></table></figure>

<h4 id="默认值"><a href="#默认值" class="headerlink" title="默认值"></a>默认值</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [g = <span class="string">'Grete'</span>, s = <span class="string">'Steffi'</span>] = [<span class="string">'葛蕾特'</span>];</span><br><span class="line"><span class="comment">// g 会被重新赋值，s 会用默认</span></span><br><span class="line"><span class="built_in">console</span>.log(g,s);</span><br><span class="line"><span class="comment">// "葛蕾特" "Steffi"</span></span><br></pre></td></tr></table></figure>

<h3 id="对象解构赋值"><a href="#对象解构赋值" class="headerlink" title="对象解构赋值"></a>对象解构赋值</h3><p>数组的解构赋值强调的是<strong>顺序</strong>，而对象的解构赋值强调的则是<strong>属性名称</strong>，属性名称必须相互对应才能够获取到值。</p>
<p>在对象解构赋值中，冒号前是用来对应对象的属性名称，冒号后才是真正创建的变量名称和被赋值的对象。</p>
<p>参照：<a href="https://pjchender.blogspot.com/2017/01/es6-object-destructuring.html" target="_blank" rel="external nofollow noopener noreferrer">[笔记] JavaScript ES6 中的对象解构赋值（object destructuring）</a></p>
<h4 id="基本用法-1"><a href="#基本用法-1" class="headerlink" title="基本用法"></a>基本用法</h4><p>从对象中取出一个特性的值，并赋予这个值一个新的变量名称。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> person = &#123;</span><br><span class="line">  name: <span class="string">'Grete'</span>,</span><br><span class="line">  age: <span class="number">25</span>,</span><br><span class="line">  city: <span class="string">'Taichung'</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> &#123;<span class="attr">city</span>: address&#125; = person;</span><br><span class="line"><span class="comment">// 冒号左边是对象特性，冒号右边是新赋予的变量名称</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(address);</span><br><span class="line"><span class="comment">// "Taichung"</span></span><br></pre></td></tr></table></figure>

<p><code>person.city</code> 的值依然是 <code>&#39;Taichung&#39;</code>，而通过解构赋值另外声明的变量 <code>address</code> 的值也是 <code>&#39;Taichung&#39;</code>。</p>
<h4 id="延伸问题"><a href="#延伸问题" class="headerlink" title="延伸问题"></a>延伸问题</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123; <span class="attr">name</span>: germanName, <span class="attr">fruits</span>: [, apple] &#125; = &#123; <span class="attr">name</span>: <span class="string">'Grete'</span>, <span class="attr">fruits</span>: [<span class="string">'香蕉'</span>, <span class="string">'苹果'</span>, <span class="string">'芭乐'</span>] &#125;</span><br><span class="line"><span class="comment">// 右边的属性对应左边的属性，右边的值对应左边的新变量名称</span></span><br><span class="line"><span class="built_in">console</span>.log(germanName, apple);</span><br><span class="line"><span class="comment">// "Grete" "苹果"</span></span><br></pre></td></tr></table></figure>

<p>左边 <code>fruits</code> 特性要解构赋值的对象是右边 <code>fruits</code> 的第二个值，所以在 <code>apple</code> 前面用逗点空了一个位置。</p>
<h4 id="默认值-1"><a href="#默认值-1" class="headerlink" title="默认值"></a>默认值</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123; <span class="attr">fruits</span>: apple = <span class="string">'苹果'</span> &#125; = &#123;&#125;;</span><br><span class="line"><span class="built_in">console</span>.log(apple); <span class="comment">// "苹果"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> &#123; <span class="attr">fruits</span>: apple = <span class="string">'苹果'</span> &#125; = &#123;<span class="attr">fruits</span>: <span class="string">'凤梨'</span>&#125;;</span><br><span class="line"><span class="built_in">console</span>.log(apple); <span class="comment">// "凤梨"</span></span><br></pre></td></tr></table></figure>

<p>第一个示例中，左边默认 <code>apple</code> 等于 ‘苹果’，因为右边没有传新的值，所以会保持 <code>apple</code> 为苹果。<br>第二个示例中，右边传了新的值，所以 <code>apple</code> 的值就变成凤梨了。</p>
<h2 id="缩写"><a href="#缩写" class="headerlink" title="缩写"></a>缩写</h2><h3 id="对象缩写"><a href="#对象缩写" class="headerlink" title="对象缩写"></a>对象缩写</h3><p>将一个字符串与一个对象，在另一个新对象里合并。<br>新对象里的特性名称如果跟来源变量一致时，就可以使用缩写。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Frieza = <span class="string">'弗利沙'</span></span><br><span class="line"><span class="keyword">const</span> GinyuTeam = &#123;</span><br><span class="line">  Ginyu: <span class="string">'基纽'</span>,</span><br><span class="line">  Jeice: <span class="string">'吉斯'</span>,</span><br><span class="line">  burter: <span class="string">'巴特'</span>,</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> newTeam = &#123;</span><br><span class="line">  GinyuTeam,  <span class="comment">// 原始写法为 GinyuTeam: GinyuTeam</span></span><br><span class="line">  Frieza  <span class="comment">// 原始写法为 Frieza: Frieza</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在 Vue CLI 里也会时常应用缩写，例如以下的情境：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> Vue <span class="keyword">from</span> <span class="string">'vue'</span></span><br><span class="line"><span class="keyword">import</span> App <span class="keyword">from</span> <span class="string">'./App'</span></span><br><span class="line"><span class="keyword">import</span> router <span class="keyword">from</span> <span class="string">'./router'</span></span><br><span class="line"><span class="comment">// 将软件包从 './App' 路径加载，并使用 App 这个变量名</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> Vue(&#123;</span><br><span class="line">  el: <span class="string">'#app'</span>,</span><br><span class="line">  router, <span class="comment">// 原始写法为 router: router</span></span><br><span class="line">  template: <span class="string">'&lt;App/&gt;'</span>,</span><br><span class="line">  components: &#123; App &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="对象方法缩写"><a href="#对象方法缩写" class="headerlink" title="对象方法缩写"></a>对象方法缩写</h3><h4 id="原始写法："><a href="#原始写法：" class="headerlink" title="原始写法："></a>原始写法：</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> newTeam = &#123;</span><br><span class="line">  showPosture: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'我们是 基纽特战队'</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="缩写："><a href="#缩写：" class="headerlink" title="缩写："></a>缩写：</h4><p>把 <code>:</code> 跟 <code>function</code> 去掉。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> newTeam = &#123;</span><br><span class="line">  showPosture () &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'我们是 基纽特战队'</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意：对象方法缩写与箭头函数的结果是不一致的。</p>
<h3 id="缩写搭配展开与解构赋值"><a href="#缩写搭配展开与解构赋值" class="headerlink" title="缩写搭配展开与解构赋值"></a>缩写搭配展开与解构赋值</h3><p>任务：将一个对象赋值到新的变量，并添加一个特性到新对象中，而且不能更改到原对象（=避免参考）。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> GinyuTeam = &#123;</span><br><span class="line">   Ginyu: &#123;</span><br><span class="line">     name: <span class="string">'基纽'</span></span><br><span class="line">   &#125;,</span><br><span class="line">   Jeice: &#123;</span><br><span class="line">     name: <span class="string">'吉斯'</span></span><br><span class="line">   &#125;,</span><br><span class="line">   burter: &#123; </span><br><span class="line">     name: <span class="string">'巴特'</span></span><br><span class="line">   &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> newTeam = &#123;...GinyuTeam&#125;;</span><br><span class="line"><span class="comment">// 将原对象的特性一个个取出（展开），并赋值到新的变量</span></span><br><span class="line"></span><br><span class="line">newTeam.ming = <span class="string">"小明"</span>;</span><br><span class="line"><span class="built_in">console</span>.log(newTeam, GinyuTeam);</span><br><span class="line"><span class="comment">// newTeam 有小明，GinyuTeam 维持原样</span></span><br></pre></td></tr></table></figure>

<h2 id="箭头函数"><a href="#箭头函数" class="headerlink" title="箭头函数"></a>箭头函数</h2><h3 id="基本写法"><a href="#基本写法" class="headerlink" title="基本写法"></a>基本写法</h3><p>传统函数长这样：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> callSomeone = <span class="function"><span class="keyword">function</span> (<span class="params">someone</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> someone + <span class="string">'吃饭了'</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">console</span>.log(callSomeone(<span class="string">'小明'</span>));</span><br></pre></td></tr></table></figure>

<p>改写成箭头函数长这样：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> callSomeone = <span class="function"><span class="params">someone</span> =&gt;</span> someone + <span class="string">'吃饭了'</span>;</span><br><span class="line"><span class="built_in">console</span>.log(callSomeone(<span class="string">'小明'</span>));</span><br></pre></td></tr></table></figure>

<h3 id="如何改写"><a href="#如何改写" class="headerlink" title="如何改写"></a>如何改写</h3><p>把参数往前提，把关键字 <code>function</code> 删掉，改成箭头。<br>只有一个参数时可以不用小括号包起来，如果没带入参数就还是需要小括号。<br>如果只有单行代码而且只是要返回一个值的情况下，可以省略大括号及 <code>return</code>。<br>箭头函数没有 <code>arguments</code> 对象，所以如果要传入多个参数值时，可以在箭头函数的小括号内使用其余参数。</p>
<h3 id="与传统函数最不同的地方：this-绑定差异"><a href="#与传统函数最不同的地方：this-绑定差异" class="headerlink" title="与传统函数最不同的地方：this 绑定差异"></a>与传统函数最不同的地方：this 绑定差异</h3><ul>
<li>传统函数：<code>this</code> 取决于函数调用的方式</li>
<li>箭头函数：<code>this</code> 取决于被定义时所在的对象，「箭头函数没有自己的 <code>this</code>」，因此它都是采用其同层级的 <code>this</code> 来作用。</li>
</ul>
<p>写 Vue <code>methods</code> 时最好使用<strong>传统函数搭配缩写</strong>，因为使用箭头函数的话，<code>this</code> 指向的可能不是 Vue 实例。</p>
<p>参照：<a href="https://pjchender.blogspot.com/2017/01/es6-arrow-function.html" target="_blank" rel="external nofollow noopener noreferrer">[笔记] JavaScript ES6 中的箭头函数（arrow function）及对 this 的影响</a></p>
<h3 id="善用方式"><a href="#善用方式" class="headerlink" title="善用方式"></a>善用方式</h3><p>在进入箭头函数之前，先把要指向的对象（<code>this</code>）用变量存储起来。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> antie = &#123;</span><br><span class="line">  name: <span class="string">'漂亮阿姨'</span>,</span><br><span class="line">  callname() &#123;</span><br><span class="line">    <span class="keyword">var</span> vm = <span class="keyword">this</span>;</span><br><span class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(vm, vm.name);</span><br><span class="line">    &#125;, <span class="number">10</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="字符串模板"><a href="#字符串模板" class="headerlink" title="字符串模板"></a>字符串模板</h2><p>各位在用 <code>innerHTML</code> 组字符串时会不会感到很痛苦呢？我承认我会，因为各种标签名、文字、加号、单双引号，全部乱糟糟地都写在一起，真 D 会让人眼花啊！</p>
<p>还好，在ES6有新的写法，可以让我们好过一点。</p>
<h3 id="基本用法-2"><a href="#基本用法-2" class="headerlink" title="基本用法"></a>基本用法</h3><p>假设我们要在一个 <code>&lt;ul&gt;&lt;li&gt;</code> 里放入图片：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// HTML</span></span><br><span class="line">&lt;ul <span class="class"><span class="keyword">class</span></span>=<span class="string">"list"</span>&gt;</span><br><span class="line">  &lt;li&gt;</span><br><span class="line">    &lt;img src=<span class="string">"logo.png"</span>&gt;</span><br><span class="line">  &lt;<span class="regexp">/li&gt;</span></span><br><span class="line"><span class="regexp">&lt;/u</span>l&gt;</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// JS</span></span><br><span class="line"><span class="comment">// 用 const 选择 DOM</span></span><br><span class="line"><span class="keyword">const</span> list = <span class="built_in">document</span>.querySelector(<span class="string">'.list'</span>)</span><br><span class="line"><span class="comment">// 用 const 存储要放入的图片路径</span></span><br><span class="line"><span class="keyword">const</span> imgURL = <span class="string">'logo.png'</span>;</span><br></pre></td></tr></table></figure>

<p>接下来要进行渲染至网页，传统写法是这样：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">list.innerHTML = <span class="string">'&lt;li&gt;&lt;img src='</span>+ imgURL +<span class="string">'&gt;&lt;/li&gt;'</span></span><br></pre></td></tr></table></figure>

<p>现在比较好的写法是：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">list.innetHTML = <span class="string">`&lt;li&gt;&lt;img src=" <span class="subst">$&#123;imgURL&#125;</span> "&gt;&lt;/li&gt;`</span></span><br></pre></td></tr></table></figure>

<p>新写法中这个长得像重音符号的东西叫做「反引号」，<code>${}</code> 里面可放入 JS 代码跟变量，支持 ES6 写法。</p>
<p>组字符串不需要使用加号，而选择变量的方法则是把变量名称包在大括号内，前面再加个 <code>$</code> 符号，这样就可以了。</p>
<h3 id="搭配数组方法使用"><a href="#搭配数组方法使用" class="headerlink" title="搭配数组方法使用"></a>搭配数组方法使用</h3><p>假设我们要根据一个数组里的数据，一笔笔输出为 <code>&lt;ul&gt; &lt;li&gt;</code> 列表：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 数组</span></span><br><span class="line"><span class="keyword">const</span> people = [</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">'小明'</span>,</span><br><span class="line">    friends: <span class="number">2</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">'阿姨'</span>,</span><br><span class="line">    friends: <span class="number">999</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    name: <span class="string">'杰伦'</span>,</span><br><span class="line">    friends: <span class="number">0</span></span><br><span class="line">  &#125;</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> newUl = <span class="string">`</span></span><br><span class="line"><span class="string">  &lt;ul&gt; <span class="subst">$&#123;people.map(person =&gt;</span></span></span><br><span class="line"><span class="string"><span class="subst">    <span class="string">`&lt;li&gt;我叫做 <span class="subst">$&#123;person.name&#125;</span>&lt;/li&gt;`</span>)</span></span></span><br><span class="line"><span class="string"><span class="subst">    .join(<span class="string">''</span>)&#125;</span></span></span><br><span class="line"><span class="string">  &lt;/ul&gt;`</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>map()</code> 会把数组里所有元素传进 callback 函数内运算，并且把运算后的元素以数组的形式返回。</li>
<li><code>join()</code> 会把数组里所有元素以指定的字符连接在一起，这个例子是让元素间不要有任何字符、逗号。</li>
</ul>
<h2 id="常用数组方法"><a href="#常用数组方法" class="headerlink" title="常用数组方法"></a>常用数组方法</h2><h3 id="forEach-与-map"><a href="#forEach-与-map" class="headerlink" title="forEach() 与 map()"></a><code>forEach()</code> 与 <code>map()</code></h3><p>两者很像，但 <code>forEach()</code> 不会返回值，而 <code>map()</code> 会返回一个新的数组。<br><code>map()</code> 的 callback 函数里需要使用 <code>return</code> 来返回运算后的结果，且也需要一个变量来接收 <code>map()</code> 执行后的新数组。</p>
<h3 id="filter"><a href="#filter" class="headerlink" title="filter()"></a><code>filter()</code></h3><p><code>filter()</code> 与 <code>find()</code>：<code>filter()</code> 返回的是一个新数组且里面包含所有符合条件的元素，而 <code>find()</code> 只会返回第一个符合条件的元素值。因此，<code>find()</code> 较适合用于搜寻一个特定的值。</p>
<h3 id="every"><a href="#every" class="headerlink" title="every()"></a><code>every()</code></h3><p><strong>所有</strong>元素都符合条件才会返回 <code>true</code>。</p>
<h3 id="some"><a href="#some" class="headerlink" title="some()"></a><code>some()</code></h3><p><strong>部分</strong>元素有符合条件就会返回 <code>true</code>。</p>
<h3 id="reduce"><a href="#reduce" class="headerlink" title="reduce()"></a><code>reduce()</code></h3><p>用于累加或比较值的大小。</p>
<h4 id="累加"><a href="#累加" class="headerlink" title="累加"></a>累加</h4><p><code>reduce()</code> 第一个参数是一个 callback 函数，第二个参数是累加的起始值。</p>
<p>callback 函数的参数中，第一个参数为「前一个元素的值」，如果没有前一个元素，就使用 <code>reduce()</code> 的第二个参数。</p>
<h4 id="比较值的大小"><a href="#比较值的大小" class="headerlink" title="比较值的大小"></a>比较值的大小</h4><p>配合使用 <code>Math.max()</code>。</p>
]]>
    </content>
    <categories>
      <category>JavaScript</category>
    </categories>
    <tags>
      <tag>w3HexSchool</tag>
      <tag>ES6</tag>
    </tags>
  </entry>
  <entry>
    <title>[Gulp] 安装 PostCSS 的地雷</title>
    <url>/Gulp/20200310/2050018576/</url>
    <content>
      <![CDATA[<p>最近在学怎么使用 Gulp 来管理项目，于是按照 Ray 的教学文慢慢跟着写 gulpfile，不过在安装 PostCSS 时有踩到一个文章中没提到的地雷，解决的过程一波二折，于是让我决定记录一下拆地雷的经过。</p>
<a id="more"></a>

<h2 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h2><p>在 Ray 的教学文：<a href="https://hsiangfeng.github.io/gulp/20190610/4048666562/" target="_blank" rel="external nofollow noopener noreferrer">这是在讲 Gulp 不是饮料是任务自动化工具这件事：安装 PostCSS 篇</a>中，有提到要安装 <code>gulp-cssnano</code>，并且将它引入 gulpfile 中。</p>
<p>我照做之后，执行 <code>gulp scss</code>，此时出现一个错误信息：</p>
<p><img src="https://i.imgur.com/94aMhVj.png" alt="错误信息1"></p>
<blockquote>
<p>Error in plugin “gulp-postcss”<br>Message: [object Object] is not a PostCSS plugin</p>
</blockquote>
<h2 id="尝试解决"><a href="#尝试解决" class="headerlink" title="尝试解决"></a>尝试解决</h2><p>由于在 Ray 的教学文章中没有提到会发生这个错误，所以我就自行估狗了一些 StackOverflow 的文章，试了好几篇解答后，终于找到了一个能成功解决问题的回答！</p>
<p>就是这一篇：<a href="https://stackoverflow.com/questions/48506482/gulpfile-autoprefixer-must-change-to-postcss" target="_blank" rel="external nofollow noopener noreferrer">gulpfile autoprefixer must change to postcss</a></p>
<p>我研究了回答者提供的代码，发现他引入 <code>gulp-cssnano</code> 的方式有点不一样。Ray 的文章以及 <code>gulp-cssnano</code> 本身的<a href="https://www.npmjs.com/package/gulp-cssnano" target="_blank" rel="external nofollow noopener noreferrer">文件</a>都说要这样引入：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> cssnano = <span class="built_in">require</span>(<span class="string">'gulp-cssnano'</span>);</span><br></pre></td></tr></table></figure>

<p>但是不知道为何，在 <code>require()</code> 内写 <code>gulp-cssnano</code> 似乎找不到文件。而如果是改成在括号内写 <code>cssnano</code>，重新运行 <code>gulp scss</code>，错误信息就消失了！</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> cssnano = <span class="built_in">require</span>(<span class="string">'cssnano'</span>);</span><br></pre></td></tr></table></figure>

<h2 id="压缩-CSS-时又报错"><a href="#压缩-CSS-时又报错" class="headerlink" title="压缩 CSS 时又报错"></a>压缩 CSS 时又报错</h2><p>改了 require 之后，我接着处理 CSS 的压缩以及判断开发环境与正视环境，结果在输入 <code>gulp scss --env prod</code> 时又跳出错误：</p>
<p><img src="https://i.imgur.com/oJEVrYs.png" alt="错误信息2"></p>
<blockquote>
<p>TypeError: dest.on is not a function</p>
</blockquote>
<p>这一次我只好找上原作者 Ray，麻烦他帮我看看是怎么回事。原来，require 内一定要写 <code>gulp-cssnano</code>，因为原始 <code>cssnano</code> 并没有 gulp 相关功能。如果 require 里面写 <code>gulp-cssnano</code> 却没有正确加载文件，可以重新安装一次 <code>gulp-cssnano</code>，可能是安装时没装好。</p>
<p>于是我重新安装 <code>gulp-cssnano</code> 并且又改回原本写的 require：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> cssnano = <span class="built_in">require</span>(<span class="string">'gulp-cssnano'</span>);</span><br></pre></td></tr></table></figure>

<p>之后就顺利运行了～</p>
<h2 id="Special-Thanks"><a href="#Special-Thanks" class="headerlink" title="Special Thanks"></a>Special Thanks</h2><p>感谢 Ray 拨空看我的源代码，协助我抓虫成功 🐛</p>
]]>
    </content>
    <categories>
      <category>Gulp</category>
    </categories>
    <tags>
      <tag>w3HexSchool</tag>
      <tag>Gulp</tag>
      <tag>PostCSS</tag>
    </tags>
  </entry>
  <entry>
    <title>[JS] 讲座笔记：JS Promise 实战教学</title>
    <url>/javascript/20200302/2110342105/</url>
    <content>
      <![CDATA[<p>本篇笔记来自 2020.2.26 六角学院在线研讨会，讲者为偷米骑巴哥的 Tommy。在这一次讲座中，Tommy 老师为大家复习了异步行为与 Queue 的概念，并接续介绍了使异步行为依次进行的四种方法。</p>
<a id="more"></a>

<p>当我们有 3 个 <code>setTimeout()</code> 时，我们可以使它们按照顺序执行；但是当我们有 3 个 AJAX 时，由于变因太多、无法确定各自返回的时间，因此不一定能照顺序执行。</p>
<p>要解决异步无法依次执行的问题，第一个方法是使用 callback function。</p>
<h2 id="让异步依次执行-解法一：callback"><a href="#让异步依次执行-解法一：callback" class="headerlink" title="让异步依次执行 解法一：callback"></a>让异步依次执行 解法一：callback</h2><p>三个 <code>setTimeout()</code> 个别用 function 包起来，且各个 function 都带入一个 callback 参数。</p>
<p><img src="https://i.imgur.com/flYXHnz.png" alt></p>
<p>调用 function A 时，同时定义 callback 的任务内容，如此一来就可以先印出 <code>&#39;A&#39;</code> 再印出 <code>&#39;OK&#39;</code>。</p>
<p><img src="https://i.imgur.com/a8Pnnxr.png" alt></p>
<p>运用这个原理，就可以把 function B 当作 function A 的 callback，把 function C 当作 function B 的 callback。</p>
<p><img src="https://i.imgur.com/rUBofui.png" alt></p>
<p>但是这种做法一旦遇到<strong>太多函数要依次执行时</strong>，就会变成 <em>callback hell</em>，将会很难去分辨代码。</p>
<h2 id="让异步依次执行-解法二：Generator"><a href="#让异步依次执行-解法二：Generator" class="headerlink" title="让异步依次执行 解法二：Generator"></a>让异步依次执行 解法二：Generator</h2><ul>
<li>在 function 后面加一个 <code>*</code>，执行时会得到一个 <code>generator</code> 对象。</li>
<li>在 for 循环中使用 <code>yield</code> 关键字：代表<strong>暂停</strong>，先跳出 function，等之后在 function 外面用 <code>函数名称.next()</code> 调用时，才会再回到 function 内部继续执行。</li>
<li><code>generator</code> 对象会带出 <code>done</code> 特性</li>
<li><code>arguments</code> : 类数组对象，可获取所有传入 function 的参数</li>
<li>递回：在函数里调用函数本身</li>
</ul>
<p><img src="https://i.imgur.com/tIwffhZ.png" alt></p>
<p><code>for</code> 循环跑到最后一次时，<code>done</code> 的值为 <code>true</code>：</p>
<p><img src="https://i.imgur.com/PPGVq6s.png" alt></p>
<p>在 <code>recursion()</code> 中使用 <code>.next()</code> 调用函数与递回，让 function A, function B, function C 依次执行：</p>
<p><img src="https://i.imgur.com/AXlstmC.png" alt></p>
<p>小结：最后一行，在 <code>generator()</code> 带入多个函数作为参数，就能依次执行并避免 callback hell。</p>
<h2 id="让异步依次执行-解法三：Promise"><a href="#让异步依次执行-解法三：Promise" class="headerlink" title="让异步依次执行 解法三：Promise"></a>让异步依次执行 解法三：Promise</h2><ul>
<li>一个封装 <code>Promise</code> 的函数</li>
<li><code>Promise</code> 的 callback function 要带入两个参数：<code>resolve</code> (成功时调用)，以及 <code>reject</code> (失败时调用)。</li>
</ul>
<p><img src="https://i.imgur.com/7m78XIL.png" alt></p>
<p>异步成功时，就会执行 <code>resolve</code>；<code>resolve</code> 执行后，就会执行 <code>.then()</code> 的内容；反过来说，<code>.then()</code> 的执行仰赖 <code>resolve</code> 是否执行。</p>
<p><img src="https://i.imgur.com/guI0D2w.png" alt></p>
<p>异步失败时，<code>Promise</code> 会执行 <code>reject</code>，<code>reject</code> 必须准备对应的函数，这个函数是针对单一的 <code>Promise</code>，在失败时执行的方案。</p>
<p>在异步失败时执行对应的行为也能用 <code>catch</code> 达成，<code>catch</code> 的意思是，只要其中一个 <code>Promise</code> 没成功执行，就跳到 <code>catch</code>。</p>
<p><img src="https://i.imgur.com/tkBRQMP.png" alt></p>
<h2 id="让异步依次执行-解法四：async…await"><a href="#让异步依次执行-解法四：async…await" class="headerlink" title="让异步依次执行 解法四：async…await"></a>让异步依次执行 解法四：async…await</h2><ul>
<li>ES7 语法，要搭配 <code>Promise</code> 使用</li>
<li><code>async</code> + function：声明这个函数是异步</li>
<li><code>await</code> + <code>Promise</code>：依次执行函数</li>
<li>条件：<code>async</code> 跟 <code>await</code> 一定要搭配使用，不能只用 <code>await</code> 却不用 <code>async</code>。</li>
</ul>
<p><img src="https://i.imgur.com/0nEubbT.png" alt></p>
<p><code>reject</code> 时与 <code>try...catch</code> 搭配使用，<code>reject()</code> 括号内的内容会传到 <code>catch(e)</code> 的 <code>e</code> 参数，也可以直接在 <code>reject()</code> 括号内放入 <code>Error</code> 对象（JS 原生错误对象）。</p>
<p><img src="https://i.imgur.com/6yejkHH.png" alt></p>
<p>三个函数异步失败时想要不同的错误信息：</p>
<p><img src="https://i.imgur.com/Yi3LpB1.png" alt></p>
<p>实际开发中使用 <code>XMLHttpRequest</code> 时的写法：</p>
<p><img src="https://i.imgur.com/IBlkZKh.png" alt></p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p><a href="https://slides.com/yi-tailin/deck-e329c0#/" target="_blank" rel="external nofollow noopener noreferrer">slides 演示文稿</a><br><a href="https://developer.mozilla.org/zh-TW/docs/Web/JavaScript/Reference/Statements/function*" target="_blank" rel="external nofollow noopener noreferrer">function*</a><br><a href="https://developers.google.com/web/fundamentals/primers/promises?hl=zh-tw#top_of_page" target="_blank" rel="external nofollow noopener noreferrer">JavaScript Promise：简介</a><br><a href="https://wcc723.github.io/development/2020/02/16/all-new-promise/" target="_blank" rel="external nofollow noopener noreferrer">JavaScript Promise 全介绍</a></p>
]]>
    </content>
    <categories>
      <category>JavaScript</category>
    </categories>
    <tags>
      <tag>w3HexSchool</tag>
      <tag>Promise</tag>
      <tag>async...await</tag>
      <tag>callback</tag>
      <tag>Generator</tag>
    </tags>
  </entry>
  <entry>
    <title>[JS] 讲座笔记：AJAX 异步 / Event Queue 详解</title>
    <url>/javascript/20200224/3747532111/</url>
    <content>
      <![CDATA[<p>本篇笔记来自 2020.2.19 六角学院在线研讨会，讲者为偷米骑巴哥的 Tommy。在这一次讲座中，Tommy 老师用许多 Demo 示范了异步行为与 Queue 之间的关系。</p>
<a id="more"></a>
<h2 id="Event-Queue-事件队列"><a href="#Event-Queue-事件队列" class="headerlink" title="Event Queue 事件队列"></a>Event Queue 事件队列</h2><p>JavaScript 是 Single Thread（单线程）语言，只用一颗 CPU 运转，一次只能做一件事。但是，计时器（setTimeout）、AJAX、Promise 是属于 Web APIs，不受单线程限制，它们会被放到 Queue 中，等待 JavaScript 执行。</p>
<p>因此，整个流程（Event Loop）就像这样：</p>
<p>所有要执行的任务会被放到 Call Stack 里面，JavaScript 的线程会逐一执行 Stack 里的任务，当碰上异步事件时，为了不让程序被这些<strong>需要等待的事件</strong>卡着，就会继续执行后续的动作。</p>
<p>那这些异步事件什么时候才能被执行？答案是，它们会在 Queue 中排队，等到 JavaScript 的线程把 Stack 的任务都消化完了，才轮到 Queue 中的异步事件依次执行。</p>
<p><strong>观念重点：异步事件会被放进排队序列，先进先出。</strong></p>
<p>单线程与 Event Queue 的概念可以参照<a href="https://wcc723.github.io/javascript/2017/12/07/javascript-event-queue/" target="_blank" rel="external nofollow noopener noreferrer">铁人赛：一次只能做一件事情的 JavaScript</a>，有动图更好理解。</p>
<h2 id="单线程与-Queue"><a href="#单线程与-Queue" class="headerlink" title="单线程与 Queue"></a>单线程与 Queue</h2><p>JavaScript 单线程会按照顺序执行在 Stack 中的任务。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">console.<span class="built_in">log</span>(a);</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span><span class="params">()</span></span> &#123;</span><br><span class="line">  console.<span class="built_in">log</span>(<span class="string">'b'</span>);</span><br><span class="line">&#125;</span><br><span class="line">console.<span class="built_in">log</span>(<span class="string">'c'</span>);</span><br><span class="line"></span><br><span class="line">// <span class="string">'a'</span> <span class="string">'b'</span> <span class="string">'c'</span></span><br></pre></td></tr></table></figure>
<p>如果在这个例子中，穿插一个异步事件进去，那么执行顺序会是如何呢？</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">console.<span class="built_in">log</span>(<span class="string">'a'</span>);</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span><span class="params">()</span></span> &#123;</span><br><span class="line">  console.<span class="built_in">log</span>(<span class="string">'b'</span>);</span><br><span class="line">&#125;</span><br><span class="line">setTimeout(run, <span class="number">3000</span>)</span><br><span class="line">console.<span class="built_in">log</span>(<span class="string">'c'</span>);</span><br><span class="line">// <span class="string">'a'</span> <span class="string">'c'</span> <span class="string">'b'</span></span><br></pre></td></tr></table></figure>
<p>通过这个例子可以发现，异步事件会被放到其他行为的后面执行。<br>我们可以把 <code>setTimeout()</code> 的秒数改成 0，来验证这个结论是否为真。</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line">console.<span class="built_in">log</span>(<span class="string">'a'</span>);</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span><span class="params">()</span></span> &#123;</span><br><span class="line">  console.<span class="built_in">log</span>(<span class="string">'b'</span>);</span><br><span class="line">&#125;</span><br><span class="line">setTimeout(run, <span class="number">0</span>)</span><br><span class="line">console.<span class="built_in">log</span>(<span class="string">'c'</span>);</span><br><span class="line">// <span class="string">'a'</span> <span class="string">'c'</span> <span class="string">'b'</span></span><br></pre></td></tr></table></figure>
<p>秒数改成 0 以后，<code>&#39;c&#39;</code> 依然是最后才印出来，就是因为异步事件都必须在 Queue 中等待 Stack 里其他任务结束，才能被执行。</p>
<p>再来看另一个例子，也可以看出在 Queue 中的事件永远会被放到最后才执行的特征。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="string">'a'</span>);</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'b'</span>);</span><br><span class="line">&#125;</span><br><span class="line">setTimeout(run, <span class="number">3000</span>)</span><br><span class="line"><span class="keyword">var</span> start = <span class="built_in">Date</span>.now();</span><br><span class="line"><span class="comment">// 让循环重复一直跑，5 秒后跳出</span></span><br><span class="line"><span class="keyword">while</span> (<span class="built_in">Date</span>.now() - start &lt;= <span class="number">5000</span>) &#123;&#125;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'c'</span>);</span><br><span class="line"><span class="comment">// 'a'</span></span><br><span class="line"><span class="comment">// 5 秒后</span></span><br><span class="line"><span class="comment">// 'c'</span></span><br><span class="line"><span class="comment">// 'b'</span></span><br></pre></td></tr></table></figure>
<p>这个例子中，<code>setTimeout()</code> 应该在 3 秒后印出 <code>&#39;b&#39;</code>，但结果却是在 5 秒后，排在 <code>&#39;c&#39;</code> 的后面出现，为什么呢？</p>
<p>这是因为，当 3 秒到时，<code>setTimeout()</code> 就已回来 JavaScript 的线程了，但在 Stack 中还有 5 秒 <code>while</code> 循环及 <code>console.log(&#39;c&#39;);</code> 还没执行完毕。<br>因此，当循环终于跑完时，<code>&#39;c&#39;</code> 接着印出来，而时间早就到了的 <code>&#39;b&#39;</code> 也就紧跟着印出来了。</p>
<h3 id="for-循环-以-Vue-环境为例"><a href="#for-循环-以-Vue-环境为例" class="headerlink" title="for 循环 - 以 Vue 环境为例"></a>for 循环 - 以 Vue 环境为例</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// HTML</span></span><br><span class="line">&lt;div id=<span class="string">"app"</span>&gt;</span><br><span class="line">  &lt;ul&gt;</span><br><span class="line">    &lt;li v-<span class="keyword">for</span>=<span class="string">"i in list"</span>&gt; &#123;&#123; i &#125;&#125; &lt;<span class="regexp">/li&gt;</span></span><br><span class="line"><span class="regexp">  &lt;/u</span>l&gt;</span><br><span class="line">&lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">/</span><span class="regexp">/ JS</span></span><br><span class="line"><span class="regexp">new Vue(&#123;</span></span><br><span class="line"><span class="regexp">  el: '#app',</span></span><br><span class="line"><span class="regexp">  data() &#123;</span></span><br><span class="line"><span class="regexp">    return &#123;</span></span><br><span class="line"><span class="regexp">      list: [],</span></span><br><span class="line"><span class="regexp">    &#125;</span></span><br><span class="line"><span class="regexp">  &#125;,</span></span><br><span class="line"><span class="regexp">  mounted() &#123;</span></span><br><span class="line"><span class="regexp">   for(let i = 0; i &lt; 5; i++)&#123;</span></span><br><span class="line"><span class="regexp">     debugger;</span></span><br><span class="line"><span class="regexp">     this.list.push(i+1);</span></span><br><span class="line"><span class="regexp">   &#125;;</span></span><br><span class="line"><span class="regexp">  &#125;,</span></span><br><span class="line"><span class="regexp">&#125;)</span></span><br></pre></td></tr></table></figure>
<p>通过 <code>debugger</code> 可以在开发人员工具中看到循环的分解动作，我们可以看到 <code>for</code> 循环是等所有次数都跑完，数字才渲染出来；而不是像我们所想像的，跑一次循环就渲染一个数字在画面上。</p>
<p>那么，要如何每跑一次循环就渲染一次数字？</p>
<p>答案是，可以用 <code>setTiomeiut()</code> 来达成。</p>
<figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">for(<span class="name">let</span> i = <span class="number">0</span><span class="comment">;i &lt; 5; i++)&#123;</span></span><br><span class="line">  setTimout(<span class="name">function</span>()&#123;</span><br><span class="line">    this.list.push(<span class="name">i+1</span>)<span class="comment">;</span></span><br><span class="line">  &#125;.bind(<span class="name">this</span>, i)) // 指定 callback 的 this 指向外层的对象</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>为什么用 <code>setTiomeiut()</code> 就能达成呢？</p>
<p>因为 <code>for</code> 循环一开始没用 <code>setTimeout()</code> 时，只有 <code>for</code> 自己一个任务在 Stack 中；而使用 <code>setTimeout()</code> 后，就变成每跑一次循环就产生一个存放在 Queue 中的任务，每个 Queue 中的任务又会被依次插入 Stack 中，于是就能一个执行（渲染）完才换成下一个执行（渲染）。</p>
<h2 id="AJAX"><a href="#AJAX" class="headerlink" title="AJAX"></a>AJAX</h2><p>传统网页在跟后端捞数据时，流程是这样的：请求 =&gt; 响应 =&gt; 请求 =&gt; 响应；而使用了 AJAX 技术的网页捞取数据的方法，则是在背景送出请求获取响应。</p>
<h3 id="常见底层"><a href="#常见底层" class="headerlink" title="常见底层"></a>常见底层</h3><ul>
<li>XMLHttpRequest (可支持 IE 7 以上, jQuery, axios)</li>
<li>HTML5 Fetch API (IE 11 以下都不支持)</li>
</ul>
<h3 id="测试用-API"><a href="#测试用-API" class="headerlink" title="测试用 API"></a>测试用 API</h3><p>以下两个网站都可以用来制作测试用的 API，也可以定制 AJAX 成功后的返回信息。</p>
<ul>
<li><a href="http://www.mocky.io" target="_blank" rel="external nofollow noopener noreferrer">Mocky: Real HTTP mocking</a></li>
<li><a href="https://www.mockable.io/" target="_blank" rel="external nofollow noopener noreferrer">Mockable.io</a></li>
</ul>
<h3 id="XMLHttpRequest-DEMO"><a href="#XMLHttpRequest-DEMO" class="headerlink" title="XMLHttpRequest DEMO"></a>XMLHttpRequest DEMO</h3><ol>
<li><p>异步 GET 请求</p>
<figure class="highlight qml"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">xhr.addEventListener(<span class="string">'load'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.responseText);</span><br><span class="line">&#125;)</span><br><span class="line">xhr.open(<span class="string">'GET'</span>, <span class="built_in">url</span>);</span><br><span class="line">xhr.send();</span><br></pre></td></tr></table></figure>
<p>AJAX 行为会被丢到 Queue，等获取响应后才回到 JS 线程。</p>
</li>
<li><p>同步 GET 请求</p>
<figure class="highlight qml"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="string">'start'</span>);</span><br><span class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">xhr.open(<span class="string">'GET'</span>, <span class="built_in">url</span>, <span class="literal">false</span>);</span><br><span class="line">xhr.send();</span><br><span class="line"><span class="built_in">console</span>.log(xhr.responseText);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'end'</span>);</span><br><span class="line"><span class="comment">// 'start'</span></span><br><span class="line"><span class="comment">// responseText</span></span><br><span class="line"><span class="comment">// 'end'</span></span><br></pre></td></tr></table></figure>
<p>这边只是示范同步的 AJAX 是长什么样，实际开发中尽量还是用异步 AJAX 才能让网页性能较好。</p>
</li>
<li><p>异步 POST 请求</p>
<figure class="highlight qml"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 如何把参数发送到后端</span></span><br><span class="line"><span class="keyword">var</span> data = <span class="keyword">new</span> FormData(); <span class="comment">// 声明 FormData 对象</span></span><br><span class="line">data.append(<span class="string">'id'</span>, <span class="string">'5'</span>);  <span class="comment">// 在 FormData 中塞入数据</span></span><br><span class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">xhr.addEventListener(<span class="string">'load'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.responseText);</span><br><span class="line">&#125;)</span><br><span class="line">xhr.open(<span class="string">'POST'</span>, <span class="built_in">url</span>);</span><br><span class="line">xhr.send(data); <span class="comment">// 把带有数据的 FormData 传送到后端</span></span><br></pre></td></tr></table></figure>

</li>
</ol>
<h3 id="Fetch-DEMO"><a href="#Fetch-DEMO" class="headerlink" title="Fetch DEMO"></a>Fetch DEMO</h3><ol>
<li><p>Fetch 发送 GET 请求</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">fetch(<span class="string">'url'</span>).then(<span class="function"><span class="params">response</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> response.json(); <span class="comment">//解读 JSON 格式</span></span><br><span class="line">&#125;)</span><br><span class="line">.then(<span class="function"><span class="params">data</span> =&gt;</span> </span><br><span class="line">  <span class="built_in">console</span>.log(data)) <span class="comment">// 获取数据</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>Fetch 发送 POST 请求</p>
<figure class="highlight lasso"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 把数据参数传到后端，一样要先声明 FormData</span></span><br><span class="line"><span class="built_in">var</span> <span class="built_in">data</span> = <span class="literal">new</span> FormData();</span><br><span class="line"><span class="built_in">data</span>.append(<span class="string">'id'</span>, <span class="string">'5'</span>);</span><br><span class="line"></span><br><span class="line">fetch(<span class="string">'url'</span>, &#123; method: <span class="string">'POST'</span>, body: <span class="built_in">data</span> &#125;) <span class="comment">// 带入参数</span></span><br><span class="line">.then(response =&gt; response.json();) <span class="comment">// 解读JSON格式</span></span><br><span class="line">.then(<span class="built_in">data</span> =&gt; </span><br><span class="line">  console.<span class="keyword">log</span>(<span class="built_in">data</span>)) <span class="comment">// 获取数据</span></span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p><a href="http://slides.com/yi-tailin/event-queue-ajax/#/" target="_blank" rel="external nofollow noopener noreferrer">slide 演示文稿</a></p>
]]>
    </content>
    <categories>
      <category>JavaScript</category>
    </categories>
    <tags>
      <tag>AJAX</tag>
      <tag>w3HexSchool</tag>
      <tag>EventQueue</tag>
      <tag>EventLoop</tag>
    </tags>
  </entry>
  <entry>
    <title>D3.js 超超超基础入门</title>
    <url>/D3-js/20200217/1734732239/</url>
    <content>
      <![CDATA[<p>之前接案时为了要实现一个动画效果而学过一点 D3.js，虽然最后没有派上用场，但当时我写下一些入门笔记，如果你没学过 D3.js 而想要尝试的话，可以参考这篇笔记。</p>
<a id="more"></a>

<h2 id="什么是-D3-js"><a href="#什么是-D3-js" class="headerlink" title="什么是 D3.js"></a>什么是 D3.js</h2><blockquote>
<p>D3.js（Data-Driven Documents）是一个使用动态图形进行数据可视化的 JavaScript 程序库。与 W3C 标准兼容，并且利用广泛实现的 SVG、JavaScript 和 CSS 标准⋯⋯最常被运用在在线新闻网站呈现互动式图形、呈现数据数据的图表和呈现含有地理信息的数据。——维基百科</p>
</blockquote>
<p>简单来说，D3.js 能以图像的方式来呈现数据，并用数据来驱动 DOM。</p>
<h2 id="SVG-标签"><a href="#SVG-标签" class="headerlink" title="SVG 标签"></a>SVG 标签</h2><p>D3.js 是用 SVG 来绘制图像的，所以需要具备一些 SVG 绘图的背景知识。SVG 与 HTML 一样是有标签的，以下就是常见的 SVG 标签：</p>
<ul>
<li><code>&lt;text&gt;</code> - 文字标签</li>
<li><code>&lt;circle&gt;</code> - 圆形标签</li>
<li><code>&lt;line&gt;</code> - 线段标签</li>
<li><code>&lt;path&gt;</code> - 弧形标签</li>
<li><code>&lt;g&gt;</code> - 集成以上的对象，在 <code>&lt;g&gt;</code> 里面可以放各类型的标签</li>
</ul>
<h2 id="创建-D3-js-画布"><a href="#创建-D3-js-画布" class="headerlink" title="创建 D3.js 画布"></a>创建 D3.js 画布</h2><p>首先先来了解如何用 D3.js 选择 DOM。</p>
<ul>
<li>选择单个 DOM：d3.select(‘.className’)</li>
<li>选择多个 DOM：d3.selectAll(‘.className’)</li>
</ul>
<p>了解如何选择 DOM 后，接着先选择一个空的 <code>&lt;div&gt;</code>，动态添加一个 SVG 画布，再用 <code>attr()</code> 动态加上属性来设置宽高，注意，<code>attr()</code> 内的格式是对象。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">d3.<span class="keyword">select</span>(<span class="string">'.chart-d3'</span>)</span><br><span class="line">  .<span class="built_in">append</span>(<span class="string">'svg'</span>)</span><br><span class="line">  .attr(&#123;</span><br><span class="line">    <span class="string">"width"</span>: <span class="string">"75px"</span>,</span><br><span class="line">    <span class="string">"height"</span>: <span class="string">"75px"</span></span><br><span class="line">  &#125;);</span><br></pre></td></tr></table></figure>

<h2 id="将数据导入"><a href="#将数据导入" class="headerlink" title="将数据导入"></a>将数据导入</h2><p>已经准备好画布之后，就可以开始用数据来绘制 DOM 了。</p>
<ol>
<li>准备一个数组当作数据基础。<figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">var vote</span> = [<span class="string">'30'</span>,<span class="string">'50'</span>,<span class="string">'100'</span>,<span class="string">'20'</span>];</span><br></pre></td></tr></table></figure></li>
<li>HTML 中准备一个空的 <code>&lt;ul&gt;</code>，className 为 <code>.list</code>。</li>
<li>用 D3 选择 <code>.list</code>。<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">d3</span><span class="selector-class">.select</span>(<span class="string">'.list'</span>)</span><br><span class="line"><span class="selector-class">.selectAll</span>(<span class="string">'li'</span>) <span class="comment">// 预先选择等一下会创建的与数据数相同的 li</span></span><br><span class="line"><span class="selector-class">.data</span>(vote)  <span class="comment">// 导入数据</span></span><br><span class="line"><span class="selector-class">.enter</span>()  <span class="comment">// 自动生成与数据对应数量的元素</span></span><br><span class="line"><span class="selector-class">.append</span>(<span class="string">'li'</span>) <span class="comment">// 插入元素</span></span><br><span class="line"><span class="selector-class">.text</span>(function(d)&#123;</span><br><span class="line">  <span class="selector-tag">return</span> <span class="selector-tag">d</span>;</span><br><span class="line">  <span class="comment">// d 指的是数据数组中的每个元素(如果数组内是对象，带进来的就是对象)</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="如何绘制柱状图"><a href="#如何绘制柱状图" class="headerlink" title="如何绘制柱状图"></a>如何绘制柱状图</h2><ol>
<li>准备一个数组，里面是对象，每个对象都是等一下会呈现的长条。<figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">var vote</span> =[</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"Grete"</span>,</span><br><span class="line">    <span class="string">"num"</span>: 80</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"Steffi"</span>,</span><br><span class="line">    <span class="string">"num"</span>: 100</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"Lala"</span>,</span><br><span class="line">    <span class="string">"num"</span>: 200</span><br><span class="line">  &#125;,</span><br><span class="line">];</span><br></pre></td></tr></table></figure></li>
<li>在 HTML 准备一个空的 <code>&lt;div&gt;</code>，className 取为 <code>.chart</code>。</li>
<li>插入数据。<figure class="highlight scilab"><table><tr><td class="code"><pre><span class="line">d3.<span class="keyword">select</span>(<span class="string">'.chart'</span>)</span><br><span class="line">  .selectAll(<span class="string">'div'</span>)</span><br><span class="line">  .data(vote)</span><br><span class="line">  .enter()</span><br><span class="line">  .append(<span class="string">'div'</span>)</span><br><span class="line">  .html(<span class="function"><span class="keyword">function</span><span class="params">(d)</span>&#123;</span></span><br><span class="line">    <span class="comment">// .html() 类似 .innerHTML，D3 允许 SVG 跟 HTML 标签混用</span></span><br><span class="line">    <span class="keyword">return</span> d.name + <span class="string">'&lt;br&gt;'</span> + d.num</span><br><span class="line">  &#125;)</span><br><span class="line">  .style(<span class="string">"height"</span>, <span class="function"><span class="keyword">function</span><span class="params">(d)</span>&#123;</span></span><br><span class="line">    <span class="keyword">return</span> d.num + <span class="string">'px'</span>  <span class="comment">// 调整每个长条的高度</span></span><br><span class="line">  &#125;);</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="如何更新图表"><a href="#如何更新图表" class="headerlink" title="如何更新图表"></a>如何更新图表</h2><p>完成上一段的柱状图之后，如果之后有新数据要更新图表，应该怎么做呢？<br>以下的例子从原始数据创建图表开始，示范如何用新数据盖过旧数据。</p>
<ol>
<li>准备原始数据数组与新数据数组。<figure class="highlight actionscript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这是原始的数据</span></span><br><span class="line"><span class="keyword">var</span> vote =[</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"Grete"</span>,</span><br><span class="line">    <span class="string">"num"</span>: <span class="number">80</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"Steffi"</span>,</span><br><span class="line">    <span class="string">"num"</span>: <span class="number">100</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"Lala"</span>,</span><br><span class="line">    <span class="string">"num"</span>: <span class="number">200</span></span><br><span class="line">  &#125;,</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这是新的数据</span></span><br><span class="line"><span class="keyword">var</span> voteUpdate =[</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"Grete"</span>,</span><br><span class="line">    <span class="string">"num"</span>: <span class="number">120</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"Steffi"</span>,</span><br><span class="line">    <span class="string">"num"</span>: <span class="number">80</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"Lala"</span>,</span><br><span class="line">    <span class="string">"num"</span>: <span class="number">60</span></span><br><span class="line">  &#125;</span><br><span class="line">];</span><br></pre></td></tr></table></figure></li>
<li>使用上一段绘制柱状图的技巧，用原始数据把节点 (<code>node</code>) 都先创建出来。<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> node = d3</span><br><span class="line">  .<span class="keyword">select</span>(<span class="string">'.chart'</span>)</span><br><span class="line">  .selectAll(<span class="string">'div'</span>)</span><br><span class="line">  .data(vote)</span><br><span class="line">  .enter()</span><br><span class="line">  .<span class="built_in">append</span>(<span class="string">'div'</span>)</span><br></pre></td></tr></table></figure></li>
<li>把绘制图像的代码都放到一个函数里 (<code>update()</code>)，直接选择 <code>node</code> 进行渲染。函数化的目的是让每次替换节点里塞入的数据时，可以通过调用函数就重新绘制图像。<figure class="highlight actionscript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">update</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  node</span><br><span class="line">  .html(<span class="function"><span class="keyword">function</span><span class="params">(d)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> d.name + <span class="string">'&lt;br&gt;'</span> + d.num</span><br><span class="line">  &#125;)</span><br><span class="line">  .style(<span class="string">"height"</span>, <span class="function"><span class="keyword">function</span><span class="params">(d)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> d.num + <span class="string">'px'</span></span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line">update();</span><br></pre></td></tr></table></figure></li>
<li>最后，变更 <code>node</code> 塞入的 data，然后调用更新函数。<figure class="highlight crmsh"><table><tr><td class="code"><pre><span class="line"><span class="keyword">node</span>.<span class="title">data</span>(voteUpdate); // 带入新数据</span><br><span class="line">update();  // 重新渲染</span><br></pre></td></tr></table></figure>
<h2 id="小补充"><a href="#小补充" class="headerlink" title="小补充"></a>小补充</h2><h3 id="加入-className-的方法"><a href="#加入-className-的方法" class="headerlink" title="加入 className 的方法"></a>加入 className 的方法</h3>用这个方法可以把自己写的样式动态加到 DOM 上面：<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.attr</span>(<span class="string">"class"</span>, <span class="string">".d3-tip"</span>) <span class="comment">// 加入自定义的 className</span></span><br></pre></td></tr></table></figure>
<h3 id="插入字符串的方法"><a href="#插入字符串的方法" class="headerlink" title="插入字符串的方法"></a>插入字符串的方法</h3>用这个方法就等同于用 <code>.innerHTML</code> 插入字符串到 DOM 里面：<figure class="highlight actionscript"><table><tr><td class="code"><pre><span class="line">.html(<span class="function"><span class="keyword">function</span><span class="params">(d)</span></span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">"字符串"</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2>本篇笔记是我在<a href="https://www.youtube.com/watch?v=0PQ8VOsyjzw" target="_blank" rel="external nofollow noopener noreferrer">观看六角学院放在 Youtube 的教学</a>时写下的，不过我也收集了一些跟 D3.js 相关的文章，也放在这里提供给大家参考。</li>
</ol>
<ul>
<li><a href="https://medium.com/@mybaseball52/%E5%A2%9E%E5%BC%B7-d3-js-%E7%9A%84%E8%A6%96%E8%A6%BA%E5%8C%96%E5%8A%9F%E8%83%BD-68eaa66eef98" target="_blank" rel="external nofollow noopener noreferrer">增强 D3.js 的可视化功能</a></li>
<li><a href="https://ithelp.ithome.com.tw/users/20105602/ironman/1582?page=3" target="_blank" rel="external nofollow noopener noreferrer">让你数据美美的(d3.js+vue.js) 系列</a></li>
<li><a href="https://www.oxxostudio.tw/articles/201509/svg-d3-18-enter-update-exit.html" target="_blank" rel="external nofollow noopener noreferrer">SVG D3.js - Enter、Update 和 Exit</a></li>
</ul>
]]>
    </content>
    <categories>
      <category>D3.js</category>
    </categories>
    <tags>
      <tag>w3HexSchool</tag>
      <tag>D3.js</tag>
      <tag>DataDrivenDocuments</tag>
      <tag>数据可视化</tag>
      <tag>柱状图</tag>
      <tag>SVG</tag>
    </tags>
  </entry>
  <entry>
    <title>[UX] 讲座笔记：工程师必须了解的 UX 设计</title>
    <url>/UI-UX/20200210/3766866311/</url>
    <content>
      <![CDATA[<p>这篇笔记来自于 2020.2.6 六角学院的在线研讨会，讲者为兽群之心 / Soking，讲者在这一节讲座中分享了设计用户体验时需要掌握的基本原则，并且以丰富的实务经验来回答许多学员的发问。</p>
<a id="more"></a>

<h2 id="UX-的起源"><a href="#UX-的起源" class="headerlink" title="UX 的起源"></a>UX 的起源</h2><blockquote>
<p>用户经验或用户体验（User Experience，UX），一词由美国学者唐·诺曼所提出⋯⋯「用户经验」包含用户与公司的产品与服务互动中的所有面向。—— 维基百科</p>
</blockquote>
<p>UX 其实相当程度地根基于认知心理学。美国有名的认知心理学者 Don Norman 可说是 UX 祖师爷，他被邀请进入 Apple，协助 Apple 重视人类如何使用电脑、重视美学。</p>
<p>在以前的时代是由技术主导设计，现在则是设计师研究如何更 user friendly，电脑于是普及化。而当网络蓬勃发展后，UX 开始大量被需要。</p>
<p>Don Norman 创立的公司 Nielsen Norman Group 有提供 UX 培训，公司官网跟 YT 有一些 UX 文章与教学。但是这些 UX 教学都还是偏理论，所以容易看了很多但不知道怎么开始，这也是在台湾学 UX 容易遇到的问题。</p>
<p>不过用户体验应该是所有团队成员的责任，只要把用户需求放在心里，而不是只有商业面、技术面的话，其实就是在做 UX。</p>
<h2 id="产品可用性原则"><a href="#产品可用性原则" class="headerlink" title="产品可用性原则"></a>产品可用性原则</h2><p>产品可用性有三个指标：<br>有效性 (must have)：没有搞定产品就完蛋。例如：电商不能购物、地图不能定位。<br>效率 (good to have)：没做到会很麻烦。<br>满意度：没做到会有人不开心，不一定所有人都能满足，需要参考自己产品定位目标。</p>
<p>有效性跟效率之间的选择，应该看客户需要的是什么而提供，并不是追求两个都要有。</p>
<p>想了解网页 UX 更细节的优化建议可以参考 Google 研究员写的 <a href="https://services.google.com/fh/files/events/pdf_retail_ux_playbook.pdf?fbclid=IwAR2ATmQ-bsUuTfo5kyNqt1n0EulwDucB0m060Am4cHvHNtSYk5oC5Wn6mDI" target="_blank" rel="external nofollow noopener noreferrer">UX Playbook</a>（<a href="https://medium.com/@yachenyu/google-uxplaybook-ux%E8%A8%AD%E8%A8%88%E5%BB%BA%E8%AD%B0%E6%8C%87%E5%8D%97-%E9%9B%BB%E5%95%86-%E9%9B%B6%E5%94%AE%E9%87%8D%E9%BB%9E%E7%BF%BB%E8%AD%AF-3ae3585b6769" target="_blank" rel="external nofollow noopener noreferrer">中文版</a>），里面有列出一些共通的原则，做到的话体验会比较好。</p>
<h2 id="设计思考"><a href="#设计思考" class="headerlink" title="设计思考"></a>设计思考</h2><p>欢迎错误，也欢迎不同背景的人贡献想法。<br>UX 没有一步到位，必须先做出一个东西，测试用户的反应，再赶快改善。</p>
<h2 id="Site-Map"><a href="#Site-Map" class="headerlink" title="Site Map"></a>Site Map</h2><p>规范出页面层级、标明各页面的编号，页面中有共用组件、有要求权限的话也会标明。</p>
<h2 id="用户旅程地图"><a href="#用户旅程地图" class="headerlink" title="用户旅程地图"></a>用户旅程地图</h2><p>可以做得简单也可以做得复杂，可以把前中后台、用户场景、商业逻辑都用<strong>可视化</strong>的方式呈现。<br>可视化是 UX 设计师很重要的工作，把抽象的事情变具体，也能帮助讨论不要发散。</p>
<h2 id="Wireframe"><a href="#Wireframe" class="headerlink" title="Wireframe"></a>Wireframe</h2><p>工程师可以在 Wireframe 上确认规格跟使用流程。</p>
<h2 id="Q-amp-A"><a href="#Q-amp-A" class="headerlink" title="Q &amp; A"></a>Q &amp; A</h2><p>Q: 没办法从过去的 UX 案例来预估成效吗？<br>A: 过去的成功无法代表未来的成功。<del>跟投资买股票一样。</del></p>
<p>Q: 哪个阶段会开始请工程师？<br>A: 在第一天就会跟工程师讨论，画出一个边界，确认解决问题到什么程度，这其实是商业模式层次的问题。<br>UX 会先定义用户角色，接着做用户研究、在线访谈，使用产品的场景、频率、需求、流程，然后把收集到的信息拿去跟客户做讨论。如果发现有些指标适合量化，就会先列出来，提供之后规格的参考。</p>
<p>Q: 请问每一个案子都会做用户测试吗？如果会的话用户测试大约会找多少人测试呢？<br>A: 依照尼尔森原则，找 5 到 8 个人就能找出 85% 的问题。在做测试时应该追求快速有效的方法，找越多人测试就越旷日费时、成本越高。测试时，也会把时间限缩在 5 到 10 分钟就做完，并会对测试者进行放声思考法，引导他们讲出对特定界面、元素的认知。</p>
<p>Q: 在判断客户的使用情况是否流畅，可否观察用户在按下某个按钮后，过了多久时间才按下设计师所期待的用户的按钮，可以这样来判断吗? （如果很快就按下的话, 可以代表这个设计是符合UX吗？）<br>A: 我们会设置一个特定的任务，针对一定数量的用户去测他们进行任务的秒数，可以用 GA 查看有些比较久才进行特定任务的人是先去干嘛了。</p>
<p>Q: 请问，目前 UX 设计遇到的大环境威胁有哪些？例如：</p>
<ol>
<li>移动设备越做越小，导致能够设计的空间变少</li>
<li>客户会为了省钱，而直接要求你们做过的案例框架拿来应用，为了不愿多花设计费</li>
<li>案例越做越多，模仿的竞争者越来越多</li>
<li>其他？</li>
</ol>
<p>面对这些，你们是怎么克服的？</p>
<p>A:</p>
<ol>
<li>有限制是好的，因为画面小反而塞不了太多东西，Web 画面较大，可以塞的东西就多，业主常常会希望把画面塞得满满的，导致画面臃肿不堪，所以 Web 比 APP 还难设计。</li>
<li>每个业主的商业模式都不一样，所以不太会拿其他人的来应用。</li>
<li>市面上越多流畅的产品，越能够带动业界质量提升，所以竞争多是好事。</li>
</ol>
<p>Q: 有没有案子是在 iPad 上面做相关的测试？<br>A: 有，但当时客户不想单独测试 iPad，iPad 可以直着看也可以横着看，所以 UI 有把尺寸设计好就没问题。</p>
<p>Q: UI 报价高还是 UX？<br>A: 如果客户只想做 UI，我们会问他是否有能力自己做出 Wireframe？能做到什么程度？我们在跟客户告知我们的专业能力后，通常客户也愿意让我们同时做 UX 跟 UI。我们现在新客户都不会接只做 UI 的案子。</p>
<h2 id="彩蛋：UI-设计师是否该学-HTML-CSS？"><a href="#彩蛋：UI-设计师是否该学-HTML-CSS？" class="headerlink" title="彩蛋：UI 设计师是否该学 HTML CSS？"></a>彩蛋：UI 设计师是否该学 HTML CSS？</h2><p>讲者认为 UI 设计师应该要碰 HTML CSS，尤其现在案子要 RWD，如果画面都只用绘图软件做，其实设计师会有盲点，会不知道实际画面是怎么变化的。</p>
]]>
    </content>
    <categories>
      <category>UI/UX</category>
    </categories>
    <tags>
      <tag>UX</tag>
      <tag>userexperience</tag>
      <tag>用户体验</tag>
      <tag>w3HexSchool</tag>
    </tags>
  </entry>
  <entry>
    <title>[JS] 讲座笔记：函数式程序设计</title>
    <url>/javascript/20200119/2279697082/</url>
    <content>
      <![CDATA[<p>这篇笔记来自于 2020.1.9 六角学院的在线研讨会，讲者为苏泰安老师，老师不只讲解了如何善用函数来帮助代码更简洁，也介绍了一些以函数为编写基础的语言（例如 Elixir），不过由于我对 Javascript 以外的语言并不了解，所以这篇笔记并不会着墨于其他语言。</p>
<a id="more"></a>

<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在 JavaScript 函数是一级公民，可以传入另一个函数作为参数。<br>传入其他函数作为参数的函数称为 Callback function。<br>而接收其他函数当作自己的参数的则称为高阶函数。</p>
<h2 id="高阶函数-High-order-function-：处理函数的函数"><a href="#高阶函数-High-order-function-：处理函数的函数" class="headerlink" title="高阶函数(High order function)：处理函数的函数"></a>高阶函数(High order function)：处理函数的函数</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">cook</span> (<span class="params">food1,food2, f</span>) </span>&#123;</span><br><span class="line">  f(food1);</span><br><span class="line">  f(food2);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;food1&#125;</span> in <span class="subst">$&#123;food2&#125;</span>`</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">cook(<span class="string">'beef'</span>, <span class="string">'curry'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">x</span>) </span>&#123;<span class="built_in">console</span>.log(x)&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="高阶函数三大台柱：map-、reduce-、filter"><a href="#高阶函数三大台柱：map-、reduce-、filter" class="headerlink" title="高阶函数三大台柱：map()、reduce()、filter()"></a>高阶函数三大台柱：<code>map()</code>、<code>reduce()</code>、<code>filter()</code></h2><h3 id="map"><a href="#map" class="headerlink" title="map()"></a>map()</h3><p>对 A 数组进行某种处理，返回处理过后的新数组 B。</p>
<figure class="highlight angelscript"><table><tr><td class="code"><pre><span class="line">let result = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>].map(i =&gt; i * <span class="number">10</span>);</span><br><span class="line">console.log(result);</span><br><span class="line"><span class="comment">// [10,20,30,40,50]</span></span><br></pre></td></tr></table></figure>

<h3 id="filter"><a href="#filter" class="headerlink" title="filter()"></a>filter()</h3><p>从 A 数组中筛选符合某条件的值，符合条件的值以新数组 B 返回。</p>
<figure class="highlight angelscript"><table><tr><td class="code"><pre><span class="line">let result = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>].map(i =&gt; i % <span class="number">2</span> == <span class="number">0</span>);</span><br><span class="line">console.log(result);</span><br><span class="line"><span class="comment">// [2,4,6]</span></span><br></pre></td></tr></table></figure>

<h3 id="reduce"><a href="#reduce" class="headerlink" title="reduce()"></a>reduce()</h3><p>有顺序上的依赖，依次对数组内的值进行处理（累加、相乘等等）。</p>
<figure class="highlight angelscript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// for 循环的累加写法</span></span><br><span class="line">let ary = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>];</span><br><span class="line">let result = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (let i of ary)&#123;</span><br><span class="line">  result = result + i;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// reduce() 的累加写法</span></span><br><span class="line"><span class="keyword">let</span> result = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>].reduce(reducer, <span class="number">0</span>);</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">reducer</span>(<span class="params">accu, i</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> accu + i;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 起始值为 0</span></span><br><span class="line"><span class="comment">// accu 为一个暂存、累加的值，就如同上一段在 for 循环之前声明的 result</span></span><br><span class="line"><span class="comment">// 第一次递回：accu 为 0，i 为 1，相加为 1</span></span><br><span class="line"><span class="comment">// 第二次递回：accu 为 1，i 为 2，相加为 3</span></span><br><span class="line"><span class="comment">// 以下类推</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 起始值的型别跟最后运算完的型别一致</span></span><br><span class="line"><span class="comment">// 常用的起始值：0, 1, [], '', &#123;&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="循环跟-map-、reduce-、filter-的差别："><a href="#循环跟-map-、reduce-、filter-的差别：" class="headerlink" title="循环跟 map()、reduce()、filter() 的差别："></a>循环跟 <code>map()</code>、<code>reduce()</code>、<code>filter()</code> 的差别：</h3><p>循环一定要依次对值进行处理，就像手摇店员对一个接一个的客人进行结帐；数组方法不强调值的处理顺序，只强调处理的手法。</p>
<h2 id="闭包-Closure"><a href="#闭包-Closure" class="headerlink" title="闭包 Closure"></a>闭包 Closure</h2><p>利用闭包（closure）的作法，让函数有自己的私有变量，避免函数受到外在值的变动而被污染。当你看到一个函数内 return 了另一个函数，通常就是有用到闭包的概念。<br>参照：<a href="https://taian.su/2012-10-17-explaining-javascript-scope-and-closures-by-robert-nyman/" target="_blank" rel="external nofollow noopener noreferrer">Robert Nyman: 解释 JavaScript 的 scope 及 closures</a>、<a href="https://pjchender.blogspot.com/2017/05/javascript-closure.html" target="_blank" rel="external nofollow noopener noreferrer">深入浅出了解 JavaScript 闭包（closure）</a></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 以下情形会让函数的结果受到外在变量的污染</span></span><br><span class="line"><span class="keyword">let</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">let</span> foo = <span class="function"><span class="keyword">function</span>(<span class="params">i</span>) </span>&#123; <span class="keyword">return</span> a + i&#125;</span><br><span class="line"><span class="built_in">console</span>.log(foo(<span class="number">100</span>));</span><br><span class="line">a = <span class="number">99</span>;</span><br><span class="line"><span class="built_in">console</span>.log(foo(<span class="number">100</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 两次 console 结果不一样</span></span><br><span class="line"><span class="comment">// 因为函数会吃到参数以外的变量值</span></span><br><span class="line"><span class="comment">// 而且改变变量值时，也会影响到函数结果</span></span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fooDad</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> a = <span class="number">1</span>;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">i</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + i;</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> foo;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">const</span> foo = fooDad();</span><br><span class="line"><span class="built_in">console</span>.log(foo(<span class="number">100</span>));  <span class="comment">// 101</span></span><br><span class="line">a = <span class="number">99</span>;</span><br><span class="line"><span class="built_in">console</span>.log(foo(<span class="number">100</span>));  <span class="comment">// 101，不受污染</span></span><br></pre></td></tr></table></figure>

<h2 id="学员提问"><a href="#学员提问" class="headerlink" title="学员提问"></a>学员提问</h2><p>Q: 具名函数 VS 匿名函数的区别？<br>A: 具名函数可提升，匿名不能提升（会 <code>undefined</code>）。</p>
]]>
    </content>
    <categories>
      <category>JavaScript</category>
    </categories>
    <tags>
      <tag>functionalprogramming</tag>
      <tag>function</tag>
      <tag>函数</tag>
    </tags>
  </entry>
  <entry>
    <title>[Vue] Vue Router 配置与运作</title>
    <url>/uncategorized/20191226/433566073/</url>
    <content>
      <![CDATA[<p>在 SPA 的架构下，页面切换不是由后端操控，而是由前端模拟路由器来替换画面上的内容。<br>而我们可以通过 Vue Router 来达成这件事。</p>
<a id="more"></a>

<h2 id="Router-是由前端所模拟的路由器"><a href="#Router-是由前端所模拟的路由器" class="headerlink" title="Router 是由前端所模拟的路由器"></a>Router 是由前端所模拟的路由器</h2><p>一个使用 Vue Router 的项目会包含三个元素：</p>
<ul>
<li>进入点：main.js</li>
<li>Router 配置文件（前端路由）：router/index.js</li>
<li>分页内容：Vue Components (**.vue)</li>
</ul>
<h2 id="建置步骤"><a href="#建置步骤" class="headerlink" title="建置步骤"></a>建置步骤</h2><ol>
<li><p>下载 Vue Router：<code>npm install vue-router --save</code></p>
</li>
<li><p>在 <code>src</code> 文件夹里新建 <code>router</code> 文件夹：在里面添加一个 <code>index.js</code>，作为放置整个路由的配置文件</p>
</li>
<li><p><code>index.js</code> 一开始要写入的是官方组件：</p>
<figure class="highlight clean"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 官方组件</span></span><br><span class="line"><span class="keyword">import</span> Vue <span class="keyword">from</span> <span class="string">'vue'</span>;</span><br><span class="line"><span class="keyword">import</span> VueRouter <span class="keyword">from</span> <span class="string">'vue-router'</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>加载自定义组件，也就是把 <code>components</code> 文件夹内的 <code>.vue</code> 文件写入 <code>index.js</code>：</p>
<figure class="highlight clean"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 自定义的分页组件</span></span><br><span class="line"><span class="keyword">import</span> Home <span class="keyword">from</span> <span class="string">'src/components/HelloWorld'</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>启用 router：<code>Vue.use(VueRouter);</code></p>
</li>
<li><p>将 <code>index.js</code> 汇出给进入点（<code>main.js</code>）使用：</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在 index.js 继续写</span></span><br><span class="line"><span class="selector-tag">export</span> <span class="selector-tag">default</span> <span class="selector-tag">new</span> <span class="selector-tag">VueRouter</span>(&#123;</span><br><span class="line">  <span class="comment">// 数组内可放多组路径及对应的组件</span></span><br><span class="line">  <span class="attribute">routes</span>:[</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="attribute">name</span>: <span class="string">'首页'</span>, <span class="comment">// 组件呈现的名称</span></span><br><span class="line">      <span class="attribute">path</span>: <span class="string">'/index'</span>, <span class="comment">// 对应的虚拟路径</span></span><br><span class="line">      <span class="attribute">component</span>: Home, <span class="comment">// 对应的组件</span></span><br><span class="line">    &#125;,</span><br><span class="line">  ],</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li><p>在 <code>main.js</code> 导入 router：</p>
<figure class="highlight clean"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> router <span class="keyword">from</span> <span class="string">'./router'</span>;</span><br><span class="line"><span class="comment">// 再到 new Vue 下面加上 router 这个单字</span></span><br></pre></td></tr></table></figure></li>
<li><p>到 <code>App.vue</code>，在要<strong>呈现组件的地方</strong>用 <code>&lt;router-view&gt;&lt;/router-view&gt;</code> 替换</p>
</li>
<li><p>当网址 <code>/#</code> 后面接的是 <code>/index</code> 时，对应的组件就会呈现在画面上</p>
</li>
</ol>
<h2 id="添加路由路径及链接"><a href="#添加路由路径及链接" class="headerlink" title="添加路由路径及链接"></a>添加路由路径及链接</h2><ol>
<li>添加新的路由：基本上就是在上一段第 6 点的 router 数组里面添加对象</li>
<li>在 index.html 用 CDN 加载 BS，或是用 GUI 下载 BS 软件包</li>
<li>在 components 文件夹下添加 pages 文件夹，专门管理分页组件</li>
<li>把 HelloWorld.vue 另存为为 page.vue，存在 pages 里</li>
<li>整理 page.vue 里的内容，只留下 <code>&lt;script&gt;</code> 区域（<code>data</code> 可留空）及空的 <code>&lt;template&gt;</code>，把 BS 的卡片组件贴在 <code>&lt;template&gt;</code> 里面</li>
<li>回到 router 的 index.js，在 <code>import Home</code> 下面添加：<br><code>import Page form ‘/components/pages/page’;</code></li>
<li>在 router 数组里添加相关信息：<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  name: ‘分页’,</span><br><span class="line">  path: ‘/page’,</span><br><span class="line">  component: Page</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>在 App.vue 插入 BS navbar，制作分页导航栏，其中 a 链接的标签改为 <code>&lt;router-link&gt;</code>，并把 <code>href</code> 属性改为 <code>to=&quot;path值&quot;</code>，例如 <code>to=&quot;/page&quot;</code>；或是使用 <code>:to=&quot;{ name: &#39;分页&#39;}&quot;</code> 也有同样效果</li>
</ol>
<h2 id="巢状路由"><a href="#巢状路由" class="headerlink" title="巢状路由"></a>巢状路由</h2><p>刚刚在 page 分页中的卡片，如果希望卡片不变但是里面内容被抽换，要如何通过虚拟路由来达成呢？</p>
<ol>
<li>将 <code>page.vue</code> 另存成一个叫做 <code>child.vue</code> 的文件，<code>page.vue</code> 本身要把 <code>.card</code> 内层都删掉并改成 <code>&lt;router-view&gt;</code>（用来切换内容）</li>
<li><code>child.vue</code> 去掉 <code>.card</code> 外层，但要留一个没加 <code>className</code> 的 <code>&lt;div&gt;</code> 外层，另存为 <code>child2</code>、<code>child3</code></li>
<li>child 系列的文件要把卡片标题改一改</li>
<li>到 router 的 <code>index.js</code>，将 child 系列 <code>import</code> 进来，并在 <code>page</code> 所属的对象中添加 <code>children</code> 特性，<code>children</code> 对应的值是一个<strong>数组</strong>，数组内的格式跟外层对象一样<figure class="highlight less"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attribute">name</span>: <span class="string">'卡片 1'</span>, <span class="comment">// 组件呈现的名称</span></span><br><span class="line">  <span class="attribute">path</span>: <span class="string">''</span>, <span class="comment">// 这里空白代表 /page 显示的内容就是这个子组件</span></span><br><span class="line">  <span class="attribute">component</span>: child, <span class="comment">// 对应的组件</span></span><br><span class="line">&#125;,</span><br><span class="line">&#123;</span><br><span class="line">  <span class="attribute">name</span>: <span class="string">'卡片 2'</span>, <span class="comment">// 组件呈现的名称</span></span><br><span class="line">  <span class="attribute">path</span>: <span class="string">'child2'</span>,</span><br><span class="line">  <span class="attribute">component</span>: child2, <span class="comment">// 对应的组件</span></span><br><span class="line">&#125;,</span><br><span class="line">&#123;</span><br><span class="line">  <span class="attribute">name</span>: <span class="string">'卡片 3'</span>, <span class="comment">// 组件呈现的名称</span></span><br><span class="line">  <span class="attribute">path</span>: <span class="string">'child3'</span>,</span><br><span class="line">  <span class="attribute">component</span>: child3, <span class="comment">// 对应的组件</span></span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure></li>
<li>在 <code>page.vue</code> 使用 <code>&lt;router-link&gt;</code> 切换子分页<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">router-link</span> <span class="attr">to</span>=<span class="string">"/page/"</span>&gt;</span>卡片 1<span class="tag">&lt;/<span class="name">router-link</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">router-link</span> <span class="attr">to</span>=<span class="string">"/page/child2"</span>&gt;</span>卡片 2<span class="tag">&lt;/<span class="name">router-link</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">router-link</span> <span class="attr">to</span>=<span class="string">"/page/child3"</span>&gt;</span>卡片 3<span class="tag">&lt;/<span class="name">router-link</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h2 id="使用动态路由切换页面-AJAX-结果"><a href="#使用动态路由切换页面-AJAX-结果" class="headerlink" title="使用动态路由切换页面 AJAX 结果"></a>使用动态路由切换页面 AJAX 结果</h2>在同样的版型下，用网址后面的 id 决定带入的内容。<br>让我们把上一段的卡片 3 改成动态路由试试看！</li>
<li>把上一段的 <code>path: &#39;child3&#39;</code> 改成 <code>path: &#39;child/:id&#39;</code>，<code>id</code> 是一个可自定义名称的参数，这时 <code>:id</code> 就成为动态路由。</li>
<li>到卡片 3 的组件文件（<code>child3.vue</code>），在 <code>export default</code> 的大括号内增加一个 <code>created()</code>，这个方法会在组件生成时执行。</li>
<li>访问 API 中固定的一笔数据（以 <a href="https://randomuser.me/" target="_blank" rel="external nofollow noopener noreferrer">randomuser</a> 为例）：<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> id = <span class="keyword">this</span>.$route.params.id;</span><br><span class="line"><span class="comment">// route 已被挂载到 Vue 上，所以可以用 $ 调用</span></span><br><span class="line"><span class="comment">// params 是 router 的方法，可以读取 router 里的参数</span></span><br><span class="line"><span class="comment">// id 是我们自定义的参数</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// AJAX</span></span><br><span class="line"><span class="comment">// get() 中的网址参考 reandomuser 文件中的 Seeds 一节</span></span><br><span class="line"><span class="comment">// ?seed=$&#123;网址参数&#125;，且 API 网址整串要用反引号包住</span></span><br><span class="line"><span class="keyword">this</span>.$http.get(<span class="string">`https://randomuser.me/api/?seed=<span class="subst">$&#123;id&#125;</span>`</span>).then(<span class="function">(<span class="params">response</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(response);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
<li>在网址列随意打几个英文字，就能从 API 中抓取到特定数据。正式开发时，id 是由后端定义的，添加产品数据后会自动产生这个产品的 id ，前端要做的只是抓到这个 id ，并把数据显示出来。</li>
</ol>
<h2 id="命名路由，同一个路径加载两个组件"><a href="#命名路由，同一个路径加载两个组件" class="headerlink" title="命名路由，同一个路径加载两个组件"></a>命名路由，同一个路径加载两个组件</h2><p>为卡片多做一个导航栏。</p>
<ol>
<li>在 <code>App.vue</code> 的 <code>.container</code> 上方添加一个 <code>&lt;router-view&gt;</code> 并且设置 <code>name</code> 属性，例如 <code>name=&quot;menu&quot;</code>。</li>
<li><code>page.vue</code> 另存为为 <code>menu.vue</code>，<code>page.vue</code> 只留 <code>.card</code> 其余的 <code>&lt;router-link&gt;</code> 删掉，<code>menu.vue</code> 只留 <code>&lt;router-link&gt;</code> 其余删掉。</li>
<li>使用 BS nav 美化 menu，把 nav 代码贴进来，用 <code>&lt;router-link&gt;</code> 取代 nav 里面的 <code>&lt;a&gt;</code>，记得补上 <code>.nav-link</code>。</li>
<li>到 router 文件夹的 <code>index.js</code> 中将 <code>menu.vue</code> <code>import</code> 进来，并到下方的路由设置改写代码：<figure class="highlight dts"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原本使用 component 特性，改用 components 设置单一路由但多个组件</span></span><br><span class="line"><span class="symbol">components:</span> &#123;</span><br><span class="line"><span class="symbol">  default:</span> Page, <span class="comment">// 在 router-view 没有设置 name 时默认显示哪个组件</span></span><br><span class="line"><span class="symbol">  menu:</span> Menu <span class="comment">// 第一个 menu 是 router-view 的 name，第二个 Menu 是组件名</span></span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>
<h2 id="Vue-Router-参数设置"><a href="#Vue-Router-参数设置" class="headerlink" title="Vue Router 参数设置"></a>Vue Router 参数设置</h2>请搭配 Vue Router 官方文件。<br>Vue Router 的 API 分为 Router 构造配置及 Router 实例，这篇只介绍常用的 Router 配置。</li>
</ol>
<h3 id="Router-构造配置"><a href="#Router-构造配置" class="headerlink" title="Router 构造配置"></a>Router 构造配置</h3><p>构造配置用于 <code>index.js</code> 这个文件中，官方文件有介绍更多更详细的方法跟属性可用。</p>
<ul>
<li>routes 对象：官方文件有列出总共有哪些特性可以调整。</li>
<li>mode 字符串：决定网址的 <code>#</code> 是否要保留，或是做其他的设置。如果使用 <code>history</code> 模式，可以去除网址中的 <code>#</code>，但是必须配合后端使用。所以基本上还是以默认模式为主即可。</li>
<li>base 字符串：根路径设置的地方。</li>
<li>linkActiveClass 字符串：当某个路径被启用时会被加上特定的启用 <code>class</code>，在这个特性里可以设置路径启用的 className。</li>
<li>linkExactActiveClass 字符串：跟上一个差不多。</li>
</ul>
<h2 id="自定义切换路由方法"><a href="#自定义切换路由方法" class="headerlink" title="自定义切换路由方法"></a>自定义切换路由方法</h2><p>有些行为必须在 AJAX 结束后才去切换画面，此时 <code>router-link</code> 就不一定适用。</p>
<p>举例来说，当我们在电商网站买完东西、结帐送出后，需要进行一些 AJAX 的行为，得到数据后使用一些方法，使网页切换。</p>
<p>此时，我们应该运用组件 <code>methods</code> + Vue Router 的实例方法，来切换画面。</p>
<p>在官方文件的 API / Router 实例方法中，有许多我们能运用的方法，其中较常用的有：</p>
<ul>
<li><code>router.push()</code>：括号里放入<code>&#39;路径&#39;</code>。</li>
<li><code>router.back()</code>：回到上一页，括号里不需放参数。</li>
<li><code>router.forward()</code>：前进下一页，括号里不需放参数。</li>
<li><code>router.go()</code>：括号里放入数值，代表前进（正数）或后退（负数）的页数。</li>
<li><code>router.replace()</code>：括号里放入<code>&#39;路径&#39;</code>，通过这个方法前往指定路径，将不会记录在浏览器。</li>
</ul>
<h3 id="应用方式"><a href="#应用方式" class="headerlink" title="应用方式"></a>应用方式</h3><ol>
<li>在组件写一个 <code>&lt;a&gt;</code>，并绑定一个自定义的事件，通过这个事件来更新路径。<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">&lt;a <span class="attribute">href</span>=<span class="string">"#"</span> @click.<span class="attribute">prevent</span>=<span class="string">"updatePath"</span>&gt;切换到指定页面&lt;/a&gt;</span><br></pre></td></tr></table></figure></li>
<li>在组件里添加 <code>methods</code>，使用 <code>router.push()</code> 方法。<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line">methods: &#123;</span><br><span class="line">  updatePath() &#123;</span><br><span class="line">    <span class="keyword">this</span>.$router.<span class="keyword">push</span>(<span class="string">'/page/child2'</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
]]>
    </content>
    <categories>
      <category>Vuejs</category>
    </categories>
    <tags>
      <tag>VueRouter</tag>
    </tags>
  </entry>
  <entry>
    <title>[Hexo] 使用 NexT 主题如何在文章中内嵌 Codepen</title>
    <url>/hexo/20191217/2656253328/</url>
    <content>
      <![CDATA[<p>估狗估不到什么东西，还好自己有摸索成功，干脆来发一篇！</p>
<a id="more"></a>
<p>在搬运 Medium 文章过来的过程中遇到一个问题，就是我之前在 Medium 写文的时候超常 embed Codepen 的，毕竟有些效果真的有 Demo 辅助说明会比较好。<br>但在 hexo NexT 的环境下要怎么嵌入 Codepen 哩？</p>
<h2 id="第一次踩雷"><a href="#第一次踩雷" class="headerlink" title="第一次踩雷"></a>第一次踩雷</h2><p>我首先找到这篇文章： <a href="https://www.jianshu.com/p/79e808bbf0a8" target="_blank" rel="external nofollow noopener noreferrer">Hexo NexT：在文章中嵌入 CodePen或 jsFiddle</a>，但是我下载完软件包后，按照文中的方式在文章里插入模板，却跑出一堆错误信息，整篇文章的内容都无法渲染。</p>
<p>所以我只好舍弃这个方法，跑去翻翻 NexT 官方文件：<a href="https://hexo.io/zh-tw/docs/tag-plugins" target="_blank" rel="external nofollow noopener noreferrer">标签插件（Tag Plugins）</a>。<br>看到官方有提供插入 iframe 的语法，我就想说 Codepen 也有提供 iframe 代码，说不定行得通。</p>
<h2 id="Next-与-Codepen-iframe-语法"><a href="#Next-与-Codepen-iframe-语法" class="headerlink" title="Next 与 Codepen iframe 语法"></a>Next 与 Codepen iframe 语法</h2><p>Next 官方提供的语法：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% iframe url [width] [height] %&#125;</span><br></pre></td></tr></table></figure>

<p>Codepen 提供的 iframe 代码：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">iframe</span> <span class="attr">height</span>=<span class="string">"265"</span> <span class="attr">style</span>=<span class="string">"width: 100%;"</span> <span class="attr">scrolling</span>=<span class="string">"no"</span> <span class="attr">title</span>=<span class="string">"bs flex order"</span> <span class="attr">src</span>=<span class="string">"https://codepen.io/gretema/embed/eYOjPJx?height=265&amp;theme-id=default&amp;default-tab=html,result"</span> <span class="attr">frameborder</span>=<span class="string">"no"</span> <span class="attr">allowtransparency</span>=<span class="string">"true"</span> <span class="attr">allowfullscreen</span>=<span class="string">"true"</span>&gt;</span></span><br><span class="line">  See the Pen <span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">'https://codepen.io/gretema/pen/eYOjPJx'</span>&gt;</span>bs flex order<span class="tag">&lt;/<span class="name">a</span>&gt;</span> by 马维君</span><br><span class="line">  (<span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">'https://codepen.io/gretema'</span>&gt;</span>@gretema<span class="tag">&lt;/<span class="name">a</span>&gt;</span>) on <span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">'https://codepen.io'</span>&gt;</span>CodePen<span class="tag">&lt;/<span class="name">a</span>&gt;</span>.</span><br><span class="line"><span class="tag">&lt;/<span class="name">iframe</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="第二次踩雷"><a href="#第二次踩雷" class="headerlink" title="第二次踩雷"></a>第二次踩雷</h2><p>我把 Next 语法中的 <code>url</code> 代换成 Codepen 链接网址 (<code>https://codepen.io/gretema/pen/eYOjPJx</code>)，<code>[width]</code> 代换成 <code>[100%]</code>，<code>[height]</code> 代换成 <code>[265]</code>。<br>宽度跟高度的数值都是从 Codepen 提供的代码中取出来的。<br>呈现出来是这样：</p>
<iframe src="https://codepen.io/gretema/pen/eYOjPJx" width="[100%]" height="[265]" frameborder="0" loading="lazy" allowfullscreen></iframe>
<p>嗯…… Codepen 画面是出来了，但尺寸很有问题。</p>
<h2 id="终于成功啦"><a href="#终于成功啦" class="headerlink" title="终于成功啦"></a>终于成功啦</h2><p>最后，我尝试把 Codepen 提供的代码中，<code>&lt;iframe&gt;</code> 的 <code>src</code> 值取出来，放到 Next iframe 语法的 <code>url</code> 位置，而 <code>[width]</code> 跟 <code>[height]</code> 都去掉。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">&#123;% iframe https://codepen.io/gretema/embed/eYOjPJx?height=265&amp;theme-id=default&amp;default-tab=html,result %&#125;</span><br></pre></td></tr></table></figure>

<p>嵌入成功！</p>
<iframe src="https://codepen.io/gretema/embed/eYOjPJx?height=265&theme-id=default&default-tab=html,result" width="100%" height="300" frameborder="0" loading="lazy" allowfullscreen></iframe>
]]>
    </content>
    <categories>
      <category>Hexo</category>
    </categories>
    <tags>
      <tag>Hexo</tag>
      <tag>next</tag>
      <tag>codepen</tag>
    </tags>
  </entry>
  <entry>
    <title>[CSS] CSS3 flex 属性</title>
    <url>/htmlcss/20191217/214491870/</url>
    <content>
      <![CDATA[<p>我是在学习 Bootstrap 4 时才真正接触到这个 CSS3 的属性，在此之前都是用 float 属性在排版。但是用 float 的缺点就是清除浮动很麻烦，如果能够掌握 flex 属性的话就能解决这个问题，而且真的能够更加「弹性」地排版。</p>
<p>以下．就让我们来了解 flex 属性吧！</p>
<a id="more"></a>

<h2 id="轴线观念"><a href="#轴线观念" class="headerlink" title="轴线观念"></a>轴线观念</h2><p>以前学到的 float 只能水平排列，现在学到的 flex 能够同时做到水平跟垂直排列，也因此衍生出不同对齐方式，所以使用了 flex 的元素也就有了「轴线」这个东西。</p>
<p>在<strong>默认</strong>的情况下，水平方向的轴线称为<strong>主轴线</strong>，垂直方向的轴线称为<strong>交错轴</strong>，两种轴线都有<strong>起点</strong>跟<strong>终点</strong>，对象会从主轴线起点（左到右）开始排列。<br><img src="https://upload.cc/i1/2019/12/17/ir0BpN.png" alt></p>
<p>必须注意的是，当对象排列的方向改变时（上到下），主轴线跟交错轴的方向就会交换，垂直的轴线变成主轴线、水平的轴线变成交错轴。<br><img src="https://upload.cc/i1/2019/12/17/puywS6.png" alt="."></p>
<h2 id="一览-flex-相关属性"><a href="#一览-flex-相关属性" class="headerlink" title="一览 flex 相关属性"></a>一览 flex 相关属性</h2><p>可以用<strong>外层属性</strong>或<strong>内层属性</strong>来分类，也可以用<strong>是否为对齐方法</strong>来分类。<br>粗体为特别重要的属性。</p>
<h3 id="外层属性"><a href="#外层属性" class="headerlink" title="外层属性"></a>外层属性</h3><ul>
<li>display - 必备属性，声明 flex 排版</li>
<li>flex-flow<ul>
<li><strong>flex-direction</strong> - 决定 flex 轴线</li>
<li>flex-wrap - 决定换行的属性</li>
</ul>
</li>
<li><strong>justify-content</strong> - 主轴线的对齐</li>
<li><strong>align-items</strong> - 交错轴的对齐</li>
</ul>
<h3 id="内层属性"><a href="#内层属性" class="headerlink" title="内层属性"></a>内层属性</h3><ul>
<li>flex<ul>
<li>flex-grow - 伸展比</li>
<li>flex-shrink - 收缩比</li>
<li>flex-basis - 绝对值</li>
</ul>
</li>
<li>order - 排序</li>
<li><strong>align-self</strong> - 单一对象的交错轴对齐</li>
</ul>
<h3 id="对齐方法"><a href="#对齐方法" class="headerlink" title="对齐方法"></a>对齐方法</h3><h4 id="flex-direction-属性"><a href="#flex-direction-属性" class="headerlink" title="flex-direction 属性"></a><code>flex-direction</code> 属性</h4><p>决定主轴线方向，共有四个值。</p>
<ul>
<li>row：默认值，对象将沿着主轴线由左到右排列</li>
<li>row-reverse：反转主轴线，对象将沿着主轴线由右到左排列</li>
<li>column：将主轴线由水平改为垂直，对象将由上至下排列</li>
<li>column-reverse：将主轴线由水平改为垂直，对象将由下至上排列</li>
</ul>
<h4 id="justify-content-属性"><a href="#justify-content-属性" class="headerlink" title="justify-content 属性"></a><code>justify-content</code> 属性</h4><p>依据主轴线决定内层对象之间的间隔方式。共有五个值。</p>
<ul>
<li>flex-start：所有内层对象对齐主轴线起点</li>
<li>flex-end：所有内层对象对齐主轴线终点</li>
<li>center：所有内层对象置中在主轴在线</li>
<li>space-between：内层对象头尾在主轴线起讫点，其余对象等距对齐</li>
<li>space-around：所有内层对象之间等距对齐</li>
</ul>
<p><img src="https://upload.cc/i1/2019/12/17/S2noQc.png" alt></p>
<h4 id="align-items-属性"><a href="#align-items-属性" class="headerlink" title="align-items 属性"></a>align-items 属性</h4><p>依据交错轴决定内层对象的垂直对齐方式。共有五个值。</p>
<ul>
<li>flex-start：所有内层对象对齐交错轴起点</li>
<li>flex-end：所有内层对象对齐交错轴终点</li>
<li>center：所有内层对象在交错轴上置中</li>
<li>baseline：跟文字行高有关，用文字的基准线作排列（很少用）</li>
<li>stretch：所有内层对象拉高，由交错轴起点拉到终点</li>
</ul>
<p><img src="https://upload.cc/i1/2019/12/17/7n3Gbm.png" alt></p>
<h4 id="align-self-属性"><a href="#align-self-属性" class="headerlink" title="align-self 属性"></a>align-self 属性</h4><p>个别的内层组件本身的垂直对齐方式，此属性的值跟 <code>align-items</code> 一模一样。</p>
<h3 id="对齐以外的其他属性"><a href="#对齐以外的其他属性" class="headerlink" title="对齐以外的其他属性"></a>对齐以外的其他属性</h3><h4 id="flex-wrap-属性（外层）"><a href="#flex-wrap-属性（外层）" class="headerlink" title="flex-wrap 属性（外层）"></a>flex-wrap 属性（外层）</h4><p>决定对象超出范围时要不要换行。</p>
<p class="codepen" data-height="265" data-theme-id="default" data-default-tab="html,result" data-user="gretema" data-slug-hash="yLBqRaQ" style="height: 265px; box-sizing: border-box; display: flex; align-items: center; justify-content: center; border: 2px solid; margin: 1em 0; padding: 1em;" data-pen-title="bs flex flex-wrap">
  <span>See the Pen <a href="https://codepen.io/gretema/pen/yLBqRaQ" target="_blank" rel="external nofollow noopener noreferrer">
  bs flex flex-wrap</a> by 马维君 (<a href="https://codepen.io/gretema" target="_blank" rel="external nofollow noopener noreferrer">@gretema</a>)
  on <a href="https://codepen.io" target="_blank" rel="external nofollow noopener noreferrer">CodePen</a>.</span>
</p>
<script async src="https://static.codepen.io/assets/embed/ei.js"></script>

<p>示例中，原本 <code>.col-*</code> 会被 <code>.row</code> 包裹，但因为 Bootstrap 已经帮 <code>.row</code> 内置 <code>flex-wrap</code> 换行了，所以改用 <code>.d-flex</code> 包裹 <code>.col-*</code>。</p>
<h4 id="align-content（外层）"><a href="#align-content（外层）" class="headerlink" title="align-content（外层）"></a>align-content（外层）</h4><p>只有在换行（<code>flex-wrap</code>）的情况下可使用，用在有高度的外层容器上，会使内层对象在父元素内垂直对齐。</p>
<p class="codepen" data-height="265" data-theme-id="default" data-default-tab="html,result" data-user="gretema" data-slug-hash="mdbjzqw" style="height: 265px; box-sizing: border-box; display: flex; align-items: center; justify-content: center; border: 2px solid; margin: 1em 0; padding: 1em;" data-pen-title="bs flex align-content">
  <span>See the Pen <a href="https://codepen.io/gretema/pen/mdbjzqw" target="_blank" rel="external nofollow noopener noreferrer">
  bs flex align-content</a> by 马维君 (<a href="https://codepen.io/gretema" target="_blank" rel="external nofollow noopener noreferrer">@gretema</a>)
  on <a href="https://codepen.io" target="_blank" rel="external nofollow noopener noreferrer">CodePen</a>.</span>
</p>
<script async src="https://static.codepen.io/assets/embed/ei.js"></script>

<h4 id="order-属性（内层）"><a href="#order-属性（内层）" class="headerlink" title="order 属性（内层）"></a>order 属性（内层）</h4><p>用来更改区域排列的顺序，将 <code>order-*</code> 写在 class 里，<code>*</code> 代表我们希望区域排列的顺序。</p>
<p class="codepen" data-height="265" data-theme-id="default" data-default-tab="html,result" data-user="gretema" data-slug-hash="eYOjPJx" style="height: 265px; box-sizing: border-box; display: flex; align-items: center; justify-content: center; border: 2px solid; margin: 1em 0; padding: 1em;" data-pen-title="bs flex order">
  <span>See the Pen <a href="https://codepen.io/gretema/pen/eYOjPJx" target="_blank" rel="external nofollow noopener noreferrer">
  bs flex order</a> by 马维君 (<a href="https://codepen.io/gretema" target="_blank" rel="external nofollow noopener noreferrer">@gretema</a>)
  on <a href="https://codepen.io" target="_blank" rel="external nofollow noopener noreferrer">CodePen</a>.</span>
</p>
<script async src="https://static.codepen.io/assets/embed/ei.js"></script>

<hr>
<p>参考资料：<a href="https://wcc723.github.io/css/2017/07/21/css-flex/" target="_blank" rel="external nofollow noopener noreferrer">图解：CSS Flex 属性一点也不难</a></p>
<hr>
<p>看完这篇，保证你可以加入六角战队去<a href="https://hexschool.github.io/flexbox-pirate/index.html#/" target="_blank" rel="external nofollow noopener noreferrer">打海盗</a>！</p>
]]>
    </content>
    <categories>
      <category>HTML&amp;CSS</category>
    </categories>
    <tags>
      <tag>CSS3</tag>
      <tag>flexbox</tag>
    </tags>
  </entry>
  <entry>
    <title>[SASS] SASS 基础概念 2</title>
    <url>/SASS/20191217/3307620580/</url>
    <content>
      <![CDATA[<p>延续上一篇的主题，本篇将整理稍微进阶一些、但更接近实现的相关应用，包含如何使用 SASS 来提升响应式设计的开发效率。</p>
<a id="more"></a>

<h2 id="化简知识库：-mixin-设置常用语法，-include-召唤出来"><a href="#化简知识库：-mixin-设置常用语法，-include-召唤出来" class="headerlink" title="化简知识库：@mixin 设置常用语法，@include 召唤出来"></a>化简知识库：<code>@mixin</code> 设置常用语法，<code>@include</code> 召唤出来</h2><p>这个语法可以省去一直回想原理跟技巧的时间。</p>
<h3 id="基本用法：复制"><a href="#基本用法：复制" class="headerlink" title="基本用法：复制"></a>基本用法：复制</h3><ol>
<li>写在文件起始处，<code>@mixin</code> 后面取一个<strong>变量名</strong>接中括号，里面写该变量所欲达成的效果。</li>
<li>后面需要调用该效果时，先写选择器加中刮弧，里面先写 <code>@include</code>，后面加变量名称。</li>
<li><code>@mixin</code> 里面写的常用语法，会用「&amp;」来表示所在的选择器。例如：<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line">&amp;<span class="selector-pseudo">:after</span> &#123;</span><br><span class="line">  <span class="attribute">clear</span>: both;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h3 id="补充用法：参数"><a href="#补充用法：参数" class="headerlink" title="补充用法：参数"></a>补充用法：参数</h3><p><code>@mixin</code> 也可以设参数，而且可以是复数的参数。</p>
<p>例如，先在开头设置如下：</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@mixin</span> circle (<span class="variable">$size</span>, <span class="variable">$bgcolor</span>) &#123;</span><br><span class="line">  <span class="attribute">border-radius</span>: <span class="number">50%</span>;</span><br><span class="line">  <span class="attribute">height</span>: <span class="variable">$size</span>;</span><br><span class="line">  <span class="attribute">width</span>: <span class="variable">$size</span>;</span><br><span class="line">  <span class="attribute">font-size</span>: <span class="variable">$size</span> / <span class="number">3</span>;</span><br><span class="line">  <span class="attribute">background</span>: <span class="variable">$bgcolor</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>之后要在某选择器使用时，就可以简单用一行字召唤出来了。</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">  <span class="keyword">@include</span> circle (<span class="number">30px</span>, <span class="number">#fff</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="用途举例"><a href="#用途举例" class="headerlink" title="用途举例"></a>用途举例</h3><ul>
<li>图片取代文字</li>
<li>兼容各浏览器的 inline-block 的写法</li>
<li>各 CSS3 语法浏览器支持解决方案</li>
<li>清除浮动</li>
<li>用 CSS 画各方向的三角型</li>
</ul>
<blockquote>
<p>温馨提示：平常可以多累积自己的 mixin 数据库，设一个 mixin 文件夹把学过的一些 CSS 技巧记录成 .sass 文件存放进去。有些面试官光看应征者的 mixin 库就能了解这个人处理过多少项目了。</p>
</blockquote>
<h2 id="将-mixin-用于开发-RWD"><a href="#将-mixin-用于开发-RWD" class="headerlink" title="将 @mixin 用于开发 RWD"></a>将 <code>@mixin</code> 用于开发 RWD</h2><p>先用 <code>@mixin</code> 设置好断点，各断点可以取名为该装置的名字，例如 <code>pad</code>、<code>iPhone5</code> 等。<br>接着在 media query 里面只要加一个东西，那就是 <code>@content</code>。</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@mixin</span> iphone5&#123;</span><br><span class="line">  <span class="keyword">@media</span> (max-width: <span class="number">320px</span>) &#123;</span><br><span class="line">    <span class="keyword">@content</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>之后就先写 PC 版的样式，但在需要 RWD 的项目里面，可以用 <code>@include</code> 调用断点，然后直接写 RWD 的样式在里面。</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.header</span>&#123;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">100px</span>;</span><br><span class="line">  <span class="attribute">height</span>:<span class="number">100px</span>;</span><br><span class="line">  <span class="keyword">@include</span> iphone5&#123;</span><br><span class="line">    <span class="attribute">height</span>: <span class="number">30</span>; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>温馨提示：应该常备一个 <code>grid.scss</code> 专放用 <code>@mixin</code> 设置好的 media query 断点，在开发时就可以打开来复制到手上的新项目。</p>
</blockquote>
<hr>
<p>掌握了这两篇介绍的 SASS 基础，就相当够用了。当然，之后如果有机会再吸收一些 SASS 技巧也会继续分享的。</p>
]]>
    </content>
    <categories>
      <category>SASS</category>
    </categories>
    <tags>
      <tag>CSS</tag>
      <tag>SASS</tag>
    </tags>
  </entry>
  <entry>
    <title>[SASS] SASS 基础概念 1</title>
    <url>/SASS/20191217/1546590558/</url>
    <content>
      <![CDATA[<p>什么是 SASS？ <del>不是 SARS 哦</del><br>来聊聊它的基本观念。</p>
<a id="more"></a>

<h2 id="什么是-SASS？"><a href="#什么是-SASS？" class="headerlink" title="什么是 SASS？"></a>什么是 SASS？</h2><p>SASS 是一种针对 CSS 的脚本程序语言，借由提供程序语言的特性，例如变量、巢状结构、混合、函数与扩充（继承）等等，可以预先对 CSS 进行结构化的工作，最后再编译可使用于网页上的 CSS 语法。需要搭配 SASS 解译器 (如 prepros) 进行撰写。</p>
<p>SASS 包括两套语法。最开始的语法叫做「缩排语法」，使用缩排来区分代码块，并且用换行将不同规则分隔开，附文件名为 .sass。而较新的语法叫做「SCSS」，使用和 CSS 一样的块语法，即使用大括号将不同的规则分开，使用分号将具体的样式分开。</p>
<p>以下以 SCSS 语法为主，整理 SASS 的应用方法。</p>
<h2 id="写法：减少重工，不必重复写同样的选择器。"><a href="#写法：减少重工，不必重复写同样的选择器。" class="headerlink" title="写法：减少重工，不必重复写同样的选择器。"></a>写法：减少重工，不必重复写同样的选择器。</h2><p>子元素的样式直接写在父元素里即可。</p>
<h2 id="变量：便于管理，快速管理出同样设置的样式"><a href="#变量：便于管理，快速管理出同样设置的样式" class="headerlink" title="变量：便于管理，快速管理出同样设置的样式"></a>变量：便于管理，快速管理出同样设置的样式</h2><p>在最前面设一个变量名，例如 <code>linkcolor</code> ，名称前加一个 <code>$</code> 字号，并写上该变量代表的值。例：</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="variable">$linkcolor</span>: <span class="number">#000</span>;</span><br></pre></td></tr></table></figure>

<p>所有要做相同管理的属性后面，都接上变量名。例：</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.header</span> <span class="selector-tag">a</span> &#123;</span><br><span class="line">  <span class="attribute">color</span>: <span class="variable">$linkcolor</span></span><br><span class="line">&#125;</span><br><span class="line">.content a &#123;</span><br><span class="line">  color: <span class="variable">$linkcolor</span></span><br><span class="line">&#125;</span><br><span class="line">.footer a &#123;</span><br><span class="line">  color: <span class="variable">$linkcolor</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>以后若要全部替代成别的值，就在 <code>$linkcolor</code> (变量名) 后面作值的修改即可。</p>
<blockquote>
<p>那么，哪些项目适合用变量做全域设置？</p>
</blockquote>
<p>字型大小这种常常需要统一规格的项目就很适合。例：</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="attribute">font</span>-m: <span class="number">16px</span>;</span><br><span class="line"><span class="attribute">font</span>-l: font-m * <span class="number">1.2</span> // 支持加减乘除</span><br><span class="line">font-s: font-m * <span class="number">0.8</span></span><br></pre></td></tr></table></figure>


<h2 id="拆分文件：-import-让每支-scss-文件各司其职"><a href="#拆分文件：-import-让每支-scss-文件各司其职" class="headerlink" title="拆分文件：@import 让每支 .scss 文件各司其职"></a>拆分文件：<code>@import</code> 让每支 .scss 文件各司其职</h2><ol>
<li>一支 .scss 文件只单独处理一件事，例如 reset 写在一个 .scss 文件，文件名命名为 <code>_reset.scss</code>；首页样式写在另一个文件，文件名命名为 <code>_index.scss</code>。前面加<u>底线</u>很重要，代表那是<strong>合并用</strong>的 sass 文件，理论上并不会编译出对应的 .css 文件。</li>
<li>在名为 all 的 .scss 文件里做汇总，用 <code>@import</code>「依次」导入个别的 .scss 文件。网页中只会引入 <code>all.scss</code> 编译出来的 <code>all.css</code>。</li>
</ol>
]]>
    </content>
    <categories>
      <category>SASS</category>
    </categories>
    <tags>
      <tag>CSS</tag>
      <tag>SASS</tag>
    </tags>
  </entry>
  <entry>
    <title>[RWD] 响应式网页 - 实现常见问题与错误</title>
    <url>/rwd/20191217/296575981/</url>
    <content>
      <![CDATA[<p>乳蹄，本篇笔记非常简洁扼要地列出 3 点 RWD 要注意的问题。</p>
<a id="more"></a>

<h2 id="确认-media-query-断点"><a href="#确认-media-query-断点" class="headerlink" title="确认 media query 断点"></a>确认 media query 断点</h2><p>先抓热门的屏幕分辨率，例如平板 (768px) 及 iPhone 5 横式 (569px)，在这两个分辨率之间会有一个断层，可能在宽度 6xx px 时版面会变得比较难看，此时就为这一段断层加条件：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">@media(max-width: 767px) and (min-width: 569px)&#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="media-query-到底要写在一起还是分开写"><a href="#media-query-到底要写在一起还是分开写" class="headerlink" title="media query 到底要写在一起还是分开写"></a>media query 到底要写在一起还是分开写</h2><p>同一个 <code>&lt;div&gt;</code> 就写在一起，包含该 <code>&lt;div&gt;</code> 的子元素。</p>
<h2 id="不要习惯性写死高度"><a href="#不要习惯性写死高度" class="headerlink" title="不要习惯性写死高度"></a>不要习惯性写死高度</h2><p>在 PC 版上是可以写固定高度的，因为版面不会随屏幕大小而变化。但是要记得在 media query 中多加一行 <code>height: auto</code>，让内容自适应延伸。</p>
]]>
    </content>
    <categories>
      <category>RWD</category>
    </categories>
    <tags>
      <tag>CSS</tag>
      <tag>响应式网页设计</tag>
    </tags>
  </entry>
  <entry>
    <title>[RWD] 响应式网页 - 固定式菜单及 off-canvas 菜单</title>
    <url>/rwd/20191216/822299504/</url>
    <content>
      <![CDATA[<p>本篇文章接续上一篇，继续记录 RWD 的常用菜单语法结构，包含固定式菜单及侧边展开（off-canvas）菜单等两种。</p>
<a id="more"></a>

<h2 id="固定式菜单"><a href="#固定式菜单" class="headerlink" title="固定式菜单"></a>固定式菜单</h2><p>让用户在滑动页面时，表头永远在屏幕上方。<br>使用 <code>position: fixed</code>，但要注意页面内容会被表头遮住，所以内容区域要用 <code>padding-top</code> 空出表头的高度，才不会被遮住部分内容。</p>
<h3 id="注意事项："><a href="#注意事项：" class="headerlink" title="注意事项："></a>注意事项：</h3><ul>
<li><code>position: fixed</code> 在安卓部分旧型手机上不适用，若能用 JS 做兼容是较好的做法。</li>
<li>要考虑到表头是否会占屏幕太多版面，会的话就不要用 <code>fixed</code>。</li>
</ul>
<h3 id="语法："><a href="#语法：" class="headerlink" title="语法："></a>语法：</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line">@media(max-width: 767px)&#123;</span><br><span class="line">  <span class="selector-class">.header</span>&#123;</span><br><span class="line">    <span class="attribute">position</span>: fixed;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">    <span class="attribute">background</span>: black; &#125;</span><br><span class="line">  <span class="selector-class">.content</span>&#123;</span><br><span class="line">    <span class="attribute">padding-top</span>: <span class="number">81px</span>; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="off-canvas-菜单设计"><a href="#off-canvas-菜单设计" class="headerlink" title="off canvas 菜单设计"></a>off canvas 菜单设计</h2><p>适合用于选项超多的菜单，PC 版（放较少选项）跟手机版（放较多选项）可以各一套菜单设计。</p>
<h3 id="说明："><a href="#说明：" class="headerlink" title="说明："></a>说明：</h3><p>请搭配 <a href="https://codepen.io/gretema/pen/zeyxqO" target="_blank" rel="external nofollow noopener noreferrer">示例代码</a> 一同阅读。</p>
<ul>
<li>结构：<code>.container</code> 底下有 <code>.aside</code>（会滑出来的菜单）跟 <code>.main</code>（底下有 <code>.header</code>）</li>
<li>预期效果：点了表头中的 a 链接按钮（<code>.mobile-link</code>）就会让 <code>body</code> 动态加入 <code>.open</code>，之后 <code>.aside</code> 菜单从左侧滑出来，点 <code>.aside</code> 中的 a 链接按钮（<code>.mobile-close</code>）又滑回去。</li>
</ul>
<h3 id="语法：-1"><a href="#语法：-1" class="headerlink" title="语法："></a>语法：</h3><h4 id="CSS："><a href="#CSS：" class="headerlink" title="CSS："></a>CSS：</h4><ol>
<li><code>.aside</code> 设置绝对定位<br>用 <code>transform</code> ( 或 <code>margin</code> ) 设置 X 轴位移的距离 (=宽度)<br>注：<code>transform</code> 在网页上的性能比 <code>padding</code>、<code>margin</code> 都好<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.aside</span>&#123;</span><br><span class="line">  <span class="attribute">position</span>: absolute;</span><br><span class="line">  <span class="attribute">top</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">bottom</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">270px</span>;</span><br><span class="line">  height: 100%;;</span><br><span class="line">  <span class="selector-tag">overflow</span>: <span class="selector-tag">hidden</span>;</span><br><span class="line">  <span class="selector-tag">transform</span>: <span class="selector-tag">translateX</span>(<span class="selector-tag">-270px</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>核心代码<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.open</span> <span class="selector-class">.main</span>&#123;</span><br><span class="line">  <span class="attribute">transform</span>: <span class="built_in">translateX</span>(<span class="number">270px</span>);</span><br><span class="line">  // 点了 .header 会让 .menu 往右推 270 px 以容纳滑出来的菜单</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.open</span> <span class="selector-class">.aside</span>&#123;</span><br><span class="line">  <span class="attribute">transform</span>: <span class="built_in">translateX</span>(<span class="number">0px</span>);</span><br><span class="line">  // 点了 .header 会让菜单滑入窗口中</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>菜单滑入的动画效果 — <code>transition</code>，可以有<strong>帧效果</strong><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.main</span>&#123;</span><br><span class="line">  ...</span><br><span class="line">  <span class="selector-tag">transition</span>: 0<span class="selector-class">.3s</span> <span class="selector-tag">ease</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h4 id="jQ："><a href="#jQ：" class="headerlink" title="jQ："></a>jQ：</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$(<span class="string">'.mobile-link'</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params">event</span>) </span>&#123;</span><br><span class="line">  $(<span class="string">'body'</span>).addClass(<span class="string">'open'</span>);</span><br><span class="line">&#125;);</span><br><span class="line">$(<span class="string">'.mobile-close'</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params">event</span>) </span>&#123;</span><br><span class="line">  $(<span class="string">'body'</span>).removeClass(<span class="string">'open'</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<hr>
<p>好啦，响应式的菜单设计就先介绍四种，虽然菜单不是响应式设计的全部，但应该算是蛮核心的部分了，其他眉眉角角就等我之后有时间精力再来补写啰（瘫）。</p>
]]>
    </content>
    <categories>
      <category>RWD</category>
    </categories>
    <tags>
      <tag>CSS</tag>
      <tag>响应式网页设计</tag>
      <tag>HTML</tag>
      <tag>jQuery</tag>
    </tags>
  </entry>
  <entry>
    <title>[RWD] 响应式网页 - 多栏多列菜单及汉堡菜单</title>
    <url>/rwd/20191216/3809709620/</url>
    <content>
      <![CDATA[<p>本篇文章将记录 RWD 的常用菜单语法结构，包含多行多列菜单及汉堡菜单等两种。</p>
<a id="more"></a>

<h2 id="多栏多列菜单排版法：以-3-2-为例"><a href="#多栏多列菜单排版法：以-3-2-为例" class="headerlink" title="多栏多列菜单排版法：以 3*2 为例"></a>多栏多列菜单排版法：以 3*2 为例</h2><h3 id="语法："><a href="#语法：" class="headerlink" title="语法："></a>语法：</h3><h4 id="HTML："><a href="#HTML：" class="headerlink" title="HTML："></a>HTML：</h4><p><code>&lt;ul&gt;&lt;li&gt;</code> 结构，<code>&lt;li&gt;</code> 需要 6 个</p>
<h4 id="CSS："><a href="#CSS：" class="headerlink" title="CSS："></a>CSS：</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line">float: right;  // PC版排版</span><br><span class="line"><span class="keyword">@media</span> ( <span class="attribute">max-width:</span> <span class="number">767px</span>)&#123;</span><br><span class="line">  float: none;  // 自然排列，这是 RWD 的精髓</span><br><span class="line">  <span class="selector-tag">width</span>: 33<span class="selector-class">.33333</span> % ;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="汉堡菜单排版法"><a href="#汉堡菜单排版法" class="headerlink" title="汉堡菜单排版法"></a>汉堡菜单排版法</h2><p>请搭配 <a href="https://codepen.io/gretema/pen/bzQveq" target="_blank" rel="external nofollow noopener noreferrer">示例代码</a> 一同观看。</p>
<h3 id="汉堡菜单是怎么出现的"><a href="#汉堡菜单是怎么出现的" class="headerlink" title="汉堡菜单是怎么出现的"></a>汉堡菜单是怎么出现的</h3><p>在写 PC 版时，就预先写好了汉堡菜单的按钮（<code>&lt;a&gt;.showmenu</code>），只是用 <code>display: none</code> 先隐藏起来；汉堡菜单里的选项跟 PC 版其实是同一套，只是把 <code>float</code> 拿掉而已。</p>
<h3 id="PC-版菜单怎么变成汉堡菜单的"><a href="#PC-版菜单怎么变成汉堡菜单的" class="headerlink" title="PC 版菜单怎么变成汉堡菜单的"></a>PC 版菜单怎么变成汉堡菜单的</h3><h4 id="CSS：-1"><a href="#CSS：-1" class="headerlink" title="CSS："></a>CSS：</h4><blockquote>
<p>以下都是写在 <code>@media (max-width: 767px)</code> 的大括号里，在相对应的装置才会触发，1~3 都是 <code>.menu</code> 的样式。</p>
</blockquote>
<ol>
<li>隐藏菜单<strong>（关键语法）</strong>（之后会用 jQ 动态加入 <code>class</code>，就会展开）<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="attribute">max-height</span>: <span class="number">0px</span>;</span><br><span class="line"><span class="attribute">overflow</span>: hidden;</span><br></pre></td></tr></table></figure></li>
<li>绝对定位叠在网页上<figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="attribute">position</span>: absolute;</span><br><span class="line"><span class="attribute">z-index</span>: <span class="number">100</span>; <span class="comment">// 权重</span></span><br><span class="line"><span class="attribute">top</span>: <span class="number">81px</span>; <span class="comment">// 因为 header 80 px + border 1 px，若汉堡菜单要从 header 下方展开就要这样设</span></span><br><span class="line"><span class="attribute">left</span>: <span class="number">0</span>; <span class="comment">// 在绝对定位之下要做出满版效果，左右都要0</span></span><br><span class="line"><span class="attribute">right</span>: <span class="number">0</span>;</span><br></pre></td></tr></table></figure></li>
<li>菜单展开的渐变效果<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">transition</span>: <span class="selector-tag">max-height</span> 2<span class="selector-class">.3s</span>;</span><br></pre></td></tr></table></figure></li>
<li>取消隐藏汉堡菜单的按钮<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.showmenu</span>&#123;</span><br><span class="line">  <span class="attribute">display</span>: block;</span><br><span class="line">  <span class="attribute">float</span>: right;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">1</span> em;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>配合 jQ 指令的 CSS 语法：点击后，动态在 <code>body</code> 加上 <code>class</code><figure class="highlight mel"><table><tr><td class="code"><pre><span class="line">.<span class="keyword">menu</span>-show .<span class="keyword">menu</span>&#123;</span><br><span class="line">  <span class="keyword">max</span>-height: <span class="number">500</span>px; <span class="comment">// 菜单的高度，原本是设成 0 px</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="jQ："><a href="#jQ：" class="headerlink" title="jQ："></a>jQ：</h4>点击汉堡按钮（<code>.showmenu</code>） 后，取消默认链接功能，动态在 <code>body</code> 加上 <code>class</code>（<code>.menu-show</code>）。<figure class="highlight arcade"><table><tr><td class="code"><pre><span class="line">$(<span class="string">'.showmenu'</span>).on(<span class="string">'click'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>)</span>&#123;</span><br><span class="line">  e.preventDefault();</span><br><span class="line">  $(<span class="string">'body'</span>).toggleClass(<span class="string">'menu-show'</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<code>body</code> 加上 <code>.menu-show</code> 之后，<code>body</code> 中的 <code>.menu</code> 才会被触发，否则按了也不会展开。</li>
</ol>
<h3 id="menu-show-为何是加在-body-上"><a href="#menu-show-为何是加在-body-上" class="headerlink" title=".menu-show 为何是加在 body 上"></a>.menu-show 为何是加在 body 上</h3><p>如果 jQ 是在 <code>.menu</code> 上动态加上 <code>class</code> 会怎么样呢？</p>
<p>其实要在哪里动态加上 <code>class</code>，需要看网页的样式怎么设计。像是有些设计可能不只影响 <code>.menu</code>，还会加上其他的效果，那就必须要加在 <code>body</code> 上。<br>若只有增加弹出菜单的效果，那 <code>.menu-show</code> 其实不一定要在 <code>body</code> 上， 加在 <code>.header</code> 、加在 <code>.menu</code> 都可以。</p>
]]>
    </content>
    <categories>
      <category>RWD</category>
    </categories>
    <tags>
      <tag>CSS</tag>
      <tag>响应式网页设计</tag>
      <tag>HTML</tag>
      <tag>jQuery</tag>
    </tags>
  </entry>
  <entry>
    <title>[RWD] 响应式网页 - 常用菜单样式与注意事项</title>
    <url>/rwd/20191216/3160700070/</url>
    <content>
      <![CDATA[<p>在学习响应式网页设计（Responsive Web Design）的路上，想记录一些学习过程跟基础语法，本篇笔记将从 RWD 常用菜单的样式及实现时的注意事项开始这个系列的旅程。</p>
<a id="more"></a>

<h2 id="常用样式"><a href="#常用样式" class="headerlink" title="常用样式"></a>常用样式</h2><ul>
<li>一行式横幅菜单：每格菜单的字数不能太多，文字总宽不能超过设备的宽度，例如一个字大约是 16px，在 iPhone 5 (320px) 上就不能超过 20 字。除了单行排列，也有 3*2 之类多栏多列的排版方式。但不要超过两行，因为排到三行就占太多版面了。</li>
<li>汉堡菜单：点开时覆盖部分网页内容，可以用下拉式，也可用从左 / 右方滑出的方式。</li>
</ul>
<h2 id="实现注意事项"><a href="#实现注意事项" class="headerlink" title="实现注意事项"></a>实现注意事项</h2><ul>
<li>要避免菜单点开后把原本网页内容往下挤的状况，因为这样浏览器在渲染网页的时候性能会降低。</li>
<li>抓 320px 为最小值：这是 iPhone 5 、SE 的宽度，大约可以放 20 个字符（一格选项不超过4个字），不过考虑到留白的问题，12~16 个字符就差不多了。</li>
<li>不放奇数菜单：3 个选项的菜单还可以刚好用 33% 去处理，5 个选项的菜单排起来会很尴尬地空一格出来，所以要与客户沟通。</li>
</ul>
]]>
    </content>
    <categories>
      <category>RWD</category>
    </categories>
    <tags>
      <tag>CSS</tag>
      <tag>响应式网页设计</tag>
    </tags>
  </entry>
  <entry>
    <title>[CSS] 浮动 (float) 与清除浮动 (clear)</title>
    <url>/htmlcss/20191216/714228330/</url>
    <content>
      <![CDATA[<p>由于老师耳提面命，<code>float</code> 及 <code>clear</code> 是网页排版的基石之一，所以决定来汇整一下相关的数据，刚好某种程度上也可以承接上一篇的主题。</p>
<a id="more"></a>

<h2 id="float-属性的使用时机"><a href="#float-属性的使用时机" class="headerlink" title="float 属性的使用时机"></a>float 属性的使用时机</h2><p>使用诸如 <code>&lt;div&gt;</code> 之类的区域元素排版时，默认的排列方式都是由左至右、由上至下。但如果我们今天想要做的是多栏式排版，或是让多个区域元素并排时，就需要使用 <code>float</code> 属性。</p>
<h2 id="float-属性的值"><a href="#float-属性的值" class="headerlink" title="float 属性的值"></a>float 属性的值</h2><figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="attribute">float</span>: 浮动方向 ;</span><br></pre></td></tr></table></figure>
<p>浮动方向可以用的值有 <code>left</code>、<code>right</code>、<code>none</code>（默认值，也就是不浮动）以及 <code>inherit</code>（继承自父层的属性），最后这个 <code>inherit</code> 尽量不要使用，避免 IE 不支持的情况发生。</p>
<h2 id="clear-属性的使用时机"><a href="#clear-属性的使用时机" class="headerlink" title="clear 属性的使用时机"></a>clear 属性的使用时机</h2><p>如果想控制浮动元素的排版，就一定要懂 <code>clear</code> 属性的操作方式。</p>
<p>有时候，浮动元素在前，而后面的元素没有使用浮动时，后面的元素就会往上跑、叠在前一个元素的下方；有时候，父层内的元素使用浮动，会让父层包不住子元素。</p>
<p>遇到这种情形时，就要使用 clear 属性来处理跑版问题。</p>
<p><img src="https://upload.cc/i1/2019/12/17/YZure6.jpeg" alt="父层包不住子元素"></p>
<h2 id="clear-属性的使用方法"><a href="#clear-属性的使用方法" class="headerlink" title="clear 属性的使用方法"></a>clear 属性的使用方法</h2><figure class="highlight scss"><table><tr><td class="code"><pre><span class="line"><span class="attribute">clear</span>: 清除方向 ;</span><br></pre></td></tr></table></figure>
<p><code>clear</code> 属性可以用的值有 <code>left</code>、<code>right</code>、<code>both</code>、<code>none</code> 以及 <code>inherit</code>（IE不支持）， 其中以 <code>both</code> 最常用。</p>
<p><code>clear</code> 属性可以有 3 种使用方法：</p>
<ol>
<li>在浮动元素的后面多加一个 <code>&lt;div&gt;</code>（命名为 <code>.clearfix</code>），并在 <code>.clearfix</code> 使用 <code>clear: both</code> 语法。</li>
<li>在要清除浮动内容的父层多加一个 <code>.clearfix</code> 并使用伪元素。★<br><img src="https://upload.cc/i1/2019/12/17/cZz5wI.jpeg" alt="after 伪元素：通过 content 在元素的后面生成了内容为一个点的区域元素"></li>
<li>在浮动元素后面的区域元素加上 <code>clear: both</code>。</li>
</ol>
<blockquote>
<p>使用 <code>clear</code> 属性须注意，在 <code>&lt;ul&gt;&lt;li&gt;</code> 结构中，必须在 <code>&lt;ul&gt;</code> 父元素清除子元素，没办法在 <code>&lt;li&gt;</code> 后面插入 <code>.clearfix</code>。</p>
</blockquote>
]]>
    </content>
    <categories>
      <category>HTML&amp;CSS</category>
    </categories>
    <tags>
      <tag>CSS</tag>
      <tag>float</tag>
      <tag>clear</tag>
    </tags>
  </entry>
  <entry>
    <title>[CSS] 没有清除浮动，还是没搞清楚元素特性？</title>
    <url>/htmlcss/20191216/3595727743/</url>
    <content>
      <![CDATA[<p>昨天在六角学院社团看到有人发问排版问题，因为是我第一次试着替别人 debug 因此特别想记录下来，顺便当作我使用博客记录前端学习之路的滥觞。</p>
<a id="more"></a>

<hr>
<p>这位同学的问题是这样的：</p>
<blockquote>
<p>「 请问大家，相似的浮动为什么第一张图 submit 会跑掉，在 textarea 结束加 clearfix 才正常（第二张图），第三张就不会？」</p>
</blockquote>
<p><img src="https://upload.cc/i1/2019/12/17/LlC0gj.jpeg" alt="图 1"><br><img src="https://upload.cc/i1/2019/12/17/WDRnSZ.jpeg" alt="图 2"><br><img src="https://upload.cc/i1/2019/12/17/vwK2S3.jpeg" alt="图 3"></p>
<p>我这样回答：</p>
<blockquote>
<p>「因为第一张 textarea 没有下 <code>display:block</code>，所以按钮就跟它并排了；而第三张的 input text 都有下 <code>display:block</code>，所以按钮就不会并排。」</p>
</blockquote>
<p><code>&lt;input&gt;</code> 跟 <code>&lt;img&gt;</code>，<code>display</code> 默认都是 <code>inline-block</code>，也就是说如果不指定为 <code>block</code>，排完它的宽度后，如果还有空间，底下非 <code>block</code> 的元素或浮动元素就会跑上来。<code>inline</code> 跟 <code>inline-block</code> 的差别在于，前者不能指定宽高，而后者可以。</p>
<p>第一个图会跑版的原因是因为 <code>&lt;textarea&gt;</code> 没有下 <code>display:block</code>，所以 <code>float</code> 的按钮便与他并排了。<br>并排之后整个 <code>&lt;form&gt;</code> 加上左边的 <code>&lt;h6&gt;</code> 的宽度后，整体超过了 <code>.faqform</code>，所以才会掉下来。</p>
<p>第二个图里，<code>&lt;textarea&gt;</code> 下面多加了一块 <code>&lt;div&gt;</code>，这个 <code>&lt;div&gt;</code> 的属性是 <code>block</code> ，所以不是因为加了 <code>clearfix</code>，如果把那个 <code>class</code> 去掉，只留下 <code>&lt;div&gt;</code>，也会有同样效果。如此，接下来的按钮便不会前面的原素并排，整个 <code>&lt;form&gt;</code> 加上左边 <code>&lt;h6&gt;</code> 的宽度后也还在 <code>.faqform</code> 的极限内，版型就正常了。</p>
<p>第三个图版型正常的原因，也是因为前面的元素加了 <code>block</code>，而且整个 <code>.form</code> 加上左边 <code>&lt;h4&gt;</code> 的宽度后还在 <code>.faqform</code> 的极限内。</p>
<p>此外，盒模型的概念也是很重要，子元素要注意宽度加起来是否会超过包住它的父层， 不然一旦内容宽度超过就一定会跑版了。</p>
]]>
    </content>
    <categories>
      <category>HTML&amp;CSS</category>
    </categories>
    <tags>
      <tag>CSS</tag>
      <tag>float</tag>
      <tag>display</tag>
    </tags>
  </entry>
</search>